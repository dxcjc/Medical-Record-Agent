# 任务：修复 Provider 设置页面

## 问题
1. 侧边栏 "Provider 设置" 链接指向 `/settings/providers`，实际路由是 `/providers` — 需要修复侧边栏链接
2. Provider 设置页面显示 4 个卡片（OCR、LLM、Storage、LIMS），都是 placeholder 数据
3. 需要接入真实可用的 OCR 服务

## 要求
1. **修复侧边栏链接** — 找到侧边栏组件，把 Provider 设置的链接从 `/settings/providers` 改为 `/providers`
2. **OCR Provider 接入真实服务** — 配置一个可用的 OCR 服务端点。选项：
   - 如果服务器上有 PaddleOCR 或 Tesseract.js 可用，配置为本地 OCR
   - 如果没有，保留当前配置但标记为"未配置真实服务"
3. **确保 Provider 设置页面能正常访问** — 从侧边栏点击能正确跳转

## 技术上下文
- 项目目录：/tmp/Medical-Record-Agent
- 前端：apps/demo-web/src/
- 侧边栏组件：需要查找（可能是 apps/demo-web/src/components/Sidebar 或 layout 目录下的组件）
- Provider 设置页面：apps/demo-web/src/pages/operations/ProviderSettingsPage.tsx
- 路由配置：apps/demo-web/src/App.tsx — `{ path: "providers", element: <ProviderSettingsPage /> }`
- API 端口：3000（后端），9901（前端）

## 完成标准
- 侧边栏点击 "Provider 设置" 能正确跳转到 /providers
- Provider 页面能正常显示
- 运行 `corepack pnpm --filter @medical-record-agent/demo-web build` 编译通过
- 输出审计报告（修改了什么、编译结果、当前 Provider 状态）
