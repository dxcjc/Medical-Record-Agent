【重要】不要问问题，不要请求确认，不要启动浏览器询问确认。直接执行。必须遵循 superpowers 流程：brainstorming → writing-plans → TDD/测试优先 → verification-before-completion。

任务：继续推进医疗项目 P1/P2 业务/安全/集成生产化闭环，不要停在 UI 阶段，不要把 UI 阶段通过误判为项目最终完成。

工作目录：/tmp/Medical-Record-Agent

必须先读取：
1. .codex-medical-p1-p2-continuation.md
2. PRODUCT-AUDIT-REPORT.md
3. MEDICAL-P2-ADAPTER-HARDENING-AUDIT-REPORT.md
4. MEDICAL-P2-INTEGRATION-HARDENING-AUDIT-REPORT.md
5. MEDICAL-P2-PRODUCTION-CLOSURE-AUDIT-REPORT.md
6. docs/2026-06-09-p2-production-handoff.md

当前已知状态：
- UI 当前阶段通过，保持 Material + Arco Design，不要粗暴重写 CSS。
- 本地 P1/P2 工程化阶段已有多个报告显示通过。
- 医疗项目最终产品仍不通过，remaining/blocked 包括真实外部 OCR/LLM/LIMS sandbox、真实 KMS/Vault/Secret Manager、真实 broker 多实例可靠队列、真实 production smoke。
- 这些外部条件缺失时必须写 blocked，不能伪造通过。

本轮目标：在不需要真实外部凭据的前提下，继续补齐“部署可执行/交接可验证”的生产 readiness 缺口，让接入真实环境时有明确 gate、脚本、文档和测试。不要重复已完成的 UI patch。

请先审计当前代码和报告，自主选择最有价值的可落地项，至少覆盖以下方向中的 2-3 项：
1. 生产 readiness gate：增加或强化脚本/测试，汇总 typecheck/test/demo-web build/smoke:demo-web/browser E2E/mock-production smoke/real production blocked 状态，输出清晰 JSON 或文本摘要；真实外部缺失必须标记 blocked。
2. 外部依赖配置校验：强化 queue broker、secret resolver、production smoke 的配置校验和错误信息，确保部署方一眼能知道缺什么。
3. Handoff 文档：更新 docs/2026-06-09-p2-production-handoff.md 或新增部署检查清单，明确真实 KMS、真实 broker、多实例 worker、sandbox smoke 的验收命令与通过/blocked/fail 判定。
4. 测试稳定性：补充相关单测/脚本测试，避免报告与实际命令不一致；不要只写报告。

硬性约束：
- 不要提交 git commit。
- 不要修改 .env、node_modules、无关缓存。
- 不要重写现有 Material + Arco CSS；如必须改 UI/CSS，只做最小增量并保持 #3370FF、#F7F8FA、白色侧栏、active nav pill、DM Sans + Noto Sans SC、移动端抽屉/单列/44px 触摸区。
- 不要把 mock-production 或 blocked smoke 写成真实生产通过。
- 如果遇到 429/上游断流，保留已完成改动和报告草稿，报告中明确中断点，不要反复重试。

必须执行并在报告中如实记录：
- corepack pnpm typecheck
- corepack pnpm test
- corepack pnpm --filter @medical-record-agent/demo-web test:styles
- corepack pnpm --filter @medical-record-agent/demo-web test:mobile
- corepack pnpm --filter @medical-record-agent/demo-web build
- corepack pnpm smoke:demo-web
- 如可行：corepack pnpm e2e:demo-web:browser
- corepack pnpm smoke:production（缺真实条件时应为 blocked，不要写通过）
- 如已有 mock-production 模式：PRODUCTION_SMOKE_MODE=mock-production PRODUCTION_SMOKE_RUN_WRITEBACK=1 corepack pnpm smoke:production

必须产出：
1. MEDICAL-P2-DEPLOYMENT-READINESS-FIX-REPORT.md：说明本轮修复点、文件、测试命令、仍 blocked 的外部条件。
2. MEDICAL-P2-DEPLOYMENT-READINESS-AUDIT-REPORT.md：必须包含 7 个维度：
   - 产品概述
   - 功能完整性
   - 业务流程完整性
   - 用户体验
   - 技术实现
   - P0/P1/P2 问题清单
   - 验收结论（必须分层说明：UI 阶段、P1/P2 readiness 阶段、真实外部集成、医疗最终产品）

验收口径：
- 可以写“UI 当前阶段通过”。
- 可以在本地命令全部通过后写“P1/P2 deployment readiness 阶段通过”。
- 真实 OCR/LLM/LIMS、真实 KMS/Vault/Secret Manager、真实 broker 多实例 smoke 缺失时必须写 blocked。
- 医疗项目最终产品仍不得写通过，除非真实外部条件全部满足并 smoke 通过。