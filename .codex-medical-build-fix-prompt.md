# Codex 任务：医疗项目构建稳定性修复 + 审计报告更新

【重要】不要问问题，不要请求确认，不要启动浏览器交互询问。直接执行。必须遵循 superpowers 流程：brainstorming → writing-plans → TDD/测试优先 → verification-before-completion。

工作目录：/tmp/Medical-Record-Agent

## 背景
医疗项目已经完成一轮 Material + Arco Design UI 改造，但当前调度复验发现 demo-web 构建失败，原因是新增测试引用了不存在的模块：
- `src/api/queryKeys.test.ts` 找不到 `./queryKeys`
- `src/components/AppFeedback.test.ts` 找不到 `./AppFeedback`
- `src/components/MedicalDataTable.test.ts` 找不到 `./MedicalDataTable`
- `MedicalDataTable.test.ts` 还有 row implicit any

同时已有 `MEDICAL-ARCO-UI-AUDIT-REPORT.md` 声称 build 通过，但当前实际 build 未通过，所以报告必须更新为真实状态。

## 任务目标
修复 demo-web 构建稳定性，并保持现有企业级 Material + Arco Design UI，不允许粗暴重写 `styles.css`，只做必要增量修复。

## 必须完成
1. 修复 `corepack pnpm --filter @medical-record-agent/demo-web build` 的 TypeScript/构建失败。
2. 保留并验证：
   - `corepack pnpm --filter @medical-record-agent/demo-web test:styles`
   - `corepack pnpm --filter @medical-record-agent/demo-web test:mobile`
   - `corepack pnpm --filter @medical-record-agent/demo-web build`
3. 如果新增/补齐组件或 API helper：
   - 必须符合医疗项目语义和 Material + Arco Design 体系。
   - 组件应使用 Arco 组件或与现有 UI 风格兼容。
   - 不要删除测试来绕过构建。
4. 检查 9901 运行入口相关文件，不破坏：
   - `apps/demo-web/src/main.tsx`
   - `apps/demo-web/src/styles.css`
   - `apps/demo-web/src/layouts/AppShell.tsx`
   - `apps/demo-web/src/pages/auth/LoginPage.tsx`
5. 生成/更新报告：
   - `STYLE-FIX-REPORT.md`：说明本轮修复了什么、验证命令结果、剩余风险。
   - 更新 `MEDICAL-ARCO-UI-AUDIT-REPORT.md`：必须是 7 维度中文审计报告，且构建结论必须与本轮真实验证一致。

## 审计报告 7 维度
`MEDICAL-ARCO-UI-AUDIT-REPORT.md` 必须包含：
1. 产品概述
2. 功能完整性
3. 业务流程完整性
4. 用户体验
5. 技术实现
6. P0/P1/P2 问题清单
7. 验收结论

## 验收标准
- 上述三个 demo-web 命令全部通过。
- `STYLE-FIX-REPORT.md` 存在且内容不是空壳。
- `MEDICAL-ARCO-UI-AUDIT-REPORT.md` 存在且 7 维度齐全。
- 不要提交 git commit。
- 不要修改 `.env`、`node_modules`、缓存目录。
