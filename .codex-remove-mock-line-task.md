【必须遵循 superpowers 流程】
读取项目内已有计划/报告；你必须按 brainstorming → writing-plans → TDD/测试优先 → verification-before-completion 执行。

任务：Medical Record Agent 去掉用户可见 mock 线路，恢复真实操作路径，并生成修复与审计报告。

背景：用户反馈“去掉 mock 线路吧，现在根本就不会操作了”。当前 demo-web/API 里大量用户可见 mock 文案和默认 mock provider 会误导实际操作：顶部显示 Mock Provider Ready，新建识别默认 mock-ocr/mock-model，Provider 设置页允许 Mock 类型且有 mock endpoint，数据集规范页提示 mock provider，README/前端说明也强化 mock 主线。用户现在需要真实操作导向，而不是 mock 演示导向。

重要边界：
1. 不要删除单元测试里的 vi mock/fetch mock，不要破坏测试基础设施。
2. 可以保留后端内部测试/CI 的 mock 能力，但必须从“用户可见主线”和“默认操作路径”里移除或降级为隐藏/开发模式。
3. 不要改成不可运行：如果真实 Provider 未配置，页面要清楚提示“请配置真实 OCR/LLM Provider”，而不是自动回退 mock。
4. 不要问问题，不要等待确认，直接修。
5. 不要提交 git commit。

必须修复的问题：

[P0-1] 前端用户可见 mock 状态误导。
- `AppShell.tsx` 顶部不能再显示 “Mock Provider Ready”。
- 应显示真实 provider 状态，例如“Provider 待配置 / Provider 已连接 / 生产 Provider 状态”，根据接口返回或配置状态决定。
- 如果后端当前仍返回 mock provider，也必须在 UI 中标为“开发占位，不可用于真实识别”，不能用 Ready 误导。

[P0-2] 新建识别默认走 mock provider。
- `NewRecognitionPage.tsx` 不能默认选择 `mock-ocr` / `mock-model`。
- Provider 下拉应过滤/弱化 mock provider：默认只选真实 OCR/LLM provider；没有真实 provider 时显示阻断提示，并禁用创建/开始识别按钮，提示先到 Provider 设置配置真实 provider。
- 表单、提交 payload、测试都要同步更新。

[P0-3] Provider 设置页公开 Mock 模式导致用户不知道怎么操作。
- `ProviderSettingsPage.tsx` 的用户主路径应只展示真实 provider 类型：HTTP OCR / LangChain / OpenAI-compatible / OpenAI Responses / LIMS 等。
- Mock 类型不应作为普通用户可选主线；如确需保留，仅放到开发/测试说明里，不参与默认配置和健康检查主流程。
- 删除或改写 `http://lims.mock.local/api`、`mock_********`、`Mock provider 未执行健康检查` 等用户可见文案。
- 页面说明从“支持 Mock 与生产兼容模式切换”改为真实生产/沙箱 Provider 配置说明。

[P0-4] API 对前端暴露的 providers 默认 mock 化。
- 检查 `apps/api/src/demo-services.ts`、`bootstrap/production-services.ts`、`config/env.ts` 和 `/providers` 输出。
- 在 demo/本地环境如果只能提供 mock，也要加明确字段/标识（如 `isMock: true` 或 displayName 包含“开发占位”），前端不能把它当真实可用主线。
- 默认 provider 选择逻辑不能把 mock 当生产默认。若没有真实 provider，返回可理解的待配置状态。

[P1-1] Dataset/README/页面文案 mock 主线过重。
- 用户可见页面不要再强调 “CI 只运行 synthetic samples 和 mock provider” 作为操作提示。
- 文档可以保留测试说明，但要把真实操作路径写清楚：配置真实 OCR/LLM Provider → 上传 → 识别 → 复核 → 写回。

[P1-2] 补上防回归测试。
- 前端测试应覆盖：没有真实 provider 时不能创建识别任务；mock provider 不作为默认选项；顶部不显示 Mock Provider Ready。
- Provider 设置测试覆盖：Mock 不在普通可选类型里，真实 provider 类型存在。
- API/服务测试如需要，覆盖 provider 列表标识 mock/真实状态。

必须执行验证：
- `corepack pnpm --filter @medical-record-agent/demo-web test:styles`
- `corepack pnpm --filter @medical-record-agent/demo-web test:mobile`
- `corepack pnpm --filter @medical-record-agent/demo-web build`
- `corepack pnpm test` 或可行的相关测试集；如果全量太慢，至少跑被修改模块相关测试并在报告说明。
- `corepack pnpm smoke:demo-web` 可行则执行。
- 访问/检查 `http://127.0.0.1:9901/` 和 `/api/health`；如需要 rebuild/restart，按项目方式处理，但不要杀不相关进程。

必须产出：
1. `MEDICAL-REMOVE-MOCK-LINE-FIX-REPORT.md`
2. `MEDICAL-REMOVE-MOCK-LINE-AUDIT-REPORT.md`

审计报告必须 7 维度：
1. 产品概述
2. 功能完整性
3. 业务流程完整性
4. 用户体验
5. 技术实现
6. 问题清单 P0/P1/P2
7. 验收结论

验收结论必须明确：
- 是否已从用户可见主线去掉 mock 线路。
- 如果仍保留内部测试 mock，说明它只用于测试/开发，不再引导真实用户操作。
- 如果真实外部 OCR/LLM/LIMS 未配置，结论应写“真实外部集成待配置”，但 UI 不应再误导为 mock ready。
