【重要】不要问问题，不要请求确认，不要启动浏览器询问确认。直接执行。必须遵循 superpowers 流程：brainstorming → writing-plans → TDD/测试优先 → verification-before-completion。

任务：医疗项目 Medical Record Agent 的 Demo Web 界面问题全局巡检与修复闭环。

工作目录：/tmp/Medical-Record-Agent

用户反馈：截图显示医疗项目界面仍有较多 UI 问题，需要修复。不要只修截图中的一个红框，要把截图暴露的问题升级为同类问题全局巡检，覆盖所有页面、表格、卡片、表单、隐私选项、按钮区、移动端与真实运行环境。

参考截图路径（必须纳入问题分析，可用本地图片查看工具或脚本读取尺寸/截图，也可依据描述执行）：
1. /home/ubuntu/.hermes/image_cache/img_367bda702f9e.jpg —— 最近任务表格局部：任务/模板区域被红框标注，说明表格首列和模板列视觉层级、间距、对齐、可读性仍有问题。
2. /home/ubuntu/.hermes/image_cache/img_069b092954c5.jpg —— 看板上半屏：右侧按钮/卡片区域存在截断或布局拥挤问题。
3. /home/ubuntu/.hermes/image_cache/img_6b475fee7a46.jpg —— 看板宽屏：左右两侧大面积空白被红框标注，说明页面 max-width、容器利用率、响应式布局存在问题。
4. /home/ubuntu/.hermes/image_cache/img_321f976b9b75.jpg —— 隐私选项：checkbox 区域样式偏原生/单薄，和 Material + Arco 企业级风格不统一。

必须先读取/参考：
- CLAUDE.md
- PRODUCT-AUDIT-REPORT.md（若存在）
- MEDICAL-ARCO-UI-AUDIT-REPORT.md（若存在）
- MEDICAL-UI-REFLOW-FIX-REPORT.md（若存在）
- apps/demo-web/src/styles.css
- apps/demo-web/src/layouts/AppShell.tsx
- apps/demo-web/src/pages/recognition/RecognitionDashboardPage.tsx
- apps/demo-web/src/pages/recognition/NewRecognitionPage.tsx
- apps/demo-web/src/pages/operations/ProviderSettingsPage.tsx
- apps/demo-web/src/pages/operations/WritebackPage.tsx
- apps/demo-web/src/pages/schema/SchemaStudioPage.tsx
- apps/demo-web/src/pages/evaluation/EvaluationPage.tsx

设计基线：
- 保持现有企业级 Material + Arco Design 方向，不要回滚成旧红色主题，不要整体重写或删除 CSS。
- UI 关键词：白侧栏、pill 高亮、蓝色 #3370FF、灰底 #F7F8FA、DM Sans + Noto Sans SC、阴影卡片、圆角、克制高级、不廉价模板感。
- 医疗系统要像真实工作台：信息密度合理、表格清晰、状态有层级、隐私/合规选项可信赖。

截图暴露的必须修复方向：
1. 宽屏容器利用率：当前看板左右空白过大。修复 page shell / content max-width / dashboard grid，使 1440px+ 宽屏下主体更充分利用宽度，但不要贴边；建议采用 clamp 或更合理 max-width（如 1520/1600）和自适应 grid。
2. 最近任务表格：任务名、记录号、模板列、Provider、状态、置信度、写回、负责人/负载等列要对齐清楚；首列内容层级更强；不要出现文字拥挤、截断不合理、红框所示区域像没设计过。
3. 顶部按钮区：新建识别/查看流程/隐私策略/刷新等按钮在不同宽度下不能被截断；小屏应换行或收纳，触摸区域 ≥44px。
4. 指标卡片：运行状态和指标卡片在宽屏/中屏/移动端都要保持均衡，不要某个卡片半截露出；grid minmax 合理。
5. 隐私选项：checkbox 列表要做成企业级设置项，不要像原生复选框堆叠；需要明确选中/未选中/说明/安全感，和 Arco 风格统一。
6. 全局一致性：按钮、pill、表单、表格、空状态、错误状态、抽屉/面板、代码/证据链展示等不要只修首页；巡检所有 demo-web 页面。
7. 移动端：必须验证移动端截图，侧栏/按钮/表格横滚/卡片堆叠/checkbox 设置项都不能溢出。

执行要求：
1. 先做 UI 巡检，记录到 docs/superpowers/plans/2026-06-09-ui-global-polish.md，包含 brainstorming、writing-plans、TDD/测试计划、verification。
2. 先补/调整可执行的样式守卫或组件测试，再实现。已有 ui-arco-style-guards / mobile / styles 测试要同步更新。
3. 具体修改应优先基于现有组件和 class，使用增量 patch，不要整体重写 apps/demo-web/src/styles.css。
4. 不要 git commit。
5. 不要修改 .env、node_modules、无关缓存。
6. 不要删除已有报告；新报告另存。

必须运行并在报告中记录结果：
- corepack pnpm typecheck
- corepack pnpm test
- corepack pnpm --filter @medical-record-agent/demo-web test:styles
- corepack pnpm --filter @medical-record-agent/demo-web test:mobile
- corepack pnpm --filter @medical-record-agent/demo-web build
- corepack pnpm smoke:demo-web
- 如可行，运行 corepack pnpm e2e:demo-web:browser 并生成/更新 UI 截图；如果因环境限制失败，报告中说明原因并至少用 Playwright/浏览器脚本或现有 e2e 脚本覆盖关键页面。

真实运行验证：
- 确认 demo-web dist/index.html 引用新 bundle。
- 检查 9901 或当前预览端口页面可访问；不要只说 build 通过。
- 截图覆盖至少：桌面首页/识别看板/新建识别/Provider 设置/写回页面，以及移动端首页/识别看板/隐私选项相关页面。

报告要求：
1. 生成 MEDICAL-UI-GLOBAL-POLISH-FIX-REPORT.md，写清楚修复点、涉及文件、测试结果、截图路径、运行验证、剩余风险。
2. 生成 MEDICAL-UI-GLOBAL-POLISH-AUDIT-REPORT.md，必须包含 7 个维度：
   - 产品概述
   - 功能完整性
   - 业务流程完整性
   - 用户体验
   - 技术实现
   - 问题清单（P0/P1/P2）
   - 验收结论
3. 验收结论必须分层：UI 当前阶段是否通过、医疗项目整体是否仍有外部集成 blocked。不要把 UI 通过写成医疗项目最终完成。

验收标准：
- 截图指出的宽屏空白、表格首列/模板列弱设计、按钮截断、隐私 checkbox 单薄感必须明确改善。
- 全页面同类问题有巡检记录，不允许只改 RecognitionDashboardPage。
- 所有必跑测试/构建必须通过；如真实外部集成 blocked，要区分 blocked 与 UI 修复失败。
- 报告必须完整、具体，不接受简单罗列。
