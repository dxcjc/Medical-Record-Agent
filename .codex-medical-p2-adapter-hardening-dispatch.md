【重要】不要问问题，不要请求确认，不要启动浏览器询问确认。直接执行。必须遵循 superpowers 流程：brainstorming → writing-plans → TDD/测试优先 → verification-before-completion。

任务：继续推进医疗项目 P1/P2 业务/安全/集成生产化闭环，但不要把 UI 阶段通过误判为最终完成。

工作目录：/tmp/Medical-Record-Agent

必须先读取：
1. .codex-medical-p1-p2-continuation.md
2. PRODUCT-AUDIT-REPORT.md
3. MEDICAL-P2-INTEGRATION-HARDENING-AUDIT-REPORT.md
4. docs/2026-06-09-p2-production-handoff.md
5. 相关已有报告：STYLE-FIX-REPORT.md、MEDICAL-ARCO-UI-AUDIT-REPORT.md、P1-FIX-REPORT.md、P1-AUDIT-REPORT.md、MEDICAL-P2-PRODUCTION-CLOSURE-*.md

背景与当前状态：
- UI 当前阶段已通过，必须保持企业级 Material + Arco Design：Primary #3370FF、背景 #F7F8FA、白色侧栏、active nav pill、DM Sans + Noto Sans SC、移动端抽屉/单列/44px 触摸区。
- 当前验证已通过：demo-web test:styles、test:mobile、build、全量 corepack pnpm test；9901 首页和 /api/health 可访问，dist bundle 已更新。
- 但最终产品仍不通过：真实 OCR/LLM/LIMS sandbox、真实 KMS/Vault/Secret Manager、多实例持久化可靠队列仍 blocked。

本轮目标：在不依赖真实外部凭据的前提下，继续把 blocked 项向可交接/可落地推进；优先做“可工程化”的真实适配边界与测试，不要伪造外部通过。

优先修复/增强方向（请自己基于代码现状取舍并完整执行）：
1. 可靠队列：把当前 JobQueueAdapter/queue contract 从纯声明推进到更可落地的 broker adapter 边界。优先实现 Redis/RabbitMQ/SQS 之一的 adapter skeleton/工厂/配置校验/单元测试；如果不能引入外部服务，使用 mockable client 或 contract tests 验证 lease、retry、dead-letter、heartbeat、幂等消费语义。真实 broker smoke 缺环境时必须输出 blocked，不能写 passed。
2. 密钥库：把 SecretResolver 从 contract 推进到更清晰的可插拔实现边界。优先补 Vault/KMS/Secret Manager resolver 的接口、工厂、配置校验、脱敏错误、测试；如果没有 SDK/凭据，不要伪造真实读取，只能明确 SDK/environment blocked。
3. production smoke：确保 real-sandbox、mock-production、blocked 三类状态清晰，缺外部条件时报告具体缺项；mock-production 只能作为 contract smoke，不得写成真实外部集成通过。
4. 交接文档：更新 docs/2026-06-09-p2-production-handoff.md，把下一步部署方需要提供的 broker/KMS/sandbox 条件、验收命令、blocked 判断写清楚。

约束：
- 不要提交 git commit。
- 不要修改 .env、node_modules、无关缓存。
- 不要粗暴重写 CSS；如非必要不要改 UI 样式。
- 不要把真实外部集成、真实密钥库、多实例队列写成通过，除非你真的能在本环境验证。
- 不要只写计划，必须实际改代码/测试/文档/报告。

修复后必须验证并在报告写明真实结果：
- corepack pnpm typecheck
- corepack pnpm test
- corepack pnpm --filter @medical-record-agent/demo-web test:styles
- corepack pnpm --filter @medical-record-agent/demo-web test:mobile
- corepack pnpm --filter @medical-record-agent/demo-web build
- corepack pnpm smoke:demo-web
- corepack pnpm smoke:production（若 blocked，必须写明 blocked 原因）
- 如有可用 mock-production：PRODUCTION_SMOKE_MODE=mock-production PRODUCTION_SMOKE_RUN_WRITEBACK=1 corepack pnpm smoke:production

必须生成/更新报告：
1. MEDICAL-P2-ADAPTER-HARDENING-FIX-REPORT.md：说明本轮修复点、文件、测试、仍 blocked 项。
2. MEDICAL-P2-ADAPTER-HARDENING-AUDIT-REPORT.md：必须包含 7 个维度：产品概述、功能完整性、业务流程完整性、用户体验、技术实现、P0/P1/P2 问题清单、验收结论。

验收结论必须分层：
- UI 当前阶段：通过/不通过。
- P1/P2 本轮工程化阶段：通过/不通过。
- 真实外部集成：通过/blocked/不通过。
- 医疗项目最终产品：通过/不通过。
