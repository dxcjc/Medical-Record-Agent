# Step 4: 系统缺陷扫描 + 修复

项目路径: /tmp/Medical-Record-Agent

## 背景
系统从 demo 升级到产品级后，需要全面扫描潜在缺陷。重点检查 5 个维度。

## 扫描维度

### 1. 安全性
- JWT token 是否有合理的过期时间
- API 输入校验是否完整（防注入、XSS）
- 文件上传是否有类型/大小限制
- 敏感信息（密码、token）是否在日志中脱敏
- CORS 配置是否合理
- Rate limiting 是否存在

### 2. 错误处理
- 所有 async 路由是否有 try/catch
- 数据库操作失败是否有友好错误消息
- 外部服务（OCR、LLM）超时/失败是否有兜底
- 文件上传失败是否有清理逻辑
- Prisma P2002/P2003 等已知错误是否统一处理

### 3. 输入校验
- 所有 API 端点是否有 Zod schema 校验
- 分页参数是否有 min/max 限制
- 文件路径是否有路径遍历防护
- JSON 字段是否有深度限制

### 4. 性能
- 数据库查询是否有 N+1 问题
- 大列表是否有分页
- 文件读取是否有流式处理
- 前端是否有不必要的重渲染

### 5. 边界条件
- 空数据集的处理（空列表、空统计）
- 超长输入的处理（超长文件名、超大 JSON）
- 并发请求的处理（同时创建多个任务）
- 时区问题（日期格式化）

## 执行方式
1. 先用 `grep` 和 `read_file` 扫描代码，列出所有问题
2. 按严重程度排序（P0 安全 > P1 错误处理 > P2 输入校验 > P3 性能 > P4 边界）
3. 逐一修复
4. 每个修复后运行测试确认无回归
5. 生成 STEP4-AUDIT.md 审计报告

## 验证标准
- `pnpm typecheck` 通过
- `cd medical-ui && pnpm build` 通过
- 后端测试全部通过（无新增失败）
- 所有 P0/P1 问题已修复
- P2/P3/P4 问题已修复或记录为已知问题
- 生成 STEP4-AUDIT.md
