# Codex 任务：医疗项目 UI 全局对齐箭牌项目 Material + Arco 企业级设计系统

【重要】不要问问题，不要请求确认，不要只写计划。直接执行。必须遵循 superpowers 流程：先分析/计划，再实施，再验证，再报告。

## 项目与参考
当前项目：/tmp/Medical-Record-Agent
当前分支：master
当前部署：nginx 9901，root 指向 `/tmp/Medical-Record-Agent/apps/demo-web/dist`，API 代理 `/api/` 到 3000。

参考项目：/tmp/arrow-dealer-system
重点参考文件：
- /tmp/arrow-dealer-system/src/styles.css
- /tmp/arrow-dealer-system/src/components/PageHeader.tsx
- /tmp/arrow-dealer-system/src/pages/EntityPage.tsx
- /tmp/arrow-dealer-system/src/icons/appIcons.tsx

设计规范：Material Design + Arco Design B2B Dashboard
- Primary: #3370FF
- Background: #F7F8FA
- Card: #FFFFFF
- Text primary: #1D2129
- Text body: #4E5969
- Text secondary: #86909C
- Border: #E5E6EB
- Sidebar 白底，不要暗黑/花哨
- Active nav pill: #E8F3FF 背景 + #3370FF 文字
- Card radius 12px，Material shadow: 0 1px 2px rgba(0,0,0,0.04)，hover 0 4px 12px rgba(0,0,0,0.08)
- Button/Input radius 8px
- 页面 padding 24px，卡片 gap 16px
- 字体：DM Sans + Noto Sans SC

## 用户反馈
用户明确指出：
“这个 UI 设计和之前定的不符合，还没有使用 arco design，参考箭牌项目做优化。”

所以本次不是局部调 CSS，而是全局 UI 体系修复：必须覆盖 AppShell、登录页、识别、Schema、评估、运营、设置等所有可见页面。

## 必须完成 P0
1. 引入/实际使用 Arco Design
- 当前 apps/demo-web 虽然依赖 @arco-design/web-react，但页面直接使用自写 div/button/input/card 风格，必须改为真实 Arco 组件或至少关键交互组件使用 Arco：Button、Card、Input、Select、Table/List、Tag、Tabs、Steps、Drawer/Modal、Alert、Form、Upload 等。
- 在 `apps/demo-web/src/main.tsx` 引入 Arco CSS：`@arco-design/web-react/dist/css/arco.css`（如已存在则确认）。
- 配置/覆盖 Arco 主题色到 #3370FF（CSS 变量或 ConfigProvider/theme 均可）。

2. 全局视觉对齐箭牌项目
- AppShell：白色侧栏、pill active、高级 topbar、灰色主背景、内容卡片。
- LoginPage：不能是廉价 demo 登录页；要有企业级登录工作台视觉，参考箭牌登录页但医疗语义化。
- Dashboard/Recognition 页面：KPI 卡、流程卡、任务列表、状态 Tag 使用 Arco/Material 风格。
- Schema/Evaluation/Operations 页面：表格/列表/配置面板/日志/反馈卡统一用 Card/Table/Tag/Tabs 等 Arco 风格，不要自造廉价模板。
- 图标尺寸统一 14-20px，不要大图标抢视觉。
- 所有页面中文文案、专业医疗 AI 系统语境。

3. 移动端适配
- <768px 侧栏变抽屉/收起，主内容单列。
- 卡片/表格可横向滚动或转卡片。
- 触摸区域 >= 44px。

4. 字体
- index.html 加 Google Fonts：DM Sans + Noto Sans SC。
- CSS font-family 使用 `'DM Sans', 'Noto Sans SC', system-ui, sans-serif`。

5. 测试守护
新增/更新测试，至少检查：
- main.tsx 引入 Arco CSS 或 App 使用 Arco Provider。
- AppShell 使用 Arco Button/Input/Badge/Avatar 或等效关键组件。
- 多个页面文件导入并使用 @arco-design/web-react。
- styles.css 包含 #3370FF、#F7F8FA、app-shell/app-sidebar/nav-pill/card shadow 等设计 token。
- Login/主要页面不再只有自写裸 button/input/card。

## 必须验证
运行：
- corepack pnpm --filter @medical-record-agent/demo-web test:styles（如脚本存在）
- corepack pnpm --filter @medical-record-agent/demo-web test:mobile（如脚本存在）
- corepack pnpm --filter @medical-record-agent/demo-web build
- 如全量 test 有 master 现存 Windows 路径失败，报告中说明，不要把 UI 验收卡死在该非 UI 用例。

部署验证：
- build 后 nginx 9901 必须能看到新 dist。
- 验证 http://127.0.0.1:9901/ 200。
- 验证 /api/health 200。
- 浏览器打开 http://localhost:9901/，至少检查登录页和进入工作台后的 3 个核心页面，不能有明显旧 UI、控制台错误、加载失败。

## 报告
生成：MEDICAL-ARCO-UI-AUDIT-REPORT.md
必须 7 维度中文报告：
1. 产品概述
2. 功能完整性
3. 业务流程完整性
4. 用户体验
5. 技术实现
6. P0/P1/P2 问题清单
7. 验收结论

报告必须明确：
- 哪些页面已使用 Arco Design。
- 如何对齐箭牌项目 Material + Arco 规范。
- 移动端处理。
- 验证命令结果。
- 浏览器/部署验证结果。
- 若还有未完成项，必须列为 P1/P2，不得写“全部完成”。

## 约束
- 不要提交 git commit。
- 不要修改 .env、node_modules、dist 以外无关产物。
- 不要破坏 API 代理和 9901 部署。
- 不要只改一个页面；必须全局覆盖。
