【重要】不要问问题，不要请求确认，不要启动浏览器询问确认。直接执行。必须遵循 superpowers 流程：brainstorming → writing-plans → TDD/测试优先 → verification-before-completion。

任务：继续推进医疗项目 /tmp/Medical-Record-Agent 的 P2/生产化遗留闭环，修复最新审计报告 MEDICAL-P2-E2E-UI-PERF-AUDIT-REPORT.md 中仍未通过/remaining 的可工程化问题，并重新生成 7 维度审计报告。

背景：
- 最新报告 MEDICAL-P2-E2E-UI-PERF-AUDIT-REPORT.md 结论：UI 当前阶段通过，本轮 P2 E2E/UI/交接阶段通过，但医疗项目最终产品不通过。
- 主要 remaining：
  1. demo-web build 仍有 vendor-arco JS 517.22 kB 超过 500 kB warning；必须安全优化，不允许提高 chunkSizeWarningLimit 掩盖，不允许恢复已知有问题的 Arco 子 chunk circular warning。
  2. 真实外部 OCR/LLM/LIMS sandbox、真实 KMS/Vault/Secret Manager、多实例持久化可靠队列仍未闭环。若无外部凭据，必须把代码可交付的契约/接口/配置/测试补齐，并在报告中明确 external credential blocked，不可假装通过。
  3. 医疗 UI 值得肯定但仍需继续细节优化：间距、遮挡、换行、移动端横滚/触摸区要保持并补 regression guard。

必须修复/推进：
1. 优先处理 vendor-arco >500k：通过安全的路由级 dynamic import、组件懒加载、manualChunks 或按需入口策略，让 build 不再出现 >500k chunk warning；不得简单调高 warning limit；不得引入 circular dependency warning；不得破坏 9901 部署和 API 代理。
2. 对生产化 remaining 做可工程化补强：
   - 若已有队列/secret/smoke 契约，补测试和文档，让边界清晰；
   - 若能实现最小 broker/secret resolver 接口或配置 fail-fast，就实现并加测试；
   - 若必须依赖真实外部 sandbox 凭据，报告明确标为 blocked，不写通过。
3. 保持现有 Material + Arco Design UI：Primary #3370FF、背景 #F7F8FA、白色侧栏、active nav pill、DM Sans + Noto Sans SC、移动端抽屉/单列/44px 触摸区。
4. 更新/新增测试，至少覆盖 chunk 策略不靠 warningLimit、无 circular warning 风险、生产 smoke/secret/queue 边界、UI reflow guard。

修复后必须运行并记录结果：
- corepack pnpm typecheck
- corepack pnpm test
- corepack pnpm --filter @medical-record-agent/demo-web test:styles
- corepack pnpm --filter @medical-record-agent/demo-web test:mobile
- corepack pnpm --filter @medical-record-agent/demo-web build
- corepack pnpm smoke:demo-web
- 如浏览器可用：corepack pnpm e2e:demo-web:browser；不可用则记录 browserE2E=blocked，不可冒充 passed。

必须生成/更新报告：
1. MEDICAL-P2-PRODUCTION-CLOSURE-FIX-REPORT.md：说明修复点、文件、测试命令、仍 blocked 的外部条件。
2. MEDICAL-P2-PRODUCTION-CLOSURE-AUDIT-REPORT.md：必须包含 7 个维度：
   - 产品概述
   - 功能完整性
   - 业务流程完整性
   - 用户体验
   - 技术实现
   - P0/P1/P2 问题清单
   - 验收结论（阶段通过/不通过 + 医疗最终产品是否通过必须分层说明）

约束：
- 不要提交 git commit。
- 不要修改 .env、node_modules、无关缓存。
- 不要删除既有报告和截图。
- 不要只写计划，必须实际修复、验证、生成报告。
- 若遇到 429/上游断流，保留已完成改动和报告草稿，明确记录中断点，不要反复重试。
