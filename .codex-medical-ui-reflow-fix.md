# Codex 任务：医疗 demo-web UI 全面返工，不要碎片修

【必须遵循 superpowers 流程】
读取 CLAUDE.md；按 brainstorming → writing-plans → TDD/验证优先 → verification-before-completion 执行。

【重要】不要问问题，不要请求确认，不要只修用户举的两个例子，直接全系统巡检并返工。不要提交 git commit。

## 背景
用户刚刚验收医疗项目 demo-web，明确判定：当前 UI 完成度远低于箭牌中台项目，不能接受。用户指出：
1. 顶部导航条高度和侧边栏 logo 区域高度不一致。
2. 一些模块间距乱，整体不顺滑。
3. 上传文件/PDF 的上传区域和下面表单紧紧贴在一起。
4. 顶部导航在平板宽度会文字换行。
5. 内容宽度不足时，顶部一些次要文字/信息应隐藏，而不是挤压换行。
6. 整体完成度明显不如 `/tmp/arrow-dealer-system` 中台项目。

## 参考标准
参考箭牌中台项目：
- `/tmp/arrow-dealer-system/src/styles.css`
- 设计模式：Material + Arco，白侧栏、pill 高亮、蓝色 #3370FF、灰背景 #F7F8FA、DM Sans + Noto Sans SC、阴影卡片、统一 header 高度。

必须重点对比医疗项目：
- `/tmp/Medical-Record-Agent/apps/demo-web/src/styles.css`
- `/tmp/Medical-Record-Agent/apps/demo-web/src/layouts/AppShell.tsx`
- 识别页、上传页、Schema、Evaluation、Operations 全部页面和共享组件。

## 任务目标
把医疗 demo-web 的 UI 质量拉到接近箭牌中台项目。不是局部 patch，而是全局巡检 + 全局修复。

## 必修问题
### P0-1 顶部栏与侧栏 logo 高度统一
- 统一 sidebar logo/header 区域与 topbar 高度，例如都使用 64px。
- sidebar brand/logo 区域、topbar、main content 起始位置必须视觉对齐。
- 不允许 logo 区域和 topbar 高度错位。

### P0-2 顶部导航平板防换行
- topbar 内标题、面包屑、状态文字、辅助说明、用户信息在 768~1180 宽度不能挤压换行。
- 宽度不足时隐藏次要信息，例如副标题、说明、冗余状态文本；保留主标题和核心操作。
- 使用 `white-space: nowrap; overflow: hidden; text-overflow: ellipsis; min-width: 0;`。
- 用媒体查询处理 tablet 宽度。

### P0-3 上传区和表单间距
- 上传文件/PDF 区域与下方表单必须有明确间距，不能贴在一起。
- 上传卡片、步骤指引、表单 section 之间统一使用 20~24px gap。
- recognition/new 页面重点检查。

### P0-4 全局页面节奏和间距
- 统一 page container padding、section gap、card padding、form grid gap。
- 页面内容不要一坨，不要乱七八糟。
- 卡片之间、表格和筛选之间、提示区和操作区之间必须有清楚层级。

### P1-1 按箭牌中台补齐 Material+Arco 细节
- 卡片 shadow-1/hover shadow-2。
- 侧栏 active pill 高亮：#3370FF，背景 rgba(51,112,255,0.1)，左侧指示条，font-weight 700。
- 表格表头 #FAFBFC，行 hover #F7F8FA。
- 按钮、卡片 hover transition。
- 字体 DM Sans + Noto Sans SC。

### P1-2 逐页巡检
至少覆盖这些页面：
- LoginPage
- RecognitionDashboardPage
- NewRecognitionPage
- JobDetailPage
- SchemaStudioPage
- EvaluationPage
- ProviderSettingsPage
- WritebackPage
- AuditLogPage
- AgentTracePage
- FeedbackSamplesPage
- DatasetSpecPage
- NotFoundPage

## 验证要求
必须完成并在报告中列出：
1. `pnpm --filter @medical-record-agent/demo-web build` 通过。
2. 检查 styles.css 和 AppShell.tsx 确认上述 class/媒体查询真实存在。
3. 生成 `MEDICAL-UI-REFLOW-FIX-REPORT.md`，必须中文，包含：
   - 修复范围
   - 用户指出的问题逐条对应修复
   - 逐页巡检结果
   - 平板/窄屏处理说明
   - 构建结果
   - 7 维产品审计：产品概述、功能完整性、业务流程、用户体验、技术实现、问题清单、验收结论
4. 如果发现仍有缺陷，在报告中列 P0/P1/P2，不要粉饰。

## 严禁
- 严禁只改用户点名的两处。
- 严禁只写报告不改代码。
- 严禁只 build 通过就说完成。
- 严禁删除 Arco/Material 设计系统。
- 严禁把布局改成廉价模板风。
