【重要】不要问问题，不要请求确认，不要启动浏览器询问确认。直接执行。必须遵循 superpowers 流程：brainstorming → writing-plans → TDD/测试优先 → verification-before-completion。

任务：继续推进 /tmp/Medical-Record-Agent 医疗项目后续业务/安全/集成优化闭环。

背景：
- 最新审计报告 /tmp/Medical-Record-Agent/MEDICAL-P1-CONTINUATION-AUDIT-REPORT.md 为 7 维度合格报告，但结论明确：医疗项目最终产品验收不通过。
- 当前已通过本地验证：corepack pnpm typecheck、corepack pnpm test、corepack pnpm --filter @medical-record-agent/demo-web build。
- 医疗 UI 比之前明显改善，但仍不能宣布最终完成；后续仍要继续优化业务/安全/集成。
- 不要提交 git commit。

必须修复/推进的 P1 项：
[P1-1] 生产异步任务队列未实现。
- 要求：在现有架构内实现最小可验收的 job queue/worker 闭环：POST /jobs 入队/返回明确状态，worker 或可测试的后台执行器异步执行 OCR/LLM/core orchestrator，任务状态可轮询，失败有状态/错误记录，保留同步模式兼容或明确切换语义。
- 要求：补充测试覆盖入队、执行、失败、状态查询，不能只写接口空壳。

[P1-2] production smoke 真实环境仍 blocked。
- 说明：如果缺少真实外部环境变量，不要伪造通过；需要改进 smoke 文档/脚本/报告，使 blocked、mock production、real sandbox 三种状态清楚区分。
- 要求：补充可在本地 mock production 环境跑通的 smoke 或 contract smoke，同时保留真实 sandbox 的 blocked 语义。

[P1-3] Provider secretRefs 尚未接入真实密钥库。
- 要求：至少实现可插拔 secret resolver 接口和测试，在线保存 provider 的 secretRefs 不能被忽略；若无真实 KMS，则提供 env/mock resolver 并在报告列明真实 KMS 仍为 P2/P1 外部部署项。

验证要求：
1. corepack pnpm typecheck 必须通过。
2. corepack pnpm test 必须通过。
3. corepack pnpm --filter @medical-record-agent/demo-web build 必须通过。
4. 如新增 smoke 命令，必须执行并记录结果；真实外部环境缺失要明确 BLOCKED，不要写通过。
5. 用代码搜索确认异步队列、secret resolver、smoke 状态语义真实落地。

报告要求：
1. 生成 /tmp/Medical-Record-Agent/MEDICAL-P1-P2-NEXT-FIX-REPORT.md，说明修复点、文件、验证命令、剩余问题。
2. 生成或更新 /tmp/Medical-Record-Agent/MEDICAL-P1-P2-NEXT-AUDIT-REPORT.md，必须包含 7 个维度：产品概述、功能完整性、业务流程完整性、用户体验、技术实现、P0/P1/P2 问题清单、验收结论。
3. 验收结论必须区分“本轮 P1 阶段是否通过”和“医疗项目最终产品是否通过”。

不要问问题，直接修复、验证、生成报告。