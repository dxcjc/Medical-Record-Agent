【重要】不要问问题，不要请求确认，不要启动浏览器询问确认。直接执行。必须遵循 superpowers 流程：brainstorming → writing-plans → TDD/测试优先 → verification-before-completion。

任务：继续推进医疗项目 P1/P2 闭环，修复当前 cron 新鲜验证发现的 demo-web build 阻断，并复核 P1/P2 报告状态。

工作目录：/tmp/Medical-Record-Agent

背景与必须读取：
1. `.codex-medical-p1-p2-continuation.md`
2. `PRODUCT-AUDIT-REPORT.md`
3. `MEDICAL-ARCO-UI-AUDIT-REPORT.md`
4. `STYLE-FIX-REPORT.md`
5. `MEDICAL-UI-REFLOW-FIX-REPORT.md`
6. `P1-AUDIT-REPORT.md`
7. `P1-FIX-REPORT.md`
8. 最新 P1/P2 报告：`MEDICAL-P1-P2-NEXT-AUDIT-REPORT.md`、`MEDICAL-P2-SESSION-QUEUE-HARDENING-AUDIT-REPORT.md`、`MEDICAL-P1-P2-CONTRACT-READINESS-FIX-REPORT.md`

当前 cron 新鲜验证结果：
- 没有医疗相关 Codex 正在运行。
- `corepack pnpm --filter @medical-record-agent/demo-web test:styles` 通过：19 passed。
- `corepack pnpm --filter @medical-record-agent/demo-web test:mobile` 通过：5 passed、14 skipped。
- `corepack pnpm --filter @medical-record-agent/demo-web build` 失败：
  `apps/demo-web/src/pages/operations/ProviderSettingsPage.tsx(136,13): error TS2375: Type '{ key: string; displayName: string | undefined; name: string; enabled: boolean; isDefault: boolean; isMock: boolean; status: string | undefined; config: ApiJsonObject | undefined; secretRefs: ApiJsonObject | undefined; }' is not assignable to type 'ApiProviderItem' with exactOptionalPropertyTypes: true. displayName: string | undefined is not assignable to string.`
- 9901 当前首页和 `/api/health` 可访问，HTML 引用 `/assets/index-CrTQt4LO.js`，但 build 失败意味着不能把当前阶段判为通过。

必须完成：
1. 按 TDD/测试优先定位并修复 `ProviderSettingsPage.tsx` 的 exactOptionalPropertyTypes build 阻断。根因是构造 `ApiProviderItem` 时把 `undefined` 显式写入可选 string 字段；修复时应只在值存在时添加可选字段，或使用已有 normalizer/类型安全 helper。不要放宽 TS 配置，不要把类型改成随意 `any`。
2. 搜索同类模式，检查 demo-web/API normalizer 中是否还有可选字段显式 `undefined` 赋给 exact optional 类型的隐患，必要时一并修复并补测试。
3. 保持企业级 Material + Arco Design UI：Primary #3370FF、背景 #F7F8FA、白色侧栏、active nav pill、DM Sans + Noto Sans SC、移动端抽屉/单列/44px 触摸区。不要粗暴重写 `styles.css`。
4. 重新运行并记录：
   - `corepack pnpm --filter @medical-record-agent/demo-web test:styles`
   - `corepack pnpm --filter @medical-record-agent/demo-web test:mobile`
   - `corepack pnpm --filter @medical-record-agent/demo-web build`
   - 如修复触及共享/API 类型，运行 `corepack pnpm test` 或至少相关测试；若失败，必须具体说明失败点，不能笼统写通过。
5. 检查 9901：确认 `http://localhost:9901/` 和 `/api/health` 可访问，并确认 dist/index.html 或 9901 HTML 引用真实 bundle。若需要重启/刷新预览，安全处理，不要破坏 API 代理。
6. 生成/更新报告：
   - `MEDICAL-P1-P2-CRON-BUILD-FIX-REPORT.md`：说明修复点、文件、验证命令、9901 状态、剩余 blocked 项。
   - `MEDICAL-P1-P2-CRON-BUILD-AUDIT-REPORT.md`：必须包含 7 个维度：产品概述、功能完整性、业务流程完整性、用户体验、技术实现、P0/P1/P2 问题清单、验收结论。

验收口径：
- 如果 build/test 恢复，通过只能说“UI/本轮 build 阶段恢复通过”或“P1/P2 当前可本地闭环项通过”。
- 不能说医疗项目最终完成。真实 OCR/LLM/LIMS sandbox、真实 KMS/Vault/Secret Manager、真实 broker 多实例可靠队列、生产多实例 session invalidation store 等仍是最终产品 blocked 项，除非你真实实现并完成 smoke。

约束：
- 不要提交 git commit。
- 不要修改 `.env`、`node_modules`、无关缓存。
- 不要并发启动其他 agent。
- 不要只写计划，必须实际修复、验证、生成报告。
- 若遇到 429/上游断流，保留已完成改动和报告草稿，明确记录中断点，不要反复重试。
