【重要】不要问问题，不要请求确认，不要启动浏览器询问确认。直接执行。必须遵循 superpowers 流程：brainstorming → writing-plans → TDD/测试优先 → verification-before-completion。

任务：继续推进医疗项目 Medical Record Agent 的 P1/P2 业务/安全/集成生产化闭环，不要停留在 UI 阶段。保持现有企业级 Material + Arco Design UI，不要粗暴重写 CSS，不要破坏 9901 部署、Vite 分包和 API 代理。

工作目录：/tmp/Medical-Record-Agent

必须先读取：
1. .codex-medical-p1-p2-continuation.md
2. PRODUCT-AUDIT-REPORT.md
3. MEDICAL-P2-PRODUCTION-CLOSURE-AUDIT-REPORT.md
4. MEDICAL-P2-SESSION-QUEUE-HARDENING-AUDIT-REPORT.md
5. docs/2026-06-09-p2-production-handoff.md

当前调度判断：
- UI 当前阶段已明显改善并通过本地样式/移动/build/9901验证，但这不等于医疗项目最终完成。
- 已闭环：P1/P2 中的大部分本地工程化项，包括 typecheck/build、demo-web style/mobile/build、chunk warning、mock demo smoke、session invalidation contract、queue readiness contract、部分 production smoke blocked 文案。
- 仍不能宣称最终完成：真实 OCR/LLM/LIMS sandbox、真实 KMS/Vault/Secret Manager、真实 broker 多实例可靠队列、多实例 session shared store 尚未在真实环境闭环。

本轮目标：推进“剩余 blocked 条件的可执行生产交接/契约化闭环”，优先做不依赖真实外部凭据、但能让上线方按 gate 执行的工作。不要伪造真实外部通过。

优先级：
1. KMS/Vault/Secret Manager contract/readiness：检查现有 provider secretRefs、env resolver、health check、文档和测试。如果当前只支持 env resolver，补齐清晰的 SecretResolver 接口/状态诊断/配置 gate/测试，确保 secret 不回显、不落日志、不被前端看到；真实 KMS 未接入时必须明确 blocked。
2. 真实 broker 与多实例 session handoff gate：在不引入真实 broker 的前提下，完善 readiness/deployment gate 的诊断输出、状态字段、测试与文档，使 Redis/RabbitMQ/SQS 或数据库 shared session store 的验收步骤可执行、不可误判。
3. API 契约与 production smoke：检查 route DTO/Zod/shared DTO 当前状态，继续减少 unknown 契约漂移风险；补充 smoke/readiness 对 /status、/health、auth、providers、writeback safety、evaluation schema 的可执行检查或 blocked 判定。
4. 浏览器 E2E/9901：如改动影响前端，必须保持 demo-web build、9901 首页与 /api/health 可访问，dist/index.html 与 9901 HTML bundle 一致。不要做大 UI 重构，只做必要 guard。

必须执行验证：
- corepack pnpm typecheck
- corepack pnpm test
- corepack pnpm --filter @medical-record-agent/demo-web test:styles
- corepack pnpm --filter @medical-record-agent/demo-web test:mobile
- corepack pnpm --filter @medical-record-agent/demo-web build
- corepack pnpm smoke:demo-web
- corepack pnpm readiness:deployment（允许真实外部集成 blocked 返回 exit code 2，但本地 gate 不应失败）
- 检查 9901 首页和 /api/health 可访问，并确认 dist/index.html 与 9901 返回 HTML 引用同一组真实 bundle。

报告要求：
1. 生成 MEDICAL-P2-BLOCKED-READINESS-FIX-REPORT.md：说明本轮修复点、文件、测试命令结果、9901/dist 检查、仍 blocked 的真实外部条件。
2. 生成 MEDICAL-P2-BLOCKED-READINESS-AUDIT-REPORT.md：必须包含 7 个维度：产品概述、功能完整性、业务流程完整性、用户体验、技术实现、P0/P1/P2 问题清单、验收结论。
3. 验收结论必须分层：UI 当前阶段、P1/P2 本轮阶段、真实外部集成、医疗最终产品。不要把阶段通过写成最终完成。

约束：
- 不要提交 git commit。
- 不要修改 .env、node_modules、无关缓存。
- 不要重写整份 CSS；任何样式调整必须保留 Material + Arco Design UI。
- 不要删除测试 mock；目标是去除用户可见 mock 主线和生产误判。
- 若遇到 429/上游断流，保留已完成改动和报告草稿，明确记录中断点，不要反复重试。
