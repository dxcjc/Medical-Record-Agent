【必须遵循 superpowers 流程】
读取 CLAUDE.md、.codex-medical-p1-p2-continuation.md、PRODUCT-AUDIT-REPORT.md、MEDICAL-P1-P2-CONTINUATION-ROLLUP-AUDIT-REPORT.md、MEDICAL-P2-NEXT-LOCAL-CLOSURE-AUDIT-REPORT.md、MEDICAL-P2-ASYNC-HANDOFF-AUDIT-REPORT.md、MEDICAL-P2-EXTERNAL-BLOCKER-READINESS-AUDIT-REPORT.md 了解当前状态。你必须按 brainstorming → writing-plans → TDD/测试优先 → verification-before-completion 执行，并把过程写入报告。

【重要】不要问问题，不要启动浏览器询问确认，不要等待人工确认。直接开始工作。不要提交 git commit。不要修改 .env、node_modules、缓存目录。不要粗暴重写 CSS，必须保持企业级 Material + Arco Design UI。

你在 /tmp/Medical-Record-Agent 工作。你是 Codex 开发/审计 worker，Hermes 是调度员。

任务：继续推进医疗项目 P1/P2 业务、安全、集成和交接优化，不要把 UI 当前阶段通过误判为项目完成。当前本地 style/mobile/build/test 与 9901 已通过，但报告仍显示真实外部集成和医疗最终产品 blocked。请从已有报告中找出仍可在本地继续闭环、且不依赖真实 OCR/LLM/LIMS/KMS/Redis/RabbitMQ/SQS 凭据的缺口，优先补齐生产交接、契约收敛、blocked 可诊断性、API/前端错误边界、异步任务/队列 readiness、安全基线或 E2E 验证中的真实缺口。

本轮目标：
1. 审计 PRODUCT-AUDIT-REPORT.md 和后续 MEDICAL-* 报告，建立 remaining/blocked 对照表，区分：已代码落地、本地可继续修复、必须等外部依赖。
2. 针对“本地可继续修复”的最高价值 P1/P2 项至少完成一批真实代码或测试/脚本/文档闭环；不能只写计划。
3. 保持 UI 现有 Material + Arco 设计系统；如触及 UI，只做增量修复间距、遮挡、换行等细节并保留既有 CSS 架构。
4. 继续强化“本地通过 ≠ 生产上线完成”的分层结论，不得把 blocked 写成 passed。
5. 生成本轮修复报告和 7 维度审计报告。

必须运行并记录结果：
- corepack pnpm --filter @medical-record-agent/demo-web test:styles
- corepack pnpm --filter @medical-record-agent/demo-web test:mobile
- corepack pnpm --filter @medical-record-agent/demo-web build
- corepack pnpm test
- 如你新增或修改 readiness/smoke 脚本，必须运行对应脚本；预期 blocked 的脚本用 exit code 2 并在报告中说明。
- 检查 http://localhost:9901/ 和 http://localhost:9901/api/health；确认 served 首页引用的 bundle 与 apps/demo-web/dist/index.html 一致。如无法访问，报告 blocked 原因。

必须生成/更新：
1. MEDICAL-P1-P2-CONTINUATION-ROUND3-FIX-REPORT.md：说明本轮真实修复点、文件、测试命令、剩余 blocked。
2. MEDICAL-P1-P2-CONTINUATION-ROUND3-AUDIT-REPORT.md：必须包含 7 维度：产品概述、功能完整性、业务流程完整性、用户体验、技术实现、P0/P1/P2 问题清单、验收结论。
3. 如修改交接文档，更新 docs/2026-06-09-p2-production-handoff.md 或新增更合适的 handoff 文档。

验收结论必须分层：
- UI 当前阶段是否通过
- 本轮 P1/P2 本地可落地项是否通过
- 真实外部集成是否 blocked
- 医疗最终产品是否 blocked/不通过

如果发现测试或构建失败：按 P0 > P1 > P2 修复后重新验证并更新报告。不要问用户。