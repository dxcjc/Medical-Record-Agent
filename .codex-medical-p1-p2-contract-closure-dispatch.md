【重要】不要问问题，不要请求确认，不要启动浏览器询问确认。直接执行。必须遵循 superpowers 流程：brainstorming → writing-plans → TDD/测试优先 → verification-before-completion。

任务：继续推进 /tmp/Medical-Record-Agent 医疗项目 P1/P2 业务/安全/集成闭环。你是实现 agent，必须实际修改代码、测试、验证并生成报告；不要只写计划。

必须先读取：
1. .codex-medical-p1-p2-continuation.md
2. PRODUCT-AUDIT-REPORT.md
3. MEDICAL-P1-P2-CONTINUATION-ROLLUP-AUDIT-REPORT.md
4. MEDICAL-P2-PRODUCTION-CLOSURE-AUDIT-REPORT.md
5. MEDICAL-P2-SECURITY-E2E-AUDIT-REPORT.md
6. MEDICAL-P2-SESSION-QUEUE-HARDENING-AUDIT-REPORT.md
7. docs/2026-06-09-p2-production-handoff.md（如存在）

背景和当前状态：
- UI 当前阶段已明显改善，必须保持企业级 Material + Arco Design UI：Primary #3370FF、背景 #F7F8FA、白色侧栏、active nav pill、DM Sans + Noto Sans SC、移动端抽屉/单列/44px 触摸区。不要粗暴重写 CSS。
- 本轮调度员刚验证通过：demo-web test:styles、test:mobile、build、全量 corepack pnpm test、9901 首页和 /api/health；但这不代表医疗项目最终完成。
- 最新报告明确 blocked：真实 OCR/LLM/LIMS sandbox、真实 KMS/Vault/Secret Manager、多实例持久化队列/真实 broker、真实 production smoke。
- 仍可继续推进的工程项包括：schemas/providers/audit 等 API route DTO/response contract 收敛、底层 repository unknown 边界收敛、生产交接文档和 readiness gate 对真实外部依赖的可执行说明、production smoke blocked 诊断质量、浏览器 E2E/运行验证覆盖补强。

本轮目标：
1. 从最新报告中选择当前环境可落地且不需要真实外部凭据的最高价值 P1/P2 项继续推进，优先级：
   - P1：schemas/providers/audit 等宽响应和 route DTO/response contract 继续收敛，避免 API 契约漂移。
   - P1/P2：生产交接/readiness/smoke 对真实外部依赖的 blocked 边界更可执行，避免把本地通过误判为最终完成。
   - P2：补强浏览器 E2E、API 契约测试、或 repository unknown 边界测试，只做能在本地验证的内容。
2. 不要伪造外部集成通过：没有真实 OCR/LLM/LIMS/KMS/broker 凭据时，必须写 blocked，不得写 passed。
3. 保持现有 UI 设计系统，除非为修复遮挡/换行/间距细节做极小 CSS patch；任何 UI 改动必须有 style/mobile/build 验证。
4. 不要提交 git commit，不要修改 .env、node_modules、缓存目录、构建产物作为源码提交内容。

必须执行并记录验证结果：
- corepack pnpm --filter @medical-record-agent/demo-web test:styles
- corepack pnpm --filter @medical-record-agent/demo-web test:mobile
- corepack pnpm --filter @medical-record-agent/demo-web build
- corepack pnpm test
- 如改动 API/production smoke/readiness，运行相关定向 vitest。
- 检查 http://localhost:9901/ 和 http://localhost:9901/api/health；如服务不可达，在报告中明确说明不可达原因，不要谎称通过。

必须生成报告：
1. MEDICAL-P1-P2-CONTRACT-CLOSURE-FIX-REPORT.md：说明修复范围、文件、测试、剩余 blocked。
2. MEDICAL-P1-P2-CONTRACT-CLOSURE-AUDIT-REPORT.md：必须包含 7 维度：产品概述、功能完整性、业务流程完整性、用户体验、技术实现、P0/P1/P2 问题清单、验收结论。结论必须分层：UI 当前阶段、P1/P2 本轮工程项、真实外部集成、医疗最终产品。

验收标准：
- 有真实代码或测试改动，不是只产计划。
- 报告文件存在且包含 7 维度。
- 构建/测试结果明确，失败必须列出具体失败点和下一步。
- 不把“本地阶段通过”写成“医疗最终完成”。
- 不破坏 9901 当前可访问状态。

如果遇到 429、上游断流或工具失败：保留已完成改动和报告草稿，报告中明确中断点，不要反复重试。