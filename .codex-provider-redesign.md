# 任务：重新设计 Provider 设置页面

## 背景
医疗项目 Provider 设置页面（apps/demo-web/src/pages/operations/ProviderSettingsPage.tsx）当前是 4 个平铺的表单卡片，UX 很差。需要重新设计为现代化的卡片布局。

## 当前问题
1. 4 个 Provider（OCR、LLM、Storage、LIMS）平铺显示，没有视觉层次
2. 字段名全是英文（Endpoint、Model/Profile、Secret）
3. 没有状态指示灯（健康/降级/未配置）
4. 保存/测试按钮在页面顶部，不在卡片内
5. 未配置时显示空白表单，没有引导
6. 信息密度太高，各种 alert/notice 堆叠

## 设计要求

### 1. 顶部状态总览
- 4 个小卡片横排，每个显示：Provider 名称 + 状态灯（绿=健康、黄=降级、红=未配置/故障）
- 点击卡片跳转到对应配置区

### 2. Provider 卡片化
每个 Provider 用独立 Card：
- 卡片头部：图标 + 名称 + 状态标签（健康/降级/未配置/未检查）
- 卡片内容：配置表单
- 卡片底部：操作按钮（保存配置、测试连接、启用/禁用开关）

### 3. 中文化字段
- Endpoint → 服务地址
- Model / Profile → 模型/配置
- Secret → API 密钥
- 超时毫秒 → 超时时间（毫秒）
- 启用状态 → 启用
- Provider 类型 → 类型
- Bucket / Prefix → 存储桶/路径

### 4. 未配置引导
- OCR 未配置时显示虚线卡片 + "点击配置 OCR 服务" 按钮
- 点击后展开配置表单

### 5. 样式规范
- Material + Arco 风格
- 白色卡片 + 阴影
- 蓝色主题 #3370FF
- 字体 DM Sans + Noto Sans SC
- 卡片间距 16px
- 响应式：平板单列，桌面双列

## 技术上下文
- 项目：/tmp/Medical-Record-Agent
- 文件：apps/demo-web/src/pages/operations/ProviderSettingsPage.tsx（835行）
- 使用 Arco Design 组件库
- 样式：apps/demo-web/src/styles.css
- 路由：/providers

## 完成标准
1. ProviderSettingsPage.tsx 重构为卡片布局
2. 中文字段名
3. 每个卡片有独立操作按钮
4. 顶部状态总览
5. `corepack pnpm --filter @medical-record-agent/demo-web build` 编译通过
6. 输出修改摘要
