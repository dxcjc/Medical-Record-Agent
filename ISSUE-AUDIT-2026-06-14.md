# Medical Record Agent — 产品问题排查报告

**排查日期**: 2026-06-14  
**排查方式**: 浏览器实测 + API 端点批量测试 + 代码审查  
**排查人**: Superpowers 系统性排查

---

## 🔴 P0 — 按钮点了没反应（API 返回错误）

### 1. Schema 停用按钮无效
- **现象**: 点击"停用"按钮无任何反馈
- **根因**: 前端 POST `/schemas/versions/:id/deactivate` 不带 body，后端 Fastify 要求 JSON body → 返回 `FST_ERR_CTP_EMPTY_JSON_BODY` (400)
- **验证**: `curl -X POST .../deactivate` 无 body → 400；带 `{}` → 正常
- **修复**: 前端发送空 `{}` body，或后端允许空 body

### 2. Schema 回滚按钮无效
- **现象**: 同上
- **根因**: 同上，POST 无 body → 400
- **修复**: 同上

### 3. 任务删除按钮无效
- **现象**: 删除任务无反应
- **根因**: DELETE `/jobs/:id` 无 body → `FST_ERR_CTP_EMPTY_JSON_BODY`
- **验证**: 带 `{}` body → 正常删除

### 4. 任务重跑按钮无效
- **现象**: 重跑任务无反应
- **根因**: POST `/jobs/:id/rerun` 无 body → `FST_ERR_CTP_EMPTY_JSON_BODY`
- **验证**: 带 `{}` body → 正常创建新任务

### 5. 写回触发按钮无效
- **现象**: 手动触发写回无反应
- **根因**: POST `/writeback/:id` 无 body → `FST_ERR_CTP_EMPTY_JSON_BODY`

**共同根因**: 前端 `request()` 函数对 POST/DELETE 请求不带 body 时，Fastify 的 JSON content-type parser 报错。

---

## 🔴 P0 — 路由不存在（前端调用的 API 后端没实现）

### 6. 字段统计 API 404
- **现象**: Schema 详情页"识别统计"始终显示"暂无统计数据"
- **根因**: 后端注册路由为 `/api/stats/fields`，但 nginx 反代后后端收到 `/stats/fields` → 404
- **验证**: 直接访问 `http://127.0.0.1:3000/stats/fields` → 404；访问 `/api/stats/fields` → 200
- **额外问题**: 即使路由通了，返回的 `fieldKey` 是数组索引 "0", "1", "2"... 而非实际字段名

### 7. Dashboard 统计 API 404
- **现象**: Dashboard 统计卡片数据可能不准确
- **根因**: 前端调用 `/stats/dashboard`，后端无此路由

### 8. 趋势图 API 404
- **现象**: 趋势图无数据
- **根因**: 同 #6，路由前缀不匹配

### 9. 任务导出 API 404
- **现象**: 导出按钮无反应
- **根因**: 后端无 `/jobs/:id/export` 路由（只有 v1 路由 `/v1/jobs/:id`）

### 10. Provider 创建 API 404
- **现象**: 无法新建 Provider
- **根因**: 后端无 `POST /providers` 创建路由（只有 GET 列表和 PUT 更新）

### 11. 反馈审核 API 404
- **现象**: 反馈管理页无法审核/批准/拒绝反馈
- **根因**: 后端无 `PATCH /feedback/:id` 路由
- **影响**: 反馈数据写入后无法闭环，不影响知识库

---

## 🟠 P1 — 功能缺失（显示了但不可用）

### 12. Provider 管理只有只读
- **现状**: 只有"健康检查"按钮，无新建/编辑/删除
- **需求**: 完整 CRUD（新建 Provider、编辑配置、删除、设置默认、启禁用）

### 13. 反馈管理是数据孤岛
- **现状**: 能提交反馈，但无法审核、批准、拒绝
- **需求**: 审核按钮 + 批准后写入 KnowledgeEntry + 批量审核

### 14. 评测中心样本导入门槛高
- **现状**: 只支持粘贴 JSON 数组
- **需求**: 从识别结果导入、逐条表单录入、CSV 上传

### 15. 回写管理无手动触发
- **现状**: "可回写任务" tab 为空，无法手动触发回写
- **需求**: 展示已完成任务列表 + 一键回写按钮

### 16. Schema 无新建/编辑能力
- **现状**: 只能查看、停用、回滚，无法创建新 Schema
- **需求**: 新建 Schema 表单 + 字段编辑器 + 发布流程

---

## 🟡 P2 — 数据/显示问题

### 17. 空值字段置信度显示 0%
- **现象**: 身份证号、输血史等空字段显示 0% 置信度，标红"建议人工复核"
- **用户要求**: 空值也是识别结果，置信度应为 100%，标为"已完成"
- **当前状态**: `normalizeFields` 已改为 100%，但老任务数据可能未更新

### 18. 字段统计返回数组索引
- **现象**: `/api/stats/fields` 返回 `fieldKey: "0", "1", "2"...`
- **根因**: stats service 遍历 RecognitionResult.fields 数组时用索引而非 fieldKey
- **影响**: Schema 详情页的字段统计完全错误

### 19. 任务列表无分页
- **现状**: 请求 `?limit=200` 一次性加载
- **需求**: 后端分页 + 前端分页组件

### 20. 登录状态不持久化
- **现象**: 刷新页面跳回登录页
- **根因**: `App.tsx` 未调用 `authStore.restore()`
- **修复**: App 启动时调用 restore()

---

## 🟢 P3 — 体验优化

### 21. 反馈列表参数错误
- **现象**: GET `/feedback` 返回 BAD_REQUEST
- **根因**: 后端要求必填参数（如 jobId），前端未传

### 22. 任务列表"文件名"列始终显示 "-"
- **现象**: 所有任务的文件名列都是 "-"
- **根因**: 前端可能未正确关联 StoredFile 的 originalName

### 23. 任务列表"整体置信度"列始终显示 "-"
- **现象**: 所有任务的整体置信度都是 "-"
- **根因**: 前端未从 RecognitionResult 计算整体置信度

### 24. 任务列表"需复核"列始终显示 "-"
- **现象**: 同上
- **根因**: 前端未从 RecognitionResult 统计需复核字段数

### 25. 任务列表"Provider"列始终显示 "-"
- **现象**: 同上
- **根因**: 任务创建时未记录使用的 Provider

### 26. 任务列表"创建人"列始终显示 "-"
- **现象**: 同上
- **根因**: 任务创建时 createdById 为 null（示例文件通过 API 创建未关联用户）

---

## 📊 统计汇总

| 等级 | 数量 | 说明 |
|------|:---:|------|
| 🔴 P0 按钮无效 | 5 | API 报错，按钮点了没反应 |
| 🔴 P0 路由缺失 | 6 | 前端调用的 API 后端没实现 |
| 🟠 P1 功能缺失 | 5 | 显示了但不可用 |
| 🟡 P2 数据问题 | 4 | 显示错误或缺失 |
| 🟢 P3 体验优化 | 6 | 列表列数据缺失 |
| **合计** | **26** | |

---

## 🔧 实施建议

### 第一批（P0 修复，预计 2-3 小时）
1. 修复 `request()` 函数：POST/DELETE 无 body 时发送空 `{}`
2. 修复 stats 路由前缀（去掉 `/api/` 或统一所有路由前缀）
3. 修复 stats service 的 fieldKey 返回数组索引问题
4. 添加缺失路由：feedback PATCH、provider POST、job export
5. App.tsx 添加 restore() 调用

### 第二批（P1 功能补全，预计 1-2 天）
1. Provider CRUD 完整化
2. 反馈审核闭环
3. 评测样本导入增强
4. 回写手动触发
5. Schema 新建/编辑

### 第三批（P2+P3 优化，预计 1 天）
1. 任务列表分页
2. 列表列数据补全
3. 老任务数据兼容
