【重要】不要问问题，不要请求确认，不要启动浏览器询问确认。直接执行。你是医疗项目 P1/P2 后续业务/安全/集成优化 worker。

【必须遵循 superpowers 流程】
读取项目内 CLAUDE.md（如存在）、.codex-medical-p1-p2-continuation.md、PRODUCT-AUDIT-REPORT.md、最新 MEDICAL-P2-ASYNC-HANDOFF-AUDIT-REPORT.md 和相关 P2 报告。必须按 brainstorming → writing-plans → TDD/测试优先 → verification-before-completion 执行。不要只写计划，必须实际开发/修复/验证/生成报告。

【项目目录】/tmp/Medical-Record-Agent

【背景口径】
医疗项目不是最终完成；UI 已明显改善，当前 UI/构建/本地异步 UX 阶段可肯定，但最终产品仍被真实 OCR/LLM/LIMS sandbox、真实 KMS/Vault/Secret Manager、生产多实例 session store、真实 broker 多实例可靠队列、production smoke/E2E 等外部/集成项阻塞。不要把本地 build 通过写成最终完成。

【本轮目标】
继续推进可在当前仓库落地的 P1/P2 闭环，优先把“外部阻塞项”转化为可执行 readiness gate、contract test、smoke harness、文档化 handoff 和失败不误判的产品保护，不要伪造真实外部服务通过。

【必须处理/复核的点】
1. 写回可信边界：确认写回服务端仍不信任前端 fields/payload，必须从 RecognitionResult/readyFields 读取或比对；如有缺口，先补测试再修复。
2. demo API 闭环与静态 fallback：生产/非 demo 路径不能用静态 fallback 掩盖真实失败；如仍存在用户可见主线 fallback，补测试并收敛。
3. Evaluation schema：确认 Evaluation run/runner 不再硬编码固定 schema；如果仍有硬编码或契约不清，补测试并修复/报告。
4. production smoke：在没有外部凭据时，必须提供明确跳过/阻塞原因和 readiness gate；不能把缺凭据写 PASS。
5. API 契约/DTO：继续收敛 route service unknown 宽松输入，补关键 DTO/contract test。
6. 异步队列/安全/E2E：继续补可本地验证的 session invalidation、queue handoff、abort/retry、security headers、E2E smoke 防线；真实 broker/KMS/sandbox 不可用则生成外部接入清单和阻塞报告。
7. UI 细节：保持现有 Material + Arco Design，不粗暴重写 CSS；可继续修间距、遮挡、换行、移动端 44px 等细节，但不要牺牲业务/安全主线。

【必须运行验证】
- corepack pnpm --filter @medical-record-agent/demo-web test:styles
- corepack pnpm --filter @medical-record-agent/demo-web test:mobile
- corepack pnpm --filter @medical-record-agent/demo-web build
- corepack pnpm test
- 如项目已有 production/readiness/smoke/e2e 脚本，运行可在当前环境执行的脚本；缺外部凭据必须明确 blocked，不能伪造通过。

【必须产出/更新报告】
1. MEDICAL-P1-P2-NEXT-BUSINESS-SECURITY-FIX-REPORT.md：说明本轮真实修复点、涉及文件、测试红绿、验证结果、剩余外部阻塞。
2. MEDICAL-P1-P2-NEXT-BUSINESS-SECURITY-AUDIT-REPORT.md：必须包含 7 维度：产品概述、功能完整性、业务流程完整性、用户体验、技术实现、P0/P1/P2 问题清单、验收结论。

【验收结论口径】
必须明确区分：UI/本地代码阶段、业务/安全 P1/P2 本轮阶段、真实外部集成、医疗最终产品。真实外部服务未接入前，最终产品只能写 blocked/不通过；可写“本轮可本地落地事项通过”。不要提交 git commit。