读取 CLAUDE.md 了解核心规则。

## 任务：继续完成识别详情页 UX 优化

上一轮已完成 NewRecognitionPage 的优化，现在继续做 JobDetailPage。

读取 `docs/superpowers/plans/2026-06-10-recognition-create-and-review-ux.md` 获取完整计划。

### JobDetailPage 要求
1. 详情页标题改为"识别结果复核"
2. 按任务状态、进度、原件预览、OCR 文本、字段复核、技术详情组织
3. 保留原件展示，但不做图片/PDF 坐标框标注
4. 证据联动先高亮 OCR 文本或 OCR block
5. 不做 LIMS 写回，移除"确认写回"主动作
6. 支持字段选择、当前确认值编辑、无法确认、保存复核的前端状态
7. 移除写回 CTA 和 Payload Preview 的默认暴露

### 文件
- Modify: `apps/demo-web/src/pages/recognition/JobDetailPage.tsx`
- Modify: `apps/demo-web/src/pages/recognition/JobDetailPage.test.ts`
- Modify: `apps/demo-web/src/styles.css`（新增详情复核布局样式）

### 验收标准
1. 测试通过
2. build 通过
3. 生成 ACCEPTANCE-REPORT.md

【重要】不要问问题，直接开始工作。完成后运行 npm test 和 npm run build 验证。
