【重要】不要问问题，不要请求确认，不要启动浏览器询问确认。直接执行。必须遵循 superpowers 流程：brainstorming → writing-plans → TDD/测试优先 → verification-before-completion。

你是医疗项目 P1/P2 产品闭环 worker。工作目录：/tmp/Medical-Record-Agent。

背景与口径：
- 医疗项目不是最终完成；UI 比之前好了很多，但仍只能算当前 UI/构建/运行验证阶段通过。
- 必须继续按 `.codex-medical-p1-p2-continuation.md`、`PRODUCT-AUDIT-REPORT.md`、`MEDICAL-P1-P2-CONTINUATION-ROLLUP-AUDIT-REPORT.md` 的未完成点推进。
- 不要把真实外部 OCR/LLM/LIMS sandbox、真实 KMS/Vault/Secret Manager、生产多实例 session invalidation store、真实 broker 多实例可靠队列、production smoke blocked 写成通过。

本轮任务：医疗 P1/P2 非外部依赖项继续闭环 + 报告更新。

必须先读取：
1. `.codex-medical-p1-p2-continuation.md`
2. `PRODUCT-AUDIT-REPORT.md`
3. `MEDICAL-P1-P2-CONTINUATION-ROLLUP-AUDIT-REPORT.md`
4. 最新的 `MEDICAL-P2-*AUDIT-REPORT.md` / `MEDICAL-P2-*FIX-REPORT.md`

优先修复/补强范围（只做当前环境可落地的，不伪造外部集成）：
1. API route/service DTO 收敛：从旧审计 P1-6 出发，继续把高风险 `unknown` route service input/response 收敛为共享 DTO/Zod 或等效 schema，优先覆盖 writeback、jobs、files、feedback、evaluation 的请求/响应契约；必须先写/补测试。
2. Production smoke blocked 可诊断性：不能绕过真实 sandbox，但要确保 blocked 输出清晰、机器可读，能区分缺 BASE_URL/EMAIL/PASSWORD、secret resolver env-only、session store in-memory、queue broker 未配置，并有测试覆盖。
3. Production readiness gate：补强文档/脚本，使“本地通过”和“生产可上线”分层明确，避免后续误报最终完成。
4. UI 细节只做低风险补强：如果发现明显间距、遮挡、换行问题，可补样式守卫或小修；不要重写 CSS，不破坏 Material + Arco Design。

必须验证并写入报告：
- `corepack pnpm --filter @medical-record-agent/demo-web test:styles`
- `corepack pnpm --filter @medical-record-agent/demo-web test:mobile`
- `corepack pnpm --filter @medical-record-agent/demo-web build`
- `corepack pnpm test`
- 如修改 API 契约/脚本，必须跑对应定向 vitest/tsc。
- 如 `corepack pnpm smoke:production` 仍 blocked，报告要明确 blocked 是因外部配置/真实依赖缺失，不得写成 failed 或 passed。

产出要求：
1. 生成/更新 `MEDICAL-P1-P2-CONTINUATION-ROLLUP-AUDIT-REPORT.md`，必须包含 7 维：产品概述、功能完整性、业务流程完整性、用户体验、技术实现、P0/P1/P2 问题清单、验收结论。
2. 生成 `MEDICAL-P1-P2-DTO-SMOKE-FIX-REPORT.md`，说明本轮修复点、文件、测试、剩余 blocked。
3. 报告结论必须分层：UI 当前阶段、P1/P2 本轮可落地项、真实外部集成、医疗最终产品。

约束：
- 不提交 git commit。
- 不修改 `.env`、node_modules、缓存。
- 不启动新项目，不重写架构。
- 如果遇到 429/上游断流，保留已完成改动和报告草稿，明确写明中断点后退出。
