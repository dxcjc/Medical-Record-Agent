【重要】不要问问题，不要启动浏览器，不要询问确认。直接开始工作。必须遵循 superpowers 流程：brainstorming → writing-plans → TDD/测试优先 → verification-before-completion。

任务：推进医疗项目本地可落地的 P2 业务/安全/集成优化。

工作目录：/tmp/Medical-Record-Agent

背景：
- UI 阶段已通过，demo 模式已移除，服务器运行在 production mode
- PostgreSQL 已运行，数据库表和种子数据已就绪
- 全量测试通过（76 文件、449 测试）
- Build 通过，9901 端口服务正常
- 当前剩余 P2 项中，以下可在本地闭环：

## 必须完成的任务

### 任务 1：验证引擎关键字段 Schema 驱动化（P2-1）
文件：`packages/core/src/engine/validationEngine.ts`
- 当前第 32 行硬编码 `clinicalDiagnosis` 为唯一关键字段
- 改为从 Schema 字段定义中读取 `critical: true` 或 `required: true` 标记
- 修改 `getCriticalFieldKeys(schema)` 函数，使其基于 schema.fields 动态返回
- 如果 schema.fields 中没有 critical 标记，回退到 required 字段
- 补充单元测试验证：有 critical 标记时用 critical、无 critical 时用 required、空 schema 时返回空数组

### 任务 2：数据库集成测试启用（P2 数据层）
- 检查 `apps/api/src` 下被 skip 的数据库集成测试
- PostgreSQL 已在 localhost:5432 运行，数据库 medical_record_agent 已有表和种子数据
- 将可本地运行的数据库集成测试启用（去掉 skip）
- 确保测试使用事务回滚或独立数据，不污染种子数据
- 如果测试需要特定环境变量，检查 .env 是否已配置

### 任务 3：API 请求速率限制（P2 安全）
- 在 `apps/api/src/server.ts` 或中间件层添加基本速率限制
- 使用内存存储（不依赖 Redis），对同一 IP 的请求做窗口限制
- 推荐配置：登录接口 5 次/分钟，普通 API 60 次/分钟
- 不要引入新依赖，用简单计数器实现或使用已有的 express 中间件
- 补充测试验证速率限制逻辑

### 任务 4：生产 Smoke 测试本地化（P2 验证）
文件：`scripts/production-smoke.ts`
- 当前 production smoke 在无外部 sandbox 时返回 blocked
- 新增 `mock-production` 模式的端到端测试：模拟登录、创建任务、获取结果、写回尝试
- 确保 mock-production 模式能完整跑通所有步骤，不依赖外部服务
- 补充测试覆盖 mock-production 模式的完整流程

### 任务 5：Prisma Schema 完善
- 检查 prisma/schema.prisma 中的模型是否覆盖所有业务实体
- 对比 API routes 使用的数据结构，确保无遗漏
- 如果发现缺失字段或模型，补充 schema 并生成迁移
- 运行 `corepack pnpm db:migrate` 验证迁移

## 验收标准
1. `corepack pnpm test` 全量通过（不能有新增 skip）
2. `corepack pnpm --filter @medical-record-agent/demo-web build` 通过
3. `corepack pnpm --filter @medical-record-agent/demo-web test:styles` 通过
4. `corepack pnpm --filter @medical-record-agent/demo-web test:mobile` 通过
5. 9901 端口 `/api/health` 返回 200
6. 生成 `P2-BUSINESS-SECURITY-FIX-REPORT.md` 报告，包含 7 维度审计

## 约束
- 不要修改 CSS 或 UI 样式
- 不要修改 .env 文件
- 不要提交 git commit
- 不要引入新的外部依赖（如 Redis、Bull 等）
- 保持 Material + Arco Design 不变
- 如果某任务因外部依赖无法完成，明确记录 blocked 原因，不要伪造通过
