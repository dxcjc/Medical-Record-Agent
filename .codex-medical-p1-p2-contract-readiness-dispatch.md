【重要】不要问问题，不要请求确认，不要启动浏览器询问确认。直接执行。必须遵循 superpowers 流程：brainstorming → writing-plans → TDD/测试优先 → verification-before-completion。

任务：继续推进医疗项目 P1/P2 业务/安全/集成闭环，不要停在 UI 阶段。保持企业级 Material + Arco Design UI，不要粗暴重写 CSS，不要破坏 9901 部署/API 代理，不要提交 git commit。

工作目录：/tmp/Medical-Record-Agent

必须先读取：
1. .codex-medical-p1-p2-continuation.md
2. PRODUCT-AUDIT-REPORT.md
3. MEDICAL-P1-P2-CONTINUATION-ROLLUP-AUDIT-REPORT.md
4. MEDICAL-P1-P2-DTO-SMOKE-FIX-REPORT.md
5. MEDICAL-ARCO-UI-AUDIT-REPORT.md、STYLE-FIX-REPORT.md、MEDICAL-UI-REFLOW-FIX-REPORT.md、P1-AUDIT-REPORT.md、P1-FIX-REPORT.md

当前状态：UI 当前阶段和上一轮 DTO/smoke/readiness 本地可落地项已通过；医疗最终产品仍未完成。真实外部 OCR/LLM/LIMS sandbox、真实 KMS/Vault/Secret Manager、生产多实例 session store、真实 broker 多实例队列仍 blocked，不允许伪造通过。

本轮目标：围绕 PRODUCT-AUDIT-REPORT 和 rollup audit 中仍可本地推进的 P1/P2 继续收敛，重点优先处理：
1. schemas/providers/audit 等仍偏宽的 route response/request contract，尽量用 Zod/显式 DTO/response guard 收敛，不破坏既有 API。
2. 补强 production smoke / readiness / handoff 对“blocked 外部依赖”的可诊断输出和测试，不要把 blocked 写成 passed。
3. 补强浏览器 E2E/production smoke 的本地可执行契约；若真实外部环境缺失，明确 blocked 分类与缺失项。
4. 保持 demo-web chunk、style/mobile/build 稳定；不要引入 500k 主业务 chunk 警告。
5. 如果发现更高优先级 P0/P1（构建失败、测试失败、写回可信边界回退、静态 fallback 掩盖生产失败、安全边界回退），先修这些。

必须执行方式：
- 先用 TDD：为本轮 contract/readiness/smoke 问题补测试或更新已有测试，先红后绿。
- 不要手写空报告冒充完成；必须实际代码落地、测试通过。
- 不要修改 .env、node_modules、无关缓存。
- 不要提交 git commit。
- 若遇到 429/上游断流，保留已完成改动并在报告中明确中断点。

必须验证并在报告中记录结果：
- corepack pnpm --filter @medical-record-agent/demo-web test:styles
- corepack pnpm --filter @medical-record-agent/demo-web test:mobile
- corepack pnpm --filter @medical-record-agent/demo-web build
- corepack pnpm test
- 如触及 production smoke/readiness：运行相关 vitest 和/或 corepack pnpm smoke:production，并明确 passed/blocked/failed 区分。

必须生成/更新报告：
1. MEDICAL-P1-P2-CONTRACT-READINESS-FIX-REPORT.md：说明本轮修复点、文件、测试、剩余 blocked。
2. MEDICAL-P1-P2-CONTRACT-READINESS-AUDIT-REPORT.md：必须包含 7 个维度：产品概述、功能完整性、业务流程完整性、用户体验、技术实现、P0/P1/P2 问题清单、验收结论。验收结论必须分层写：UI 当前阶段、P1/P2 本轮可落地项、真实外部集成、医疗最终产品。
3. 如更新其他既有报告，必须保留 7 维度，不要删减。

验收标准：
- 代码中存在真实 contract/readiness/smoke 改动和对应测试，不只是计划。
- 上述 demo-web style/mobile/build 通过。
- corepack pnpm test 通过；若失败必须明确失败原因和下一步，不能写通过。
- 报告存在且 7 维度齐全。
- 不得声明医疗项目最终完成。