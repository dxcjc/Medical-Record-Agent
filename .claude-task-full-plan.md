【必须遵循 superpowers 流程】
读取 CLAUDE.md 了解核心规则。

## 任务：完成识别创建与详情复核 UX 优化（完整计划）

读取 `docs/superpowers/plans/2026-06-10-recognition-create-and-review-ux.md` 获取完整实现计划。

**你必须按照计划中的所有 Task 和 Step 逐一执行，直到全部完成。不要只做一部分就停。**

### 计划概览

计划包含 4 个 Task，每个 Task 有多个 Step：

**Task 1: New Recognition Behavior Tests**（红灯）
- Step 1-4: 写失败测试，验证 RED

**Task 2: New Recognition Implementation**（绿灯）
- Step 1-11: 实现新建识别页的所有改动
- 关键：移除 OCR Provider 下拉、合成样本按钮、写回选项

**Task 3: Job Detail Review Tests**（红灯）
- Step 1-3: 写失败测试，验证 RED

**Task 4: Job Detail Review Implementation**（绿灯）
- Step 1-10: 实现详情页复核工作台
- 关键：标题改"识别结果复核"、移除写回 CTA、新增字段复核

### 执行要求

1. **按顺序执行所有 Task 和 Step**，不要跳过任何步骤
2. **TDD 流程**：先写测试（红灯），再实现（绿灯），最后验证
3. **每个 Step 完成后勾选计划中的 checkbox**（`- [x]`）
4. **遇到错误就修复**，不要停在错误状态
5. **最后运行完整验证**：
   - `npx tsc --noEmit`（零错误）
   - `npm test`（全部通过）
   - `npm run build`（通过）

### 验收要求

完成所有 Step 后，生成 `ACCEPTANCE-REPORT.md`，包含：

1. **任务完成度**：列出每个 Task/Step 的完成状态
2. **改动文件清单**：每个文件的改动内容
3. **测试结果**：tsc、test、build 的输出
4. **审计维度**（7 维度）：
   - 功能完整性：是否覆盖计划中的所有功能
   - 代码质量：类型安全、无 any/unknown
   - 测试覆盖：测试是否覆盖新行为
   - UI/UX：界面是否符合计划要求
   - 性能：是否有性能问题
   - 安全：是否有安全隐患
   - 可维护性：代码是否易于维护
5. **剩余问题**：如有未完成或有问题的部分，明确列出

### 重要提示

- 不要问问题，直接开始工作
- 不要只做一部分就停，必须完成所有 Task 和 Step
- 如果某个 Step 无法完成，记录原因并继续下一个
- 最终必须有完整的审计报告
