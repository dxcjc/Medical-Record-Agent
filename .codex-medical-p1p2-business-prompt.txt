【重要】不要问问题，不要请求确认，不要启动浏览器询问确认。直接执行。必须遵循 superpowers 流程：brainstorming → writing-plans → TDD/测试优先 → verification-before-completion。

任务：继续推进医疗项目 P1/P2 业务闭环修复，重点修复以下三个问题并生成修复报告。

工作目录：/tmp/Medical-Record-Agent

背景：
- 项目 build/typecheck/test 全部通过，UI 已完成 Material + Arco Design 升级。
- 当前处于"阶段通过"状态，需要推进剩余 P1/P2 项以接近最终通过。

必须完成的任务（按优先级）：

## 任务 1：P1-2 Demo API 识别任务创建闭环

当前问题：
- `apps/api/src/demo-services.ts` 的 `jobService.create()` 直接 throw `createRealProviderNotConfiguredError()` (503)
- 但 `internalTestRecognitionService.createWithSyntheticProviders()` 可以通过 `demoRecognitionOrchestrator.start()` 跑 mock 编排
- 前端 `NewRecognitionPage.tsx` 调用 `api.createRecognitionJob()` → 路由 → `jobService.create()` → 503 错误
- 用户在 demo 模式下无法创建识别任务

修复要求：
- 让 demo API 的 `jobService.create()` 使用 mock OCR + mock LLM + core validation 编排，按 jobId 保存结果
- 不再抛出 503 错误，而是走 `demoRecognitionOrchestrator.start()` 完整编排链路
- 保持 `resultService.getByJobId()` 从 Map 中按 jobId 读取真实编排结果
- 确保 demo-services.test.ts 覆盖：创建任务后按 jobId 返回 mock 编排结果，不存在 jobId 返回 null

## 任务 2：P1-6 API 路由 Service 接口类型收紧

当前问题：
- `apps/api/src/routes/writeback.routes.ts` 的 `WritebackRouteService.execute(input: unknown)` 使用 unknown
- `apps/api/src/routes/jobs.routes.ts` 的 `JobRouteService` 已有具体类型 ✅
- 需要检查其他路由 service 接口是否有 unknown，逐步收紧

修复要求：
- 检查所有 `apps/api/src/routes/*.ts` 中 service interface 的方法签名
- 对仍有 `unknown` 参数的 service 方法，定义具体的 Zod schema 或 DTO 类型
- 不要一次性改完，优先处理高频使用的路由（writeback、providers、schemas、evaluation）
- 确保 typecheck 和 test 仍然通过

## 任务 3：P2-1 关键字段规则 Schema 化

当前问题：
- `packages/core/src/engine/validationEngine.ts:21` 硬编码 `fieldKey === "clinicalDiagnosis"` 判断关键字段
- `packages/core/src/engine/autoDecisionPolicy.ts:35` 同样硬编码

修复要求：
- 在 `CoreFieldDefinition` 类型中增加 `critical?: boolean` 和 `required?: boolean` 属性
- validationEngine 和 autoDecisionPolicy 从 Schema 定义读取 critical/required，不再硬编码字段名
- 保留 `clinicalDiagnosis` 作为默认 critical 字段的向后兼容（如果 Schema 没有标记任何字段为 critical）
- 新增单元测试覆盖：标记其他字段为 critical 时行为正确

## 验证要求（必须全部通过）：
1. `corepack pnpm --filter @medical-record-agent/demo-web build`
2. `corepack pnpm typecheck`
3. `corepack pnpm test`
4. `corepack pnpm --filter @medical-record-agent/demo-web test:styles`
5. `corepack pnpm --filter @medical-record-agent/demo-web test:mobile`
6. `curl -s http://localhost:9901/api/health` 返回 ok

## 报告要求：
生成 `MEDICAL-P1P2-BUSINESS-FIX-REPORT.md`，包含：
1. 每个任务的修复文件列表
2. 具体改动描述
3. 新增/修改的测试
4. 验证命令结果
5. 与上次审计报告的差异对比
6. 剩余未完成项（P1-5 production smoke、P2-2~P2-5）

约束：
- 不要提交 git commit
- 不要修改 .env、node_modules、无关缓存
- 不要粗暴重写 CSS，保持 Material + Arco 设计系统
- 不要问问题，直接执行
- 若遇到 429/上游断流，保留已完成改动和报告草稿，明确记录中断点
