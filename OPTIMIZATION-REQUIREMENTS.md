# Medical Record Agent — 产品优化需求文档 v1.0

**文档日期**: 2026-06-14  
**审计范围**: 全系统前端 + 后端 + 运行时  
**优先级定义**: P0=阻断体验 / P1=核心缺陷 / P2=功能缺失 / P3=体验优化

---

## 一、P0 — 阻断体验（必须立即修复）

### 1.1 登录状态不持久化

**现象**: 刷新页面 → 跳回登录页 → 需要重新登录  
**根因**: `authStore.ts` 有 `restore()` 方法从 localStorage 恢复 token，但 `App.tsx` **从未调用** `restore()`。Zustand 内存状态在刷新后丢失，`ProtectedRoute` 检查 `isAuthenticated` 为 false，直接跳转。  
**代码位置**: `App.tsx:30`（缺少 useEffect 调用 restore）  
**修复方案**: App 启动时调用 `restore()`，或使用 Zustand persist middleware  
**影响**: 每次刷新都要重新登录，用户体验极差

### 1.2 识别结果字段统计不更新

**现象**: 完成多次识别后，Schema 详情页的字段识别率/覆盖率统计始终为空  
**根因**: `stats.service.ts` 查询的是 `RecognitionResult` 表的数据，但识别完成后没有触发统计更新。字段统计是从结果表实时聚合查询的，需要确认查询逻辑是否正确。  
**代码位置**: `stats.service.ts` 的 `getFieldStats` 函数  
**影响**: Schema 管理页面的统计功能完全失效，用户无法看到识别质量数据

---

## 二、P1 — 核心缺陷（影响产品可信度）

### 2.1 反馈管理是"数据孤岛"

**现状**:
- 反馈提交（JobDetailPage 中）→ 仅写入 `FeedbackSubmission` 表
- `FeedbackPage` 仅展示列表，**无审核/批准/拒绝按钮**
- 后端无 `PATCH /feedback/:id/status` 端点
- 反馈数据**不影响**知识库、不影响后续识别结果

**需求**:
1. 反馈管理页增加审核操作（批准/拒绝）
2. 批准的反馈自动写入 KnowledgeEntry 表
3. 后续识别时 RAG 检索能命中反馈修正的字段值
4. 批量审核支持

### 2.2 评测中心样本导入门槛高

**现状**: 只支持粘贴 JSON 数组，非技术用户完全无法使用  
**代码位置**: `EvaluationPage.tsx` 的 `ImportSamplesModal`  
**需求**:
1. 支持从已有识别结果中一键选择样本（下拉选择任务 → 导入其结果）
2. 支持逐条表单录入（字段名 + 期望值）
3. 支持 CSV/Excel 上传

### 2.3 Provider 管理只有只读展示

**现状**: ProviderPage 只能查看 Provider 列表和状态，无法新建/编辑/删除  
**代码位置**: `ProviderPage.tsx` 无任何 mutation 调用  
**需求**:
1. 新建 Provider（选择类型 OCR/LLM/Storage/LIMS → 填配置）
2. 编辑 Provider 配置
3. 删除 Provider（带确认弹窗）
4. 设置默认 Provider
5. 启用/禁用切换

### 2.4 Schema 管理缺少新建/编辑能力

**现状**: SchemaPage 只能查看和停用/回滚，无法创建新 Schema 或编辑字段定义  
**代码位置**: `SchemaPage.tsx` 无 create/update mutation  
**需求**:
1. 新建 Schema（填写 key、名称、字段列表）
2. 编辑字段定义（类型、描述、是否必填）
3. 发布新版本
4. 字段顺序调整

---

## 三、P2 — 功能缺失（影响产品完整性）

### 3.1 任务列表缺少分页和搜索

**现状**: 任务列表请求 `?limit=200`，一次性加载全部任务  
**需求**:
1. 后端分页（page + pageSize 参数）
2. 前端分页组件
3. 搜索框支持按任务 ID / Schema 名搜索
4. 状态筛选下拉框（已完成 / 部分完成 / 需复核 / 失败 / 识别中）

### 3.2 审计日志只有只读

**现状**: AuditPage 只展示操作日志，无导出/筛选能力  
**需求**:
1. 按时间范围筛选
2. 按操作类型筛选
3. 按操作人筛选
4. 导出 CSV

### 3.3 回写管理缺少实际操作

**现状**: WritebackPage 展示回写记录，但无手动触发回写的能力  
**需求**:
1. 对已完成的任务手动触发回写
2. 回写失败后重试
3. 回写目标配置

### 3.4 Dashboard 工作台缺少关键信息

**现状**: 工作台只有今日任务数、待复核数、已完成数、Provider 在线数  
**需求**:
1. 最近 7 天识别趋势图
2. 字段识别准确率趋势
3. 常见失败原因 TOP5
4. 快捷操作入口（新建识别、查看待复核）

### 3.5 置信度/状态逻辑对老任务不生效

**现状**: 前端 `calculateDisplayStatus` 对所有任务生效（每次渲染重新计算），但老任务的 `normalizedFields` 数据格式可能不兼容（后端返回的 confidence 字段可能缺失）  
**需求**:
1. 确保所有老任务的 result 数据中包含 confidence 字段
2. 或者在后端统一计算任务状态，不依赖前端计算

---

## 四、P3 — 体验优化（提升专业度）

### 4.1 全局 Loading / Skeleton

**现状**: 页面加载时只有简单的 Spin，无骨架屏  
**需求**: 关键页面（Dashboard、任务列表、任务详情）增加 Skeleton 加载态

### 4.2 错误提示统一化

**现状**: 部分 API 错误静默吞掉，部分用 Message.error  
**需求**: 全局错误边界 + 统一 toast 提示 + 网络断开检测

### 4.3 移动端适配

**现状**: 侧边栏在小屏设备上遮挡内容  
**需求**: 响应式布局，移动端侧边栏可折叠/抽屉式

### 4.4 暗色主题

**现状**: 有切换按钮但主题实现不完整  
**需求**: 完整的暗色主题变量覆盖

### 4.5 国际化

**现状**: 大部分中文，但部分英文硬编码（如 status tag、error messages）  
**需求**: 统一中文，消除英文残留

---

## 五、技术债务

### 5.1 401 竞态问题

**现状**: 多个请求同时收到 401 会触发多次 redirect  
**修复**: 加 401 锁，只 redirect 一次

### 5.2 无 Token Refresh 机制

**现状**: access token 过期后直接踢出登录  
**修复**: 实现 refresh token 或静默续期

### 5.3 API 进程启动方式

**现状**: 手动启动容易忘记加载 .env  
**修复**: 使用 `start-api.sh` 统一启动，或改用 dotenv 库自动加载

### 5.4 前端构建缓存

**现状**: nginx 缓存策略已优化，但 JS hash 文件名变化后旧文件未清理  
**修复**: 构建脚本增加清理旧 dist 的步骤

---

## 六、实施优先级建议

### 第一批（立即，1-2天）
- [ ] P0: 登录持久化（App.tsx 调用 restore）
- [ ] P0: 字段统计不更新排查修复
- [ ] P1: 反馈审核闭环（前端审核按钮 + 后端审核 API + 写入知识库）
- [ ] P2: 置信度/状态逻辑兼容老任务

### 第二批（3-5天）
- [ ] P1: Provider CRUD 完整化
- [ ] P1: Schema 新建/编辑
- [ ] P1: 评测中心样本从识别结果导入
- [ ] P2: 任务列表分页 + 搜索

### 第三批（1-2周）
- [ ] P2: Dashboard 趋势图
- [ ] P2: 审计日志筛选导出
- [ ] P3: Loading/Skeleton
- [ ] P3: 移动端适配
- [ ] 技术债务: Token refresh + 401 竞态

---

## 七、验收标准

每个需求完成后需要通过以下检查：
1. **功能测试**: 正常流程 + 边界情况 + 错误处理
2. **UI 检查**: 空状态 / 加载态 / 错误态 三种状态都有合理展示
3. **审计报告**: 更新 FINAL-AUDIT.md，记录修复项
4. **GitHub 提交**: commit message 包含修复描述
