# 任务：Provider 设置页面重构（严格按需求）

## 需求来源
- 计划文档：docs/superpowers/plans/2026-06-10-provider-settings-ux-fix.md
- 禁词扫描：docs/hard-remove-mock-provider-user-surface.test.ts

## 必须做的事（P0）

### 1. 删除 LIMS Provider
- 删除所有 LIMS 相关代码（类型、配置、UI、测试）
- ProviderArea 从 ["OCR", "LLM", "storage", "LIMS"] 改为 ["OCR", "LLM", "storage"]
- 删除 LIMS REST 类型、初始配置、健康检查、表单区域
- 禁词扫描不能出现 "lims-sandbox", "LIMS REST", "LIMS 写回"

### 2. Storage 改为本地存储
- 删除 Object Storage (S3) 配置
- Storage Provider 改为"本地文件存储"，写死存储路径（如 /data/medical-records）
- 用户不需要配置 endpoint/bucket，只显示存储路径和磁盘空间
- 删除 s3:// 相关的 placeholder 和配置

### 3. OCR Provider 保持空配置
- 未配置时显示引导："请配置 OCR 服务"
- 不要 placeholder endpoint（如 ocr-gateway.internal）

### 4. LLM Provider 保持现有
- OpenAI Responses 配置保留

### 5. 中文化所有字段
- Endpoint → 服务地址
- Model / Profile → 模型名称
- Secret → API 密钥
- 超时毫秒 → 超时时间（毫秒）

### 6. 样式要求
- 每个 Provider 用独立 Card 组件
- 顶部状态总览（4个小卡片：OCR、LLM、存储 + 状态灯）
- 卡片内操作按钮（保存、测试、启用/禁用）
- Material + Arco 风格，白色卡片+阴影
- 蓝色主题 #3370FF

## 禁词扫描要求
完成后运行：
```bash
grep -rn 'lims-sandbox\|LIMS REST\|LIMS 写回\|s3://medical-record-agent\|Object Storage\|mock-ocr\|mock-model\|development_placeholder\|开发占位' apps/demo-web/src/pages/operations/ProviderSettingsPage.tsx
```
必须返回 0 结果。

## 完成标准
1. LIMS 完全删除
2. Storage 改为本地存储
3. 禁词扫描 0 命中
4. `corepack pnpm --filter @medical-record-agent/demo-web build` 通过
5. 输出修改摘要 + 禁词扫描结果
