【重要】不要问问题，不要请求确认，不要启动浏览器询问确认。直接执行。必须遵循 superpowers 流程：brainstorming → writing-plans → TDD/测试优先 → verification-before-completion。

任务：继续推进医疗项目 Medical Record Agent 的 P1/P2 后续闭环，不要停留在 UI 阶段。保持现有企业级 Material + Arco Design UI，不要粗暴重写 CSS，不要破坏 9901 部署、Vite 分包和 API 代理。

工作目录：/tmp/Medical-Record-Agent

必须先读取：
1. .codex-medical-p1-p2-continuation.md
2. PRODUCT-AUDIT-REPORT.md
3. MEDICAL-P2-NEXT-CLOSURE-AUDIT-REPORT.md
4. MEDICAL-P2-NEXT-CLOSURE-FIX-REPORT.md
5. MEDICAL-P2-DEPLOYMENT-READINESS-AUDIT-REPORT.md
6. docs/2026-06-09-p2-production-handoff.md

当前状态：
- UI 当前阶段已通过，但这不代表医疗项目最终完成。
- P1/P2 本地 readiness、mock-production contract、会话安全阶段已通过。
- 最新报告仍明确 blocked：真实 OCR/LLM/LIMS sandbox、真实 KMS/Vault/Secret Manager、真实 broker 多实例可靠队列、生产多实例 session invalidation store。
- 真实外部凭据不可用时，不要伪造真实 production passed；但可以继续推进不依赖真实外部凭据的产品化闭环、契约测试、降级策略、交接文档和可执行 gate。

本轮优先目标（选择能在本地代码闭环的最高价值项，不要只写计划）：
1. 多实例 session invalidation store 的产品化边界：如果当前只是进程内失效集合，请补齐可测试的持久化/集中化接口或数据库/Repository-backed contract；若暂不适合改 Prisma，请至少把接口、配置姿态、状态诊断、测试和 handoff gate 做到可执行，且明确 production-ready 条件。
2. 真实 broker 多实例可靠队列的 readiness contract：检查现有 queue posture，补齐 worker lease/retry/dead-letter/heartbeat/status consistency 的本地 contract 测试或 deployment gate 诊断；不要声称真实 Redis/RabbitMQ/SQS 已通过。
3. 如果上面两项已充分闭环，则推进 KMS/Vault/Secret Manager contract：脱敏状态、配置检查、secret 不泄露测试、handoff 文档和 production smoke blocked code。

必须执行：
- 先写/更新 docs/superpowers/plans/2026-06-09-p2-session-queue-hardening.md，记录 brainstorming、writing-plans、TDD 和 verification。
- 测试优先：先补/改测试，再实现。
- 修复后运行并报告：
  - corepack pnpm typecheck
  - corepack pnpm test
  - corepack pnpm --filter @medical-record-agent/demo-web test:styles
  - corepack pnpm --filter @medical-record-agent/demo-web test:mobile
  - corepack pnpm --filter @medical-record-agent/demo-web build
  - corepack pnpm smoke:demo-web
  - corepack pnpm readiness:deployment（允许因真实外部集成 blocked 返回 exit code 2，但本地 readiness 不应失败）
- 检查 9901 首页和 /api/health 可访问，并确认 dist/index.html 引用真实新 bundle。

报告要求：
1. 生成 MEDICAL-P2-SESSION-QUEUE-HARDENING-FIX-REPORT.md，说明修复点、文件、测试、9901/dist 检查、剩余 blocked 条件。
2. 生成 MEDICAL-P2-SESSION-QUEUE-HARDENING-AUDIT-REPORT.md，必须包含 7 个维度：产品概述、功能完整性、业务流程完整性、用户体验、技术实现、P0/P1/P2 问题清单、验收结论。
3. 验收结论必须分层：UI 当前阶段、P1/P2 本轮阶段、真实外部集成、医疗最终产品。不要把阶段通过写成最终完成。

约束：
- 不要 git commit。
- 不要修改 .env、node_modules、无关缓存。
- 不要删除或整体重写现有 Material + Arco CSS。
- 不要并发启动其他 agent。
- 若遇到 429/上游断流，保留已完成改动和报告草稿，明确中断点，不要反复重试。
