# Phase 11: 用户反馈 5 个问题修复

## 问题 1: Schema 停用做成删除了
**用户原话**: "停用不是删除啊，停用了只是不用，但还是要显示在列表"
- 当前行为：Schema 停用后从列表消失
- 期望行为：停用的 Schema 仍然显示在列表中，但标记为"已停用"状态，不可被新任务选用
- 检查 SchemaPage.tsx 中停用操作的逻辑
- 检查后端 schema service 的 list 方法是否过滤了停用的 schema

## 问题 2: 反馈管理 500 错误
**用户原话**: "反馈管理还是报错了，服务错误"
- API `/api/feedback/all` 返回 `{"error":"INTERNAL_ERROR"}`
- 需要检查 feedbackService.listAll 的实现，找到 500 的根因
- 可能是数据库查询错误或数据结构不匹配

## 问题 3: 评测创建后列表无记录
**用户原话**: "新建了评测，提示了创建成功，但是右侧的评测列表点击之后没有对应的记录"
- 创建评测运行成功后，运行记录列表不显示
- 检查评测 API `/api/evaluation/runs` 是否正确返回数据
- 检查前端 EvaluationPage 的"运行记录" tab 是否正确加载数据

## 问题 4: 任务详情统计对不上
**用户原话**: "总字段是25，为空的是4，不可信的又来啊，识别成功的有22，加起来都不是25了"
- 统计数字加起来不等于总数
- 需要重新设计统计逻辑：
  - 总字段 = 所有提取的字段数
  - 为空 = 值为空/null 的字段数（这些应该算 100% 置信度）
  - 已识别 = 有值的字段数
  - 已识别 = 总字段 - 为空
  - 不可信 = 置信度低于阈值的字段（不包括空值字段）

## 问题 5: 为空字段的置信度应该是 100%
**用户原话**: "为空就代表它识别置信度为百分百，它都没有给你个假值，只是为空。就证明对于后续不影响"
- 空值字段不应该被标记为"不可信"
- 空值字段的置信度应该显示为 100%（或显示 N/A）
- 空值字段应该单独统计，不混入"不可信"分类
- 修改 JobDetailPage.tsx 中的置信度计算和显示逻辑

## 执行顺序
1. 先修后端（反馈 500 + Schema 停用逻辑 + 评测列表）
2. 再修前端（统计逻辑 + 置信度显示 + Schema 列表显示停用项）
3. 构建验证
