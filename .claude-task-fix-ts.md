读取 CLAUDE.md 了解核心规则。

## 任务：修复 TypeScript 编译错误

当前 build 失败，有 9 个 TypeScript 错误需要修复：

### JobDetailPage.tsx 错误
1. `RecognitionDetailState` 缺少 `status` 字段 — 补上
2. `activeKey` 类型不包含 `"stored"` — 扩展类型定义

### NewRecognitionPage.tsx 错误（上一轮遗留）
3. `ocrProviderChoices` 未定义引用 — 删除或定义
4. `ocrProvider` 未定义引用 — 删除或定义

### 验收标准
1. `npx tsc --noEmit` 零错误
2. `npm test` 全部通过
3. `npm run build` 通过

【重要】不要问问题，直接修复。修完运行 tsc、test、build 验证。
