【重要】不要问问题，不要请求确认，不要启动浏览器询问确认。直接执行。必须遵循 superpowers 流程：brainstorming → writing-plans → TDD/测试优先 → verification-before-completion。

工作目录：/tmp/Medical-Record-Agent
角色：你是医疗项目 P1/P2 业务/安全/集成闭环修复 worker。不要提交 git commit，不要修改 .env、node_modules、缓存目录，不要粗暴重写 CSS，必须保持现有企业级 Material + Arco Design UI（Primary #3370FF、背景 #F7F8FA、白色侧栏、active nav pill、DM Sans + Noto Sans SC、移动端抽屉/单列/44px 触摸区）。

必须先读取这些文件并基于现状继续，而不是重做已完成 UI 补丁：
1. .codex-medical-p1-p2-continuation.md
2. PRODUCT-AUDIT-REPORT.md
3. MEDICAL-ARCO-UI-AUDIT-REPORT.md
4. STYLE-FIX-REPORT.md
5. MEDICAL-UI-REFLOW-FIX-REPORT.md
6. P1-AUDIT-REPORT.md
7. P1-FIX-REPORT.md

背景结论：UI 阶段已明显改善，P1/P2 chunk 和 9901 验证已有阶段性报告，但项目尚未最终完成。当前 P1-AUDIT-REPORT.md 明确为“不通过”，打回清单包括 P1-3、P1-6、P1-8；PRODUCT-AUDIT-REPORT.md 还列出写回可信边界、demo API 闭环、静态 fallback、Evaluation schema、production smoke、API 契约、异步任务队列、安全基线、密钥库、浏览器 E2E 等 P1/P2 未完成点。

本轮目标：不要停在 UI 阶段，继续推进 P1/P2 业务、安全、集成闭环。优先修复 P1-AUDIT-REPORT.md 打回项，同时补齐后续业务/安全/集成修复报告和 7 维审计报告。

必须修复/推进的 P1 阻断项：
1. P1-3 Schema 发布危险操作门禁：发布动作必须与停用/回滚同级二次确认。点击“发布”只能打开确认弹窗，确认后才调用 api.publishSchemaDraft()；补测试证明未确认时不会调用发布 API。
2. P1-6 API 契约集中和写回可信边界：
   - 将页面内 API response shape 解析尽量迁移到集中 API mapper/normalizer 层。页面不应直接维护 response: unknown、job: unknown、result: unknown 的后端 shape 兼容逻辑；保留 unknown 仅在 JSON 入口、错误响应、外部 payload 入口。
   - 修复生产写回装配漂移。/writeback 生产路径必须使用服务端持久化 RecognitionResult / payload.writeback.readyFields 构造 LIMS payload，不能依赖 executor 入参 fields/payload；补生产装配测试，断言手动 /writeback 会把服务端 readyFields 写入 LIMS adapter，而不是 payload: {}。
3. P1-8 长任务取消/重跑：buildRecognitionFileUploadInput()、blobToBase64()、blobSha256Hex() 或等价调用链接收 AbortSignal，并在每个异步阶段前后检查 abort、抛出 AbortError；补识别创建取消和重跑测试。

必须继续审计/推进 PRODUCT-AUDIT-REPORT.md 中仍未闭环的 P1/P2：
- 写回可信边界是否真实落地到 production executor 与 route test。
- demo API 真实闭环与静态 demo fallback 风险：至少产出清晰代码整改或产品门禁，不要让生产/验收模式静默展示静态假数据。
- Evaluation runner schemaKey/schemaVersion 解析：如本轮无法完整实现，要在报告中列为明确未完成 P1/P2，并给出代码位置和下一步。
- production smoke：修复可修的测试稳定性；若缺少真实外部环境变量，报告中必须明确“环境未配置，不能视为外部集成验收通过”，不要虚报通过。
- API 契约、安全基线、密钥库、异步任务队列、浏览器 E2E：按 P1/P2 优先级列出已修/未修，能补测试或文档门禁的要补。

验证要求（必须实际运行并写入报告，不要只写计划）：
1. corepack pnpm --filter @medical-record-agent/demo-web test:styles
2. corepack pnpm --filter @medical-record-agent/demo-web test:mobile
3. corepack pnpm --filter @medical-record-agent/demo-web build
4. corepack pnpm test（如失败，必须具体列出失败测试和剩余问题）
5. 尽量验证 http://localhost:9901/ 与 http://localhost:9901/api/health；确认 dist/index.html 引用的新 bundle。若 9901 服务未运行或无法重启，报告必须明确。

报告产出要求：
1. 生成或更新 P1-FIX-REPORT.md：说明本轮修复文件、测试、仍未闭环项。
2. 生成新的 P1-P2-CONTINUATION-AUDIT-REPORT.md：必须包含 7 个维度：
   - 产品概述（定位、目标用户、核心价值）
   - 功能完整性（页面/功能/实现状态）
   - 业务流程完整性（核心流程是否闭环）
   - 用户体验（性能、交互、错误处理、移动端）
   - 技术实现（代码质量、API 完整性、数据模型）
   - 问题清单（P0/P1/P2，必须具体到文件/功能/影响/建议）
   - 验收结论（通过/不通过 + 改进建议）
3. 如修改了 MEDICAL-ARCO-UI-AUDIT-REPORT.md，必须保留 7 维度和 UI 阶段通过的边界，不要写成医疗项目最终完成。

验收口径：
- 不能把“UI 当前阶段通过”写成“医疗项目最终完成”。
- 若 P1-AUDIT 打回项全部修复且测试通过，可写“P1 打回项阶段通过”；若 PRODUCT-AUDIT 中仍有 P2/外部集成未完成，必须继续列出。
- 不要只写报告不改代码；不要只跑 build 不做 7 维审计。
