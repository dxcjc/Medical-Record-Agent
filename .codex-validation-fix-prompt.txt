【重要】不要问问题，不要启动浏览器，不要询问确认。直接开始工作。不要读取大量文件，直接修改。

工作目录：/tmp/Medical-Record-Agent

任务：修复 validationEngine.ts 中硬编码的 clinicalDiagnosis 回退逻辑。

当前文件 packages/core/src/engine/validationEngine.ts 第 32 行：
```
return schema.fields.some((field) => field.key === "clinicalDiagnosis") ? ["clinicalDiagnosis"] : [];
```

这行代码在没有 required/critical 字段时，硬编码回退到 clinicalDiagnosis。需要改为基于 schema 字段定义的通用回退。

修改要求：
1. 将第 32 行改为：如果 schema 中没有任何 required 或 critical 标记的字段，则返回所有 schema.fields 的 key 列表（即把所有字段视为需要验证的字段）
2. 这样任何 schema 都能正确工作，不依赖硬编码字段名

具体修改：
```typescript
// 第 32 行改为：
return schema.fields.map((field) => field.key);
```

同时在 packages/core/test/ 目录下创建 validationEngine.test.ts 测试文件：
```typescript
import { describe, expect, it } from "vitest";
import { getRequiredFieldKeys } from "../src/engine/validationEngine";

describe("getRequiredFieldKeys", () => {
  it("返回标记为 required 的字段", () => {
    const schema = {
      key: "test",
      fields: [
        { key: "diagnosis", label: "诊断", type: "string" as const, required: true },
        { key: "name", label: "姓名", type: "string" as const }
      ]
    };
    expect(getRequiredFieldKeys(schema)).toEqual(["diagnosis"]);
  });

  it("返回标记为 critical 的字段", () => {
    const schema = {
      key: "test",
      fields: [
        { key: "diagnosis", label: "诊断", type: "string" as const, critical: true },
        { key: "name", label: "姓名", type: "string" as const }
      ]
    };
    expect(getRequiredFieldKeys(schema)).toEqual(["diagnosis"]);
  });

  it("无 required/critical 时返回所有字段 key", () => {
    const schema = {
      key: "test",
      fields: [
        { key: "diagnosis", label: "诊断", type: "string" as const },
        { key: "name", label: "姓名", type: "string" as const }
      ]
    };
    expect(getRequiredFieldKeys(schema)).toEqual(["diagnosis", "name"]);
  });

  it("空 schema 返回空数组", () => {
    const schema = { key: "test", fields: [] };
    expect(getRequiredFieldKeys(schema)).toEqual([]);
  });
});
```

修改完成后运行：
1. corepack pnpm vitest run packages/core/test/validationEngine.test.ts
2. corepack pnpm test（全量测试）

生成报告 P2-VALIDATION-FIX-REPORT.md，包含修改说明和测试结果。
