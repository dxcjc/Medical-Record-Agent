【重要】不要问问题，不要请求确认，不要启动浏览器询问确认。直接执行。必须遵循 superpowers 流程：brainstorming → writing-plans → TDD/测试优先 → verification-before-completion。

任务：修复医疗项目剩余 P1 问题并推进业务闭环。

工作目录：/tmp/Medical-Record-Agent

背景：
- 当前 build 通过，9901 部署正常，test:styles 通过。
- 但 `pnpm test` 有 1 个失败：`apps/api/src/demo-services.test.ts:27` 期望 providers 数组只有 2 项（local-storage + lims-writeback），但实际有 4 项（还包含 paddle-ocr 和 gpt5-llm）。这是 commit 9a4c8c4 新增了 paddle-ocr 和 gpt5-llm provider 但未更新测试导致的。
- 最新审计 MEDICAL-AUDIT-2026-06-11.md 列出了未修复的 P1：
  - P1-2: Demo API 不执行 OCR/LLM 编排（demo-services.ts 创建 queued 任务但不执行）
  - P1-3: 静态 demo 数据兜底（JobDetailPage 和 WritebackPage API 失败时用假数据）
  - P1-5: 生产 smoke 未配置
  - P1-6: API 路由契约使用 unknown

第一优先：修复测试
1. 更新 `apps/api/src/demo-services.test.ts` 第一个测试用例，将 providers 期望从 2 项扩展为 4 项（local-storage, lims-writeback, paddle-ocr, gpt5-llm），使用 expect.objectContaining 匹配。
2. 确认 `corepack pnpm test` 全部通过（455 passed, 0 failed）。

第二优先：修复 P1-3 静态 demo 兜底
1. 在 `apps/demo-web/src/pages/recognition/JobDetailPage.tsx` 中，当 API 失败时：
   - 生产模式（非 VITE_DEMO_MODE）：显示错误状态 + 重试按钮，不用静态 demo 数据
   - Demo 模式（VITE_DEMO_MODE=true）：展示静态样例但加标注"演示数据"
2. 在 `apps/demo-web/src/pages/operations/WritebackPage.tsx` 中同样处理。
3. 确保 build 和测试仍然通过。

第三优先：P1-6 API 契约收紧（部分）
1. 为 `apps/api/src/routes/writeback.routes.ts` 和 `apps/api/src/routes/jobs.routes.ts` 的 service 方法添加具体输入类型，替换 `unknown`。
2. 使用 Zod schema 定义请求体，在路由层做 validation。
3. 不需要一次性修完所有路由，至少修写回和识别这两个核心路由。

验证（必须全部执行）：
1. `corepack pnpm test` — 全部通过
2. `corepack pnpm --filter @medical-record-agent/demo-web test:styles` — 通过
3. `corepack pnpm --filter @medical-record-agent/demo-web test:mobile` — 通过
4. `corepack pnpm --filter @medical-record-agent/demo-web build` — 通过
5. `corepack pnpm typecheck` — 通过（如有失败，在报告中说明具体失败原因）

生成报告：
1. `MEDICAL-P1-FIX-ROUND2-REPORT.md`：包含 7 个维度（产品概述、功能完整性、业务流程完整性、用户体验、技术实现、问题清单、验收结论）
2. 每个修复点：文件、改动说明、验证结果

约束：
- 不要修改 .env、node_modules
- 不要并发启动其他 agent
- 不要只写计划，必须实际修复、验证、生成报告
- 保持 Material + Arco 设计系统，不粗暴重写 CSS
- 若遇到 429/断流，保留已完成改动和报告，明确记录中断点
