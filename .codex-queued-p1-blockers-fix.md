【重要】不要问问题，不要请求确认，不要启动浏览器询问确认。直接执行。必须遵循 superpowers 流程：brainstorming → writing-plans → TDD/测试优先 → verification-before-completion。

任务：修复医疗项目 P1 最新审计报告中的阻断问题，并重新生成 7 维度产品级审计报告。

工作目录：/tmp/Medical-Record-Agent
背景：最新 /tmp/Medical-Record-Agent/P1-AUDIT-REPORT.md 结论为不通过，且报告不是完整产品级 7 维度格式。必须以工作区实际代码为准修复后重新审计。

【必须修复的问题】
[P1-3] Schema 发布动作缺少二次确认门禁
- 点击“发布”只能打开确认弹窗，确认后才允许调用 api.publishSchemaDraft()。
- 与停用/回滚保持同级危险操作确认体验。
- 补测试证明未确认时不会调用发布 API。

[P1-6] 页面本地 API shape 解析未收敛，生产写回装配漂移
- 将页面内 response/job/result: unknown 的后端 shape 兼容逻辑迁移到集中 API mapper/normalizer 层；页面不直接维护后端响应形状。
- 修复生产 writeback executor：手动 /writeback 必须使用服务端持久化 RecognitionResult / readyFields 构造 payload，不依赖 executor 入参 fields/payload。
- 补生产装配测试，断言 LIMS adapter 收到服务端 readyFields，而不是 payload: {}。

[P1-8] 识别创建本地文件读取/哈希不可取消
- buildRecognitionFileUploadInput、blobToBase64、blobSha256Hex 或等价链路接收 AbortSignal。
- 每个异步阶段前后检查 abort 并抛 AbortError。
- 补识别创建取消和重跑测试。

【修复后必须做】
1. 运行并记录：pnpm typecheck、pnpm test、cd apps/demo-web && pnpm build。
2. 生成 /tmp/Medical-Record-Agent/P1-FIX-REPORT.md，说明修复文件、测试、剩余风险。
3. 重新生成 /tmp/Medical-Record-Agent/P1-AUDIT-REPORT.md，必须包含 7 个维度：产品概述、功能完整性、业务流程完整性、用户体验、技术实现、P0/P1/P2 问题清单、验收结论。
4. 验收结论必须明确：通过/不通过；问题清单必须具体。
5. 不要提交 git commit，不要改 .env/node_modules/缓存。
