# 任务：医疗项目接入真实 OCR 服务

## 目标
为医疗记录识别系统接入真实的 OCR 服务，替换当前的 Mock OCR Provider。

## 当前状态
- 代码支持 HTTP OCR Provider（`httpOcrProvider`）
- 环境变量配置：`OCR_PROVIDER=http`、`OCR_ENDPOINT=http://...`
- 当前使用 Mock OCR Provider

## 任务步骤

### 1. 安装 PaddleOCR HTTP 服务
```bash
pip install paddlepaddle paddleocr
```

### 2. 创建 PaddleOCR HTTP 服务
创建一个简单的 FastAPI 服务，提供 `/recognize` 端点：
- 接收文件（图片或 PDF）
- 返回 OCR 识别结果

### 3. 配置环境变量
在 `/tmp/Medical-Record-Agent/.env` 中添加：
```
OCR_PROVIDER=http
OCR_ENDPOINT=http://localhost:8080/recognize
```

### 4. 测试 OCR 服务
- 启动 PaddleOCR 服务
- 上传测试图片
- 验证识别结果

## 验收标准
1. PaddleOCR HTTP 服务运行正常
2. 能识别中文医疗文档
3. 医疗项目配置正确使用 HTTP OCR Provider
4. 能通过 API 调用 OCR 服务

## 项目路径
/tmp/Medical-Record-Agent
