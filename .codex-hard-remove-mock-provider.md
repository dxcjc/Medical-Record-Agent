【必须遵循 superpowers 流程】
读取当前 git diff、`MEDICAL-REMOVE-MOCK-LINE-AUDIT-REPORT.md`、`MEDICAL-REMOVE-MOCK-LINE-FIX-REPORT.md`、`docs/superpowers/plans/2026-06-09-remove-mock-line.md`。你必须按 brainstorming → writing-plans → TDD/测试优先 → verification-before-completion 执行。

任务：Medical Record Agent 彻底删除用户/业务主线中的 mock provider 内容，改为等待真实模型提供商接入。

用户纠正：上一轮把 mock 标成“开发占位 provider”还不对。用户明确要求：
“不是占位，是删掉 mock 相关的内容，后面我提供真实的模型提供商。”

因此目标不是把 mock 改名成开发占位，而是：
- 产品页面、API 业务返回、Provider 设置、新建识别、README 操作主线中不要再出现 mock provider / 开发占位 provider；
- 没有真实 OCR/LLM Provider 时，就显示“未配置真实 Provider / 等待接入真实模型提供商”，创建识别阻断；
- 后续由用户提供真实模型提供商后再接入。

重要边界：
1. 单元测试里的 `vi.mock`、`fetchMock`、测试 fixture 可以保留，不能粗暴删除测试技术 mock。
2. 内部 provider 实现函数如果测试依赖很深，可以保留在测试/fixture 层，但不得通过业务 API `/providers` 作为用户可见 provider 返回；不得显示在前端下拉；不得写入用户文档主线。
3. 不要提交 git commit。
4. 不要问问题，直接修。

必须修复：

[P0-1] API 业务 provider 列表不要返回 mock/开发占位 provider。
- 检查 `apps/api/src/demo-services.ts`、`apps/api/src/bootstrap/production-services.ts`、providers routes/service。
- `/providers` 在没有真实 OCR/LLM 时应返回空列表或真实 provider 未配置状态，而不是 mock-ocr/mock-model 或 development_placeholder。
- health/default 逻辑对不存在真实 provider 要返回合理错误/未配置，不要提供 mock fallback。
- 不允许把 mock provider 作为 fallback default key 暴露给前端。

[P0-2] 前端用户可见文案删除 mock/开发占位 provider。
- `AppShell.tsx` 顶部状态应为“Provider 待配置 / Provider 已连接”，不得出现 mock、开发占位。
- `NewRecognitionPage.tsx` 没有真实 provider 时：下拉为空、按钮禁用、提示“请先配置真实 OCR/LLM Provider；等待接入真实模型提供商”。不得提 mock/开发占位。
- `ProviderSettingsPage.tsx` 不显示 Mock 类型、不显示开发占位 provider、不显示 mock endpoint/secret/health。
- `DatasetSpecPage.tsx`、README 等用户可见文档不得出现 mock provider 或开发占位 provider 作为说明。测试章节可说“自动化测试使用合成样本和测试替身”，不要写 mock provider 名。

[P0-3] 测试更新为真实“未配置”语义。
- 保留测试 mock 技术实现，但测试期望不应要求 API 返回 mock provider 给前端。
- 增加/更新测试：无真实 provider 时 `/providers` 不返回 mock；新建识别阻断；页面/文档不含 mock/development placeholder 文案。

[P1-1] 报告必须说明纠偏：上一轮“开发占位”不符合用户要求，本轮改为完全从用户/业务主线删除。

必须执行验证：
- `corepack pnpm --filter @medical-record-agent/demo-web test:styles`
- `corepack pnpm --filter @medical-record-agent/demo-web test:mobile`
- `corepack pnpm --filter @medical-record-agent/demo-web build`
- 相关前端/API 测试；可行则全量 `corepack pnpm test`
- `corepack pnpm smoke:demo-web` 可行则执行
- 检查 `http://127.0.0.1:9901/` 和 `/api/health`
- 检查构建产物/用户可见源码不包含以下用户主线词：`Mock Provider Ready`、`Mock OCR Provider`、`Mock Model Provider`、`development_placeholder`、`开发占位 provider`、`mock-ocr`、`mock-model`（测试文件内可保留，但报告要说明只限测试）

必须产出：
1. `MEDICAL-HARD-REMOVE-MOCK-PROVIDER-FIX-REPORT.md`
2. `MEDICAL-HARD-REMOVE-MOCK-PROVIDER-AUDIT-REPORT.md`

审计报告必须包含 7 维度：产品概述、功能完整性、业务流程完整性、用户体验、技术实现、问题清单 P0/P1/P2、验收结论。

结论口径：
- 如果已从用户/业务主线彻底删除 mock/development placeholder provider，写“彻底删除 mock provider 用户/业务主线阶段通过”。
- 明确说明：真实模型提供商等待用户后续提供并接入；当前没有真实 provider 时系统阻断识别创建而不是展示 mock/占位。
