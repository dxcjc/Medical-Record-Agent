【重要】不要问问题，不要请求确认，不要启动浏览器询问确认。直接执行。必须遵循 superpowers 流程：brainstorming → writing-plans → TDD/测试优先 → verification-before-completion。

工作目录：/tmp/Medical-Record-Agent

角色：你是医疗项目 P1/P2 产品化闭环实现 agent。不要把 UI 当前阶段通过、本地 mock-runtime smoke 或 contract 测试误判为医疗最终产品完成。

必须先读取：
1. .codex-medical-p1-p2-continuation.md
2. PRODUCT-AUDIT-REPORT.md
3. MEDICAL-P2-SESSION-QUEUE-HARDENING-AUDIT-REPORT.md
4. MEDICAL-P2-SESSION-QUEUE-HARDENING-FIX-REPORT.md
5. MEDICAL-P2-PRODUCTION-CLOSURE-AUDIT-REPORT.md
6. docs/2026-06-09-p2-production-handoff.md

当前已知状态：
- UI 当前阶段通过，保持 Material + Arco Design，不要粗暴重写 CSS。
- 本地 typecheck/test/demo-web styles/mobile/build 已通过。
- 9901 首页和 /api/health 可访问，当前 dist 与 9901 首页一致。
- vendor-arco 当前约 415.91 kB，旧报告里的 517k remaining 不要再当作当前阻塞。
- 仍不能写最终产品完成：真实 OCR/LLM/LIMS sandbox、真实 KMS/Vault/Secret Manager、生产多实例 session invalidation store、真实 broker 多实例可靠队列仍 blocked。

本轮目标：继续推进“不依赖真实外部凭据也能本地闭环”的最高价值 P1/P2 项。优先从以下方向选择可实际落地的一个或多个，不要只写计划：
1. 生产多实例 session invalidation store 的下一步本地可执行闭环：如果已有 repository contract，补齐更具体的 database/redis adapter skeleton、readiness gate、状态诊断、测试或交接验收脚本；不要声称真实多实例已通过。
2. 真实 broker 多实例可靠队列的下一步本地可执行闭环：补齐 Redis/RabbitMQ/SQS adapter contract、worker lease/retry/dead-letter/heartbeat/status-result consistency 的测试/诊断/readiness gate；不要声称真实 broker 已通过。
3. KMS/Vault/Secret Manager 的下一步本地可执行闭环：补齐 resolver 配置校验、脱敏错误、SDK client 注入契约、secret 不泄露测试、production smoke blocked/diagnostic；不要声称真实密钥库已接入。
4. 如果上述都已充分闭环，则推进 production smoke/readiness handoff：让 blocked 状态更可执行、更分层、更利于部署方接真实 sandbox。

必须完成：
- 先写/更新 docs/superpowers/plans/ 下的本轮计划文件。
- 采用 TDD/测试优先，先补测试再实现。
- 保持 UI 设计系统，不做无关大改。
- 不修改 .env、node_modules、缓存，不 git commit。
- 运行并确保通过或如实记录：
  - corepack pnpm typecheck
  - corepack pnpm test
  - corepack pnpm --filter @medical-record-agent/demo-web test:styles
  - corepack pnpm --filter @medical-record-agent/demo-web test:mobile
  - corepack pnpm --filter @medical-record-agent/demo-web build
  - corepack pnpm readiness:deployment（允许 exit code 2，但必须 localReadiness=passed、externalIntegration=blocked、finalProduct=blocked）
- 检查 9901 首页和 /api/health 可访问，并确认 dist/index.html 与 9901 HTML 引用的是当前 bundle。

必须生成报告：
1. MEDICAL-P2-NEXT-LOCAL-CLOSURE-FIX-REPORT.md：写清修复范围、测试、9901/dist、剩余 blocked。
2. MEDICAL-P2-NEXT-LOCAL-CLOSURE-AUDIT-REPORT.md：必须包含 7 个维度：产品概述、功能完整性、业务流程完整性、用户体验、技术实现、P0/P1/P2 问题清单、验收结论。

验收结论必须分层：
- UI 当前阶段：通过/不通过。
- 本轮 P1/P2 本地产品化闭环：通过/不通过。
- 真实外部集成：blocked 或通过。
- 医疗最终产品：只能在真实 OCR/LLM/LIMS sandbox、真实 KMS/Vault/Secret Manager、生产多实例 session store、真实 broker 多实例可靠队列全部通过后才写通过；否则写不通过/blocked。

若遇到 429、上游断流或环境限制：保留已完成改动和报告草稿，明确中断点，不要反复重试。