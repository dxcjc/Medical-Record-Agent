【重要】不要问问题，不要请求确认，不要启动浏览器。直接执行。
必须遵循 superpowers 流程：brainstorming → writing-plans → TDD → verification-before-completion。

任务：修复医疗项目中 6 个失败的测试，使 corepack pnpm test 全部通过。

工作目录：/tmp/Medical-Record-Agent

背景：
- corepack pnpm test 当前有 6 个测试失败，分布在 3 个文件中
- 构建（corepack pnpm --filter @medical-record-agent/demo-web build）已通过
- 这些测试失败来自最近一次 commit（feat: Provider 配置优化），不是未提交变更引起的

失败的测试文件和测试：

1. apps/demo-web/src/ui-arco-style-guards.test.ts (2 failures)
   - "Open Design 参考落到专业医疗工作台 hero、状态摘要和操作面板样式"
   - "所有点名页面都有统一 app-page/业务页面容器，并使用 Arco/中台组件"

2. apps/demo-web/src/pages/operations/ProviderSettingsPage.test.ts (3 failures)
   - "只恢复结构合法的本地 Provider 草稿，避免坏数据污染配置页"
   - "普通用户可选 Provider 类型不包含 Mock，并保留真实生产类型"
   - "Provider 保存和健康检查状态提供取消、重试和错误恢复提示"

3. apps/api/src/demo-services.test.ts (1 failure)
   - "demo provider 列表不再向业务 API 返回内部 mock provider"

要求：
1. 先读取每个失败的测试文件和对应的实现文件，理解测试期望什么
2. 修复实现代码使测试通过（不是修改测试来匹配代码）
3. 保持 Material + Arco Design 设计系统不变
4. 不要修改 .env、node_modules
5. 不要提交 git commit

验证：
- corepack pnpm test 必须全部通过（除了已知 skipped 的测试）
- corepack pnpm --filter @medical-record-agent/demo-web build 必须通过
- 9901 服务不需要重启

完成后生成修复报告到 .codex-test-fix-report.md
