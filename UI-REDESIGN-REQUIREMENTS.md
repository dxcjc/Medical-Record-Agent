# 医疗 OCR 识别系统 — UI/UX 全面重构需求

## 背景

当前系统页面过多（12+页）、操作路径不清晰、术语对非技术用户不友好。核心目标用户是**医院/检验科行政人员或LIMS操作员**，不是开发者。

## 设计原则

1. **三步完成核心任务**：上传 → 识别 → 查看/导出结果，不超过3次点击
2. **渐进式披露**：高级功能（Schema编辑、评测、Agent追踪）藏在二级入口
3. **引导优先**：首次使用有步骤引导，每个页面有空状态引导
4. **容错设计**：操作可撤销、删除需确认、错误可重试

---

## 页面结构（重构后）

### 一级导航（侧边栏只保留4个）

| # | 页面 | 定位 |
|---|------|------|
| 1 | **工作台** (Dashboard) | 首页，今日概览+快捷操作+最近任务 |
| 2 | **识别任务** (Jobs) | 核心功能：新建+任务列表+结果查看 |
| 3 | **数据管理** (Data) | 合并：Schema管理+已确认数据+导出 |
| 4 | **系统设置** (Settings) | 合并：Provider配置+评测+审计日志+Agent追踪 |

### 二级页面（从一级页面进入）

- 识别任务 → 新建识别（引导式向导）
- 识别任务 → 任务详情（结果查看+编辑+确认）
- 数据管理 → Schema编辑器
- 数据管理 → 样本库
- 系统设置 → Provider管理
- 系统设置 → 评测中心
- 系统设置 → 操作日志

---

## 各页面详细需求

### 1. 工作台 (Dashboard)

**目标**：打开系统就知道该做什么

**内容**：
- **快捷操作区**：大按钮「开始识别」「查看最近结果」「导出数据」
- **今日统计卡片**：今日识别数、成功率、平均处理时间
- **最近任务列表**：最近5条任务，状态一目了然（待处理/识别中/已完成/失败）
- **首次使用引导**：如果没有任何任务，显示3步引导（上传文件→等待识别→查看结果）

**空状态**：
- 无任务时：显示引导卡片 + 大按钮「开始第一次识别」

### 2. 识别任务 — 任务列表

**目标**：查看所有识别任务的状态和结果

**功能**：
- 列表展示：任务名、文件名、状态、创建时间、操作
- 状态筛选：全部/进行中/已完成/需审核/失败
- 搜索：按文件名/任务名搜索
- 批量操作：批量删除、批量导出
- 点击行进入任务详情

### 3. 识别任务 — 新建识别（引导式向导）

**目标**：用户不需要理解Schema、Provider等概念，3步完成

**步骤**：
1. **上传文件**：拖拽或点击上传，支持图片/PDF，预览缩略图
2. **选择识别类型**：下拉选择（如"血常规报告"、"生化报告"），自动关联Schema
3. **确认并开始**：显示摘要（文件名+识别类型+预计时间），点击开始

**高级选项**（折叠，默认隐藏）：
- 自定义Provider
- 隐私设置（脱敏）
- 自定义Schema

**引导**：
- 每个步骤有说明文字
- 第一次使用有动画指引

### 4. 识别任务 — 任务详情

**目标**：查看识别结果、编辑、确认、导出

**布局**：
- **顶部**：任务状态、文件信息、操作按钮（重试/删除/导出）
- **左侧**：原始文档预览（图片/PDF）
- **右侧**：结构化提取结果（表单形式，可编辑）
- **底部**：操作栏 — 「确认并保存」「编辑后保存」「重新识别」

**功能**：
- 字段值可直接点击编辑
- 每个字段显示置信度（高/中/低 颜色标识）
- 低置信度字段高亮提示
- 支持"确认全部"一键操作
- 导出：CSV/JSON/复制到剪贴板

### 5. 数据管理 — Schema管理

**目标**：管理识别模板（高级功能，普通用户不常接触）

**简化方案**：
- 列表展示所有Schema，带使用次数统计
- 新建Schema用表单引导（不是JSON编辑器）
- 字段配置用可视化表单（添加字段→选类型→设枚举值）
- 预置模板：血常规、生化、尿常规等常用模板

### 6. 数据管理 — 已确认数据

**目标**：查看所有已确认的识别结果，支持搜索和导出

**功能**：
- 表格展示：按Schema分组
- 搜索/筛选：按字段值、时间范围
- 导出：CSV、Excel
- 数据详情：点击查看完整字段

### 7. 系统设置 — Provider管理

**目标**：配置OCR和LLM服务

**简化方案**：
- 卡片式展示每个Provider的状态（在线/离线/错误）
- 测试连接按钮
- 编辑配置用表单（不用手动编辑.env）

### 8. 系统设置 — 评测中心（二级入口）

**目标**：评估识别准确率（高级功能）

**简化方案**：
- 合并"评测样本"和"评测运行"到一个页面
- 样本导入向导
- 一键运行评测
- 结果用图表展示

---

## 全局设计规范

### UI风格
- Material + Arco Design
- 白侧栏 + pill高亮 + 蓝 #3370FF + 灰 #F7F8FA
- 字体：DM Sans + Noto Sans SC
- 卡片式布局，圆角 8px，阴影 0 2px 8px rgba(0,0,0,0.08)

### 交互规范
- 所有列表支持搜索和筛选
- 所有表单有实时校验
- 所有删除操作需二次确认
- 所有异步操作有loading状态
- 错误信息用中文，包含可操作建议
- 空状态有引导，不是空白

### 响应式
- 主要面向桌面端（1280px+）
- 移动端（768px以下）：侧边栏折叠为底部tab

---

## 验收标准

1. 核心流程（上传→识别→查看结果）≤3次点击完成
2. 所有页面有空状态引导
3. 首次使用有步骤引导
4. 所有操作有中文提示
5. `npm run build` 成功，无 TS 错误
6. 所有页面能正常加载和交互
7. 侧边栏只有4个一级入口
8. 高级功能不干扰核心流程

---

## 技术约束

- 前端：React + TypeScript + Arco Design（不引入新框架）
- 后端API：不改动，前端适配现有接口
- 端口：9901
- 构建：`npm run build` in `apps/demo-web`
