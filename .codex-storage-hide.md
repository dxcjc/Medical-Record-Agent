# 补充：Storage Provider 完全隐藏

在上次任务基础上，额外要求：

## Storage Provider 不要显示在页面上
- 删除 Storage 相关的所有 UI（表单区域、状态卡片、配置区）
- 删除 ProviderArea 中的 "storage" 选项
- 删除 Object Storage / 本地存储 相关代码
- 页面只显示 2 个 Provider：OCR 和 LLM
- 顶部状态总览也只显示 OCR 和 LLM 两个卡片

## 需要删除的内容
- `ProviderArea` 类型删除 "storage"
- `providerKindByArea` 删除 storage 映射
- `providerAreaIcons` 删除 storage
- `providerKindOptions` 删除 storage
- `areaHelpText` 删除 storage
- `initialProviderConfigs` 删除 storage 配置
- `initialHealthStatus` 删除 storage 状态
- UI 中删除 storage 的 Card/Form 区域
- `isStorage` 判断函数可以保留但不使用

## 禁词扫描额外添加
```
grep -rn 's3://\|Object Storage\|bucket\|local-storage\|存储桶' apps/demo-web/src/pages/operations/ProviderSettingsPage.tsx
```
必须返回 0 结果。
