【重要】不要问问题，不要请求确认，不要启动浏览器询问确认。直接执行。必须遵循 superpowers 流程：brainstorming → writing-plans → TDD/测试优先 → verification-before-completion。

任务：继续推进 /tmp/Medical-Record-Agent 医疗项目 P1/P2 剩余业务/安全/集成闭环，不要停在 UI 阶段。保持企业级 Material + Arco Design UI，不要粗暴重写 CSS，不要破坏现有 9901 部署和 API 代理。

必须先读取并综合：
1. .codex-medical-p1-p2-continuation.md
2. PRODUCT-AUDIT-REPORT.md
3. MEDICAL-P2-NEXT-LOCAL-CLOSURE-AUDIT-REPORT.md
4. MEDICAL-P2-NEXT-LOCAL-CLOSURE-FIX-REPORT.md
5. docs/2026-06-09-p2-production-handoff.md
6. 已有 MEDICAL-P1-P2-*、MEDICAL-P2-* 审计/修复报告

当前调度判断：
- UI 当前阶段已通过，但用户仍反馈间距、遮挡、换行等细节后续可微调；本轮不要把 UI 阶段通过等同医疗项目最终完成。
- 本地 P1/P2 产品化闭环已有进展：typecheck/test/demo-web build/style/mobile/9901 检查此前通过。
- 最新可见闭环到 MEDICAL-P2-NEXT-LOCAL-CLOSURE-*，真实外部集成仍 blocked：真实 OCR/LLM/LIMS sandbox、真实 KMS/Vault/Secret Manager、生产多实例 session store、真实 broker 多实例可靠队列。
- 尚未看到 MEDICAL-P2-EXTERNAL-BLOCKER-READINESS-FIX-REPORT.md 和 MEDICAL-P2-EXTERNAL-BLOCKER-READINESS-AUDIT-REPORT.md，本轮优先完成该外部 blocker readiness/交接闭环。

本轮目标：在不伪造外部依赖通过的前提下，继续推进“外部 blocker readiness/交接闭环”。优先选择当前仓库内可落地、可测试、能减少交接风险的事项：
1. 将真实 OCR/LLM/LIMS sandbox、KMS/Vault、session store、queue broker 的 blocked 条件固化为清晰 readiness gate/diagnostics/test，避免部署方误判。
2. 补强 production smoke/readiness 输出，使 localReadiness、externalIntegration、finalProduct 分层更清楚，并列出缺失 env/config/endpoint/credential/smoke 步骤。
3. 如已有脚本覆盖不足，补充测试优先的 contract/readiness 测试；不要接入假的外部服务，不要把 skeleton 标记 production ready。
4. 更新交接文档，明确下一位工程师拿到真实凭据后如何执行真实 smoke、预期通过标准、失败排查方向。
5. 若 PRODUCT-AUDIT-REPORT.md 中还有可本地修复的 P1/P2（例如 API 契约、Evaluation schema、static fallback、写回可信边界）未被后续报告覆盖，优先修复其中最高价值且可验证的一项，并记录选择理由。

必须完成验证：
- corepack pnpm typecheck
- corepack pnpm test
- corepack pnpm --filter @medical-record-agent/demo-web test:styles
- corepack pnpm --filter @medical-record-agent/demo-web test:mobile
- corepack pnpm --filter @medical-record-agent/demo-web build
- 9901 首页和 /api/health 可访问；dist/index.html 与 9901 served HTML 引用同一真实 bundle
- 如新增/修改 readiness 脚本，运行对应 readiness 命令；如果因真实外部依赖缺失返回 blocked/exit code 2，必须说明这是预期 blocked，不是产品最终通过。

必须生成报告：
1. MEDICAL-P2-EXTERNAL-BLOCKER-READINESS-FIX-REPORT.md：说明修复点、文件、测试、9901/dist 检查、剩余 blocked。
2. MEDICAL-P2-EXTERNAL-BLOCKER-READINESS-AUDIT-REPORT.md：必须包含 7 个维度：产品概述、功能完整性、业务流程完整性、用户体验、技术实现、P0/P1/P2 问题清单、验收结论。

验收口径：
- 可以写“UI 当前阶段通过”。
- 可以写“本轮本地 readiness/交接闭环通过”。
- 不允许写“医疗最终产品完成/通过”，除非真实 OCR/LLM/LIMS sandbox、真实 KMS/Vault/Secret Manager、生产多实例 session store、真实 broker 多实例可靠队列全部真实验证通过。

约束：
- 不要提交 git commit。
- 不要修改 .env、node_modules、无关缓存。
- 不要只写计划，必须实际修复、验证、生成报告。
- 不要大规模重写 CSS；若必须碰 UI 样式，保持 Material + Arco Design 系统。
- 若遇到 429/上游断流，保留已完成改动和报告草稿，明确中断点，不要反复重试。