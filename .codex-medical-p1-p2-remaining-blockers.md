【重要】不要问问题，不要请求确认，不要启动浏览器询问确认。直接执行。必须遵循 superpowers 流程：brainstorming → writing-plans → TDD/测试优先 → verification-before-completion。

任务：继续推进医疗项目 P1/P2 剩余业务/安全/集成闭环，不要停在 UI 阶段；保持现有企业级 Material + Arco Design UI，不要粗暴重写 CSS，不要破坏 9901 部署和 API 代理。

工作目录：/tmp/Medical-Record-Agent

必须先读取：
1. .codex-medical-p1-p2-continuation.md
2. PRODUCT-AUDIT-REPORT.md
3. MEDICAL-P1-P2-CRON-BUILD-AUDIT-REPORT.md
4. MEDICAL-P2-SECURITY-E2E-AUDIT-REPORT.md
5. MEDICAL-P2-INTEGRATION-HARDENING-AUDIT-REPORT.md
6. MEDICAL-P1-P2-CONTINUATION-ROLLUP-AUDIT-REPORT.md
7. STYLE-FIX-REPORT.md、MEDICAL-ARCO-UI-AUDIT-REPORT.md、MEDICAL-UI-REFLOW-FIX-REPORT.md、P1-AUDIT-REPORT.md、P1-FIX-REPORT.md

背景现状：
- UI 当前阶段明显改善，Material + Arco 设计系统必须保留。
- 本调度轮已复验：demo-web test:styles、test:mobile、build、全量 corepack pnpm test 均通过；9901 首页和 /api/health 可访问，dist bundle 与 9901 HTML 一致。
- 不能把 UI 阶段通过误判为医疗项目最终完成。
- 最新报告仍显示 remaining/blocked：真实 production smoke 外部 OCR/LLM/LIMS sandbox、真实 KMS/Vault/Secret Manager、真实 Redis/RabbitMQ/SQS broker 多实例队列、生产多实例 session invalidation store、外部慢 provider 下队列积压/重试/失败可视化、punycode deprecation warning。

本轮目标：在没有真实外部凭据的前提下，继续推进“可本地落地”的业务/安全/集成优化，不能伪造外部通过。优先处理可用代码/测试/文档闭环：
1. 复扫 remaining/blocked 中哪些是可本地实现的 readiness gate、contract test、adapter interface、配置校验、文档交接或 smoke harness 改进。
2. 对可本地落地项进行 TDD 修复；例如但不限于：
   - production smoke 的配置/错误提示/可跳过外部项/报告输出更清晰；
   - KMS/Vault/Secret Manager 的 secret resolver 接口、配置门禁、健康检查契约或安全测试；
   - session invalidation store / broker readiness 的多实例配置门禁、contract test、handoff doc；
   - 队列 broker 的 lease/retry/dead-letter/heartbeat/status-result consistency 的本地 contract/readiness 测试；
   - punycode deprecation warning 的来源定位与可安全替换/隔离（若来自外部依赖不可修，报告中准确说明）。
3. 对不可本地证明的外部 OCR/LLM/LIMS/KMS/Vault/Broker smoke，必须明确标注 blocked，需要真实 sandbox/secret，不得写“通过”。
4. 继续硬约束：用户/业务主线不得出现 mock/development placeholder provider；测试替身只能留在测试边界。
5. 不要改 UI CSS；如必须触碰 demo-web，保持现有 Material + Arco 设计系统与已有 style/mobile guard。

必须验证：
- corepack pnpm --filter @medical-record-agent/demo-web test:styles
- corepack pnpm --filter @medical-record-agent/demo-web test:mobile
- corepack pnpm --filter @medical-record-agent/demo-web build
- corepack pnpm test
- 检查 http://localhost:9901/ 和 http://localhost:9901/api/health（如 9901 服务未运行，不要自行长期占端口；在报告中说明）
- 如涉及 smoke/readiness 脚本，运行相应测试或脚本 dry-run，并记录结果。

必须生成/更新报告：
1. MEDICAL-P1-P2-REMAINING-BLOCKERS-FIX-REPORT.md：说明本轮修复点、文件、测试、未能本地闭环的外部阻塞。
2. MEDICAL-P1-P2-REMAINING-BLOCKERS-AUDIT-REPORT.md：必须包含 7 维度：产品概述、功能完整性、业务流程完整性、用户体验、技术实现、P0/P1/P2 问题清单、验收结论。

验收口径：
- 可以说“本地可闭环 P1/P2 子项通过”；
- 不能说“医疗项目最终完成”；
- 真实 OCR/LLM/LIMS sandbox、真实 KMS/Vault/Secret Manager、真实 broker 多实例 smoke 未跑通前，最终产品仍 blocked。

约束：
- 不要提交 git commit。
- 不要修改 .env、node_modules、无关缓存。
- 不要并发启动其他 agent。
- 不要只写计划，必须实际修复、验证、生成报告。
- 若遇到 429/上游断流，保留已完成改动和报告草稿，明确记录中断点，不要反复重试。
