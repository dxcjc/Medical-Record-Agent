【重要】不要问问题，不要请求确认，不要启动浏览器询问确认。直接执行。必须遵循 superpowers 流程：brainstorming → writing-plans → TDD/测试优先 → verification-before-completion。

任务：继续推进医疗项目 P1/P2 剩余业务/安全/集成闭环，但不要把 UI 阶段通过误判成医疗项目最终完成。

工作目录：/tmp/Medical-Record-Agent

必须先读取：
1. .codex-medical-p1-p2-continuation.md
2. PRODUCT-AUDIT-REPORT.md
3. MEDICAL-P1-P2-NEXT-BUSINESS-SECURITY-AUDIT-REPORT.md
4. MEDICAL-P1-P2-NEXT-BUSINESS-SECURITY-FIX-REPORT.md
5. MEDICAL-P2-NEXT-LOCAL-CLOSURE-AUDIT-REPORT.md
6. MEDICAL-P2-EXTERNAL-BLOCKER-READINESS-AUDIT-REPORT.md
7. MEDICAL-P2-ASYNC-HANDOFF-AUDIT-REPORT.md
8. docs/2026-06-09-p2-production-handoff.md

当前状态：UI/本地 P1/P2 多项已通过；9901 首页和 /api/health 可访问；demo-web style/mobile/build 当前可通过；但真实 OCR/LLM/LIMS sandbox、真实 KMS/Vault/Secret Manager、生产多实例 session store、真实 broker 多实例可靠队列、真实 production smoke 仍 blocked。用户明确要求继续推进交接文件和剩余业务/安全/集成内容，不要停在 UI。

本轮目标：
1. 不粗暴重写 CSS，不破坏 Material + Arco Design UI。
2. 审核现有报告中的 remaining/blocked 项，找出当前仓库还能本地推进的事项，优先补齐：
   - production smoke/readiness 的可执行性和 requiredChecks 清晰度；
   - 外部 blocker 交接文档是否足够让后续真实供应商/运维接入；
   - session invalidation / queue broker / secret resolver / provider health / writeback readyFields-only smoke 的本地 contract 与文档一致性；
   - API 契约、Evaluation schema 解析、demo fallback、写回可信边界是否仍有可补测试或报告缺口。
3. 如果发现本地可修问题：按 TDD 先补测试再修实现。
4. 如果剩余全部依赖外部真实环境：不要伪造通过；补齐交接文档、readiness 脚本输出、审计报告，明确 blocked 条件和下一步输入。

必须验证：
- corepack pnpm --filter @medical-record-agent/demo-web test:styles
- corepack pnpm --filter @medical-record-agent/demo-web test:mobile
- corepack pnpm --filter @medical-record-agent/demo-web build
- corepack pnpm test（若失败，必须具体说明失败项，不得笼统写通过）
- 9901 首页和 /api/health 可访问；确认 dist/index.html 使用最新 index bundle。
- 如涉及 readiness/smoke 脚本，运行对应脚本并区分 passed 与 blocked，不得把 blocked 写成 passed。

必须生成/更新：
1. MEDICAL-P1-P2-CONTINUATION-ROUND2-FIX-REPORT.md：说明本轮实际推进、测试、文件、剩余 blocked。
2. MEDICAL-P1-P2-CONTINUATION-ROUND2-AUDIT-REPORT.md：必须包含 7 维度：产品概述、功能完整性、业务流程完整性、用户体验、技术实现、P0/P1/P2 问题清单、验收结论。
3. 如 docs/2026-06-09-p2-production-handoff.md 仍不够可执行，更新它。

约束：
- 不要提交 git commit。
- 不要修改 .env、node_modules、无关缓存。
- 不要启动其他 agent。
- 不要只写计划，必须实际检查、修复可本地修复的问题、验证并生成报告。
- 若遇到 429/上游断流，保留已完成改动和报告草稿，明确记录中断点，不要反复重试。
