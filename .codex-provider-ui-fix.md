# 任务：修复 Provider 设置页面 UI 问题

## 问题清单（从界面审查发现）

### P0（必须修复）
1. **后端 Provider 列表仍显示 LIMS Writeback Adapter** — "后端 Provider 列表"区域直接渲染了 API 返回的 provider 列表，需要过滤掉 LIMS 和 storage 类型的 provider
2. **后端 Provider 列表仍显示 Local Storage Provider** — 同上

### P1（应该修复）
3. **统计显示 "健康实例 2/3"** — 应该只统计 OCR 和 LLM（2个），不是 3 个
4. **描述文字说 "与本地存储 Provider"** — 应改为 "OCR 与 LLM Provider"
5. **"API 密钥" label 重复** — LLM 卡片中 SecretField 的 label 和 Form.Item 的 label 重复了

## 修复要求
1. 在 "后端 Provider 列表" 渲染时过滤掉 `lims` 和 `storage` 类型
2. 统计数字只计算 OCR 和 LLM
3. 修改描述文字
4. 修复重复 label（去掉 Form.Item 的 label 或 SecretField 的 label）

## 完成标准
- 页面只显示 OCR 和 LLM 两个 Provider
- 后端 Provider 列表不显示 LIMS 和 storage
- 统计数字正确（2 个 provider）
- `corepack pnpm --filter @medical-record-agent/demo-web build` 通过
