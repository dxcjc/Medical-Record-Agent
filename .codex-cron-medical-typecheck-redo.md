【重要】不要问问题，不要请求确认，不要启动浏览器询问确认。直接执行。必须遵循 superpowers 流程：brainstorming → writing-plans → TDD/测试优先 → verification-before-completion。

任务：修复医疗项目 P1/P2 审计闭环中当前本地验证失败的问题，并重新生成 7 维度审计报告。

工作目录：/tmp/Medical-Record-Agent

背景：统一产品审计调度员在 2026-06-09 定时巡检时重新跑当前环境验证，发现 MEDICAL-P2-INTEGRATION-HARDENING-AUDIT-REPORT.md 声称 typecheck/test/build 通过，但当前 `corepack pnpm typecheck` 实际失败。失败点：
- apps/api/src/bootstrap/production-services.test.ts 导入了未导出的 createKmsSecretResolver、createSecretManagerResolver、createVaultSecretResolver。
- apps/api/src/services/api-services.test.ts 导入了未导出的 createRedisJobQueueAdapter、RedisJobQueueClient。

当前 `corepack pnpm --filter @medical-record-agent/demo-web build` 通过，但根级 typecheck 未通过，因此不能判定阶段完成。必须修复真实代码/测试契约，不允许只改报告。

必须完成：
1. 先分析现有 production-services.ts、production-services.test.ts、api-services.ts、api-services.test.ts，判断是实现缺失还是测试契约漂移。
2. 修复 typecheck 失败：要么补齐真实导出和最小 contract，要么调整测试到已实现的真实 API，但必须保持 P1/P2 目标：SecretResolver 可插拔边界、真实 KMS/Vault/Secret Manager blocked/contract 语义、Queue adapter/broker/Redis contract 不被虚假宣称为生产 ready。
3. 运行并确保通过：
   - corepack pnpm typecheck
   - corepack pnpm test
   - corepack pnpm --filter @medical-record-agent/demo-web build
   - 如时间允许，补跑 corepack pnpm smoke:demo-web；production smoke 如外部条件缺失允许 blocked，但必须报告 STATUS。
4. 更新或重新生成 MEDICAL-P2-INTEGRATION-HARDENING-FIX-REPORT.md，写清修复文件、验证命令、剩余 blocked 条件。
5. 更新或重新生成 MEDICAL-P2-INTEGRATION-HARDENING-AUDIT-REPORT.md，必须包含 7 个维度：产品概述、功能完整性、业务流程完整性、用户体验、技术实现、P0/P1/P2 问题清单、验收结论。报告不能再写与实际命令不一致的“通过”。
6. 如果无法全部修复，报告必须明确“不通过”和具体失败，不允许粉饰为通过。

约束：
- 不要提交 git commit。
- 不要修改 .env、node_modules、无关缓存。
- 不要整体重写 UI/CSS。
- 不要只写计划；必须实际修复、验证、生成报告。
- 若遇到 429/上游断流，保留已完成改动和报告草稿，明确记录中断点。
