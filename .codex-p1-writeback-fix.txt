【重要】不要问问题，不要请求确认，不要启动浏览器。直接开始工作。

任务：修复 /tmp/Medical-Record-Agent 项目 P1-1 写回安全边界问题。

【背景】
审计报告 MEDICAL-AUDIT-2026-06-11.md 中 P1-1 指出：
写回服务端信任客户端 fields/payload，未重新读取 RecognitionResult.readyFields。
拥有写回权限的调用方可构造与识别结果不一致的字段写入 LIMS。

【修复要求】
1. 修改 `apps/api/src/routes/writeback.routes.ts`：
   - POST /writeback 只接收 jobId、confirmed、可选幂等键
   - 不再从请求体读取 fields/payload 作为执行数据
2. 修改 `apps/api/src/bootstrap/production-services.ts` 中的 writeback executor：
   - 服务端必须重新读取 RecognitionJob 和 RecognitionResult
   - 确认 status=completed、reviewRequired=false、无阻塞 writeback
   - 存在 payload.writeback.readyFields
   - 只使用服务端 readyFields 生成 LIMS payload
   - 客户端传入的数据仅可作为展示/日志参考，不可作为执行数据
3. 更新前端 `WritebackPage.tsx`：
   - 确认前端只发送 jobId + confirmed，不再发送 fields/payload
4. 补充测试验证写回安全边界

【验证要求】
1. pnpm typecheck 必须通过
2. pnpm build 必须通过
3. pnpm test 必须通过（包括新增测试）
4. 生成修复报告到 /tmp/Medical-Record-Agent/P1-1-WRITEBACK-FIX-REPORT.md

