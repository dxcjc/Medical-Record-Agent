你是产品经理助手。对「医疗OCR识别系统」进行 UI/UX 全面重构的头脑风暴。

【必须遵循 superpowers 流程】
读取 .codex/skills/ 了解核心规则。

【重要】不要问问题，不要启动浏览器，不要询问确认。直接开始工作。

步骤：
1. 读取 /tmp/Medical-Record-Agent/apps/demo-web/src 下的源码结构
2. 读取 App.tsx 了解当前路由和页面结构
3. 读取每个页面文件了解现状
4. 逐条分析以下重构方案的可行性
5. 对每个方案，提出需要产品经理决策的问题（用 ❓ 标记）
6. 不要自己做决策，所有不确定的都提出来
7. 分析现有API与新页面的映射关系
8. 将完整报告写入 /tmp/Medical-Record-Agent/UI-REDESIGN-BRAINSTORM.md

重构方向：
一级导航只保留4个：工作台、识别任务、数据管理、系统设置

P0：报错改toast、识别流程闭环（上传→识别→查看→编辑→确认→导出）、任务状态实时反馈、空状态引导、结果查看简化
P1：新建识别简化（用识别类型代替Schema/Provider选择）、确认+导出一体化、表单校验优化、Schema管理隐藏

输出要求：
- 全部用中文
- 每个优化点：现状分析 + 问题 + 需要决策的问题（用 ❓ 标记）
- 分析现有API与新页面的映射关系
- 末尾写上「头脑风暴完成，请产品经理回答以上 ❓ 问题」