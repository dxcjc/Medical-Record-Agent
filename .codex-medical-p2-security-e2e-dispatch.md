【重要】不要问问题，不要请求确认，不要启动浏览器询问确认。直接执行。必须遵循 superpowers 流程：brainstorming → writing-plans → TDD/测试优先 → verification-before-completion。

任务：继续推进 /tmp/Medical-Record-Agent 医疗项目 P2/上线前安全、集成、E2E 验收闭环。不要把 UI 阶段通过或上一轮 P1 通过误判为项目最终完成。

背景：
- 最新 /tmp/Medical-Record-Agent/MEDICAL-P1-P2-NEXT-AUDIT-REPORT.md 显示本轮 P1 阶段通过，但医疗项目最终产品验收仍不通过。
- 已验证：corepack pnpm typecheck、corepack pnpm test、corepack pnpm --filter @medical-record-agent/demo-web test:styles、test:mobile、build 均通过。
- 9901 首页和 /api/health 可访问，dist/index.html 已引用新 bundle。
- 不要提交 git commit。
- 医疗 UI 比之前明显改善，必须保持企业级 Material + Arco Design UI；如涉及样式只能增量修复，不能粗暴重写 CSS。

优先推进的未闭环项（按可本地落地优先，不要伪造外部环境）：
[P2-1] 前端异步识别任务 UX 闭环增强。
- 新建识别后要适配 asynchronous/queued/running/statusUrl/resultUrl 语义，提供明确进度、轮询、terminal 后跳转/读取结果、失败错误提示。
- 补充测试覆盖 queued/running/completed/failed 关键状态。

[P2-2] 真实浏览器 E2E / smoke 验收补齐。
- 在现有测试框架内增加可本地稳定运行的 demo-web basic E2E 或 runtime smoke：登录页、首页 shell、关键路由、/api/health、生产 dist bundle 可加载。
- 如果无法启动真实浏览器，不要伪造；用可执行脚本/测试明确标记 blocked 或 mock-runtime，并写入报告。

[P2-3] 安全基线继续增强。
- 优先做无需外部系统即可落地的安全项：API 安全响应头/CSP 基线、登录/高风险接口 rate limit 或测试化策略、secretRefs 不泄漏回归测试、生产 demo 凭据不预填/不泄漏。
- 不要宣称真实 KMS 已完成；只能保留为外部部署项。

[P2-4] 打包优化告警。
- demo-web build 仍有 Arco manualChunks circular chunk 提示。请分析 Vite manualChunks，尽量消除循环 chunk 提示或在报告中说明为什么暂不能安全消除；不要靠忽略警告。

验证要求：
1. corepack pnpm typecheck 必须通过。
2. corepack pnpm test 必须通过。
3. corepack pnpm --filter @medical-record-agent/demo-web test:styles 必须通过。
4. corepack pnpm --filter @medical-record-agent/demo-web test:mobile 必须通过。
5. corepack pnpm --filter @medical-record-agent/demo-web build 必须通过。
6. 检查 http://localhost:9901/ 和 http://localhost:9901/api/health 可访问，并确认 apps/demo-web/dist/index.html 引用新 bundle；如果 9901 环境不是由当前进程控制，报告中说明验证结果。
7. 用代码搜索确认异步 UX、E2E/smoke、安全基线、chunk 优化确实落地。

报告要求：
1. 生成 /tmp/Medical-Record-Agent/MEDICAL-P2-SECURITY-E2E-FIX-REPORT.md，说明修复点、文件、验证命令、剩余问题。
2. 生成 /tmp/Medical-Record-Agent/MEDICAL-P2-SECURITY-E2E-AUDIT-REPORT.md，必须包含 7 个维度：产品概述、功能完整性、业务流程完整性、用户体验、技术实现、P0/P1/P2 问题清单、验收结论。
3. 验收结论必须区分“本轮 P2 安全/E2E/集成阶段是否通过”和“医疗项目最终产品是否通过”。真实外部 sandbox、持久化队列、真实 KMS 未配置时，不得写最终产品通过。

不要问问题，直接修复、验证、生成报告。