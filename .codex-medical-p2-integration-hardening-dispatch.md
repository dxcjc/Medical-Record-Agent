【重要】不要问问题，不要请求确认，不要启动浏览器询问确认。直接执行。必须遵循 superpowers 流程：brainstorming → writing-plans → TDD/测试优先 → verification-before-completion。

任务：继续推进医疗项目 P1/P2 业务/安全/集成生产化闭环（下一轮工程化可落地项），不要把 UI 当前阶段通过误判为项目最终完成。

工作目录：/tmp/Medical-Record-Agent

必须先读取：
1. .codex-medical-p1-p2-continuation.md
2. PRODUCT-AUDIT-REPORT.md
3. MEDICAL-P2-PRODUCTION-CLOSURE-AUDIT-REPORT.md
4. MEDICAL-P2-PRODUCTION-CLOSURE-FIX-REPORT.md
5. docs/2026-06-09-p2-production-handoff.md
6. 已有 P1/P2/安全/E2E 报告：MEDICAL-P2-SECURITY-E2E-AUDIT-REPORT.md、MEDICAL-P2-E2E-UI-PERF-AUDIT-REPORT.md、MEDICAL-P1-P2-NEXT-AUDIT-REPORT.md

背景与当前状态：
- UI 当前阶段已明显改善并通过阶段验证，必须保持企业级 Material + Arco Design UI：Primary #3370FF、背景 #F7F8FA、白色侧栏、active nav pill、DM Sans + Noto Sans SC、移动端抽屉/单列/44px 触摸区。
- 不允许粗暴重写 CSS，不允许破坏现有 9901 部署和 /api/health。
- 当前本地已验证：demo-web test:styles、test:mobile、build、全量 corepack pnpm test 均通过；9901 首页和 dist bundle 一致，/api/health 可访问。
- 项目仍未最终完成：真实外部 OCR/LLM/LIMS sandbox、真实 KMS/Vault/Secret Manager、多实例持久化可靠队列仍是 blocked/remaining。

本轮目标：在不需要真实外部凭据的前提下，继续推进“可工程化落地”的生产化闭环，重点不要停留在 UI：
1. 真实密钥库边界进一步工程化：
   - 在现有 env secret resolver contract 基础上，设计并实现可插拔 SecretResolver 接口/工厂边界；
   - 支持 env resolver 保持现有测试通过；
   - 为 Vault/KMS/Secret Manager 接入预留清晰配置与 fail-fast contract（没有真实 SDK/凭据时不要伪造真实接入）；
   - 确保 API/Provider health/日志不泄露明文密钥，只暴露 secretRef/provider key/blocked reason。
2. 多实例可靠队列边界进一步工程化：
   - 在现有 queue contract 基础上，补充 QueueAdapter/JobQueue 接口、in-process 实现、broker 模式配置校验/blocked 输出；
   - 如仓库已有任务状态模型，补充 lease/retry/dead-letter/heartbeat 的接口或最小 contract 测试；
   - 不要引入无法安装的大依赖；无真实 Redis/RabbitMQ/SQS 时不能写“真实 broker 已通过”。
3. production smoke 与交接文档：
   - smoke 输出必须明确区分 passed、blocked、failed；
   - blocked 时列出缺失的真实 sandbox/KMS/broker 条件；
   - 更新 docs/2026-06-09-p2-production-handoff.md，说明下一步部署方如何接真实 sandbox、KMS/Vault、broker。
4. 测试与构建：
   - 新增/更新测试覆盖上述 contract；
   - 必须运行并记录：corepack pnpm typecheck、corepack pnpm test、corepack pnpm --filter @medical-record-agent/demo-web test:styles、corepack pnpm --filter @medical-record-agent/demo-web test:mobile、corepack pnpm --filter @medical-record-agent/demo-web build；
   - 验证 9901 首页和 /api/health 可访问，确认 dist/index.html 引用新 bundle。
5. 报告：
   - 生成 MEDICAL-P2-INTEGRATION-HARDENING-FIX-REPORT.md，说明修复范围、文件、测试、blocked 外部条件。
   - 生成 MEDICAL-P2-INTEGRATION-HARDENING-AUDIT-REPORT.md，必须包含 7 个维度：产品概述、功能完整性、业务流程完整性、用户体验、技术实现、P0/P1/P2 问题清单、验收结论。
   - 验收结论必须分层：UI 当前阶段、P1/P2 本轮工程化阶段、真实外部集成、医疗项目最终产品。不能把 blocked 写成通过。

约束：
- 不要提交 git commit。
- 不要修改 .env、node_modules、无关缓存。
- 不要覆盖/破坏现有 Material + Arco CSS；如必须改样式，只做增量补丁并保留测试。
- 不要只写计划，必须实际修复、测试、生成报告。
- 若遇到 429/上游断流，保留已完成改动和报告草稿，明确记录中断点，不要反复重试。
