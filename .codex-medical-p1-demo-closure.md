【重要】不要问问题，不要请求确认，不要启动浏览器。直接执行。必须遵循 superpowers 流程。

任务：修复医疗项目 P1-2 和 P1-3，让 demo 模式真正执行识别编排，去除静态数据兜底。

工作目录：/tmp/Medical-Record-Agent

背景：
- MEDICAL-AUDIT-2026-06-11.md 显示 P1-2 和 P1-3 未修复
- P1-2：demo-services.ts 的 jobService.create 只创建 queued 内存任务，不执行 OCR/LLM/校验编排
- P1-3：JobDetailPage.tsx 在 API 失败后仍展示静态 OCR/字段/证据/trace 数据

必须完成：
1. 修改 apps/api/src/demo-services.ts：
   - demo 模式创建任务后，使用 mock OCR + mock LLM + core orchestrator 完整跑一遍识别流程
   - 结果必须与 jobId/sourceFileId 关联，不能返回固定演示数据
   - 保持现有 API 接口不变

2. 修改 apps/demo-web/src/pages/recognition/JobDetailPage.tsx：
   - API 失败时不再用静态 demo 数据兜底
   - 改为空态 + 重试按钮
   - 仅在显式 VITE_DEMO_MODE=true 时展示静态样例，并在 UI 上标识为"演示数据"

3. 修改 apps/demo-web/src/pages/operations/WritebackPage.tsx：
   - eligible 加载失败时不再保留静态 demo 写回列表
   - 改为空态 + 重试按钮

4. 验证：
   - pnpm build 必须通过
   - pnpm test 必须通过（至少 454 passed）
   - pnpm typecheck 必须通过

5. 生成报告：
   - MEDICAL-P1-2-P1-3-FIX-REPORT.md：修复点、文件、验证结果

约束：
- 不要修改 .env、node_modules
- 不要破坏现有 Material + Arco Design UI
- 不要破坏 9901 部署
- 保持 mock OCR/LLM provider 作为 demo 模式的默认 provider
- 不要硬删除 demoData.ts（它可能被其他页面引用作为类型/标签定义）
