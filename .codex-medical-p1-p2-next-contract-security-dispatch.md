【重要】不要问问题，不要请求确认，不要启动浏览器询问确认。直接执行。必须遵循 superpowers 流程：brainstorming → writing-plans → TDD/测试优先 → verification-before-completion。

任务：继续推进医疗项目 P1/P2 业务/安全/集成闭环，不要把 UI 阶段通过误判为医疗项目完成。

工作目录：/tmp/Medical-Record-Agent

必须优先读取：
1. .codex-medical-p1-p2-continuation.md
2. PRODUCT-AUDIT-REPORT.md
3. MEDICAL-P1-P2-CONTINUATION-ROLLUP-AUDIT-REPORT.md
4. MEDICAL-P2-PRODUCTION-CLOSURE-AUDIT-REPORT.md
5. MEDICAL-P1-P2-CONTRACT-HARDENING-AUDIT-REPORT.md、MEDICAL-P1-P2-CONTRACT-CLOSURE-AUDIT-REPORT.md、MEDICAL-P1-P2-CONTRACT-READINESS-AUDIT-REPORT.md

当前状态：
- UI 当前阶段已明显改善，Material + Arco Design 必须保持，不能粗暴重写 CSS。
- 本轮调度员已验证：test:styles 通过、test:mobile 通过、demo-web build 通过且无 500k JS chunk warning；http://localhost:9901/ 与 /api/health 可访问，dist 为新构建。
- 医疗最终产品仍未完成：真实 OCR/LLM/LIMS sandbox、真实 KMS/Vault/Secret Manager、多实例 session invalidation store、真实 broker/queue、多实例 production smoke 仍 blocked。
- 当前可继续推进的非外部依赖方向：schemas/providers/audit 等 route/response DTO 收敛、repository unknown 边界收敛、真实 blocked 条件的 readiness/smoke 可诊断性、生产安全基线文档与测试、浏览器 E2E/移动端覆盖补强。

执行要求：
1. 先分析已有报告中的 P1/P2 未闭环项，避免重复已经完成的 UI/chunk/writeback/demo fallback/evaluation schema 工作。
2. 选择当前环境可落地且不依赖外部凭据的下一批 P1/P2：优先收敛 schemas/providers/audit API 契约与响应类型，补测试；如发现更高优先级阻断，先修阻断。
3. 保持 TDD：先写/补失败测试，再实现。
4. 不要提交 git commit。
5. 不要修改 .env、node_modules、缓存、无关生成物。
6. 医疗样式只能做小范围修复，必须保持 Material + Arco Design，不得整体覆盖 CSS。
7. 必须运行并记录：
   - corepack pnpm --filter @medical-record-agent/demo-web test:styles
   - corepack pnpm --filter @medical-record-agent/demo-web test:mobile
   - corepack pnpm --filter @medical-record-agent/demo-web build
   - corepack pnpm typecheck
   - corepack pnpm test（若失败，报告必须列出具体失败和是否为本轮新增）
8. 必须检查 9901 首页和 /api/health，确认 dist bundle 为新构建。
9. 产出报告：
   - MEDICAL-P1-P2-NEXT-CONTRACT-SECURITY-FIX-REPORT.md
   - MEDICAL-P1-P2-NEXT-CONTRACT-SECURITY-AUDIT-REPORT.md
10. 审计报告必须包含 7 个维度：产品概述、功能完整性、业务流程完整性、用户体验、技术实现、P0/P1/P2 问题清单、验收结论。
11. 结论必须分层：UI 当前阶段、P1/P2 本轮阶段、真实外部集成、医疗最终产品。不要把 blocked 外部项写成通过。

完成标准：
- 有实际代码/测试/文档变更，不允许只写计划。
- 报告文件存在且内容具体。
- 验证命令结果写入报告。
- 如遇 429/上游断流，保留已完成改动并在报告中写清中断点，不要反复重试。
