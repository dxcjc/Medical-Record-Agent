【重要】不要问问题，不要请求确认，不要启动浏览器询问确认。直接执行。不要提交 git commit。不要改 .env/node_modules/缓存。不要粗暴重写 CSS，必须保持现有企业级 Material + Arco Design UI（Primary #3370FF、背景 #F7F8FA、白色侧栏、active nav pill、DM Sans + Noto Sans SC、移动端抽屉/单列/44px 触摸区）。

【必须遵循 superpowers 流程】
读取仓库内已有 superpowers 文档/计划（如存在 CLAUDE.md、docs/superpowers/plans、docs/superpowers/specs）以及以下报告：
- .codex-medical-p1-p2-continuation.md
- PRODUCT-AUDIT-REPORT.md
- MEDICAL-ARCO-UI-AUDIT-REPORT.md
- STYLE-FIX-REPORT.md
- MEDICAL-UI-REFLOW-FIX-REPORT.md
- P1-AUDIT-REPORT.md
- P1-FIX-REPORT.md
- MEDICAL-P1-P2-NEXT-AUDIT-REPORT.md
- MEDICAL-P2-SECURITY-E2E-AUDIT-REPORT.md

你必须在报告中说明已执行：
1. brainstorming：分析当前 UI 阶段已通过但项目未最终完成，梳理剩余 P1/P2 中可在本地闭环的事项；
2. writing-plans：写入 docs/superpowers/plans/ 下的新计划文件；
3. TDD/测试优先：先补测试/脚本断言，再实现；
4. verification-before-completion：完成后运行验证并生成报告。

任务：继续推进 /tmp/Medical-Record-Agent 医疗项目 P1/P2 业务/安全/集成闭环。不要把 UI 阶段通过误判为最终完成。本轮优先处理“无需真实外部 sandbox/KMS 即可在本地代码闭环”的剩余项，同时对真实外部项保持 blocked/remaining 语义，不能伪造通过。

当前已知状态：
- UI 已明显改善，已有 Material + Arco Design 企业级风格；后续 UI 只做增量细节修复，不要整体重写。
- 最新阶段报告显示：typecheck/test/demo-web styles/mobile/build、9901 基础访问和 mock-runtime smoke 已通过。
- 医疗项目最终产品仍不通过，剩余包括：真实浏览器 E2E/截图、真实外部 OCR/LLM/LIMS sandbox smoke、持久化/多实例可靠队列、真实 KMS/Vault/Secret Manager、vendor-arco 体积进一步优化、以及少量间距/遮挡/换行 UI 细节。

本轮必须优先尝试闭环：

[P2-1] 真实浏览器 E2E / 移动端截图验收能力
- 在现有工程中增加可本地稳定运行的浏览器 E2E（优先 Playwright；如果依赖/浏览器不可用，必须实现明确的 blocked 检测与报告，不得伪造）。
- 覆盖：登录页、首页 shell、/recognition/new、/recognition/jobs/demo、/providers、/writeback、移动端导航抽屉或移动布局关键断言。
- 至少要有脚本/测试能区分：browserE2E=passed / blocked，并输出原因。
- 如能运行浏览器，保存截图到受控目录（例如 ui-parity-screenshots/medical-e2e-current/）并在报告说明；如不能运行，保留 blocked 语义并给出下一步环境要求。

[P2-2] UI 细节增量巡检与修复
- 针对用户反馈的间距、遮挡、换行：全局巡检 AppShell、页面头部、卡片、表格、表单、移动端，不要只修单页。
- 只做小范围 CSS/组件 class 增量修复；不要覆盖/重写整个 styles.css；不要破坏现有 Material + Arco 设计系统。
- 增加/更新 style guard 或 mobile guard，防止侧栏遮挡、header 换行溢出、触摸区域不足、表格横滚失效。

[P2-3] vendor chunk / 打包体积进一步优化分析与安全改进
- 复查 demo-web build 输出。如果 vendor-arco 仍超过 500kB，优先采用不会破坏循环依赖的安全策略：路由懒加载、业务页面拆分、按需 import、分析报告；不要为了消除警告盲目拆 Arco 子 chunk 导致 circular warning 回归。
- 必须保留/补充测试，防止恢复有问题的 Arco 细拆 manualChunks。
- 如果无法安全降低 vendor-arco 到 500k 以下，报告必须明确原因、当前最大 chunk、后续策略，不能写“已完全解决”。

[P2-4] 可靠队列 / KMS / 外部 sandbox 的产品化交接
- 不要求本轮接入真实 KMS 或真实外部 OCR/LLM/LIMS；但要把当前 blocked/remaining 转为可执行交接：更新 README 或 docs/handoff，让部署方知道需要哪些 env、sandbox、KMS/Vault、队列/broker、CI smoke 参数。
- 如能在代码中增加 contract-level tests 或配置校验（例如 production smoke mode、secret resolver、queue mode）则补充；不能影响现有构建。

必须运行并尽量通过：
1. corepack pnpm typecheck
2. corepack pnpm test
3. corepack pnpm --filter @medical-record-agent/demo-web test:styles
4. corepack pnpm --filter @medical-record-agent/demo-web test:mobile
5. corepack pnpm --filter @medical-record-agent/demo-web build
6. corepack pnpm smoke:demo-web
7. 如新增浏览器 E2E 脚本，运行对应脚本；若 blocked，脚本必须以报告可识别的方式输出 blocked 原因。

9901 验证：
- 检查 http://localhost:9901/ 和 http://localhost:9901/api/health 可访问。
- 确认 apps/demo-web/dist/index.html 与 9901 返回 HTML 引用真实 dist bundle；不要只看 title。

必须生成/更新报告：
1. MEDICAL-P2-E2E-UI-PERF-FIX-REPORT.md：说明本轮修复点、文件、测试先行过程、验证命令、9901 结果、剩余 blocked 项。
2. MEDICAL-P2-E2E-UI-PERF-AUDIT-REPORT.md：必须包含 7 个维度：产品概述、功能完整性、业务流程完整性、用户体验、技术实现、P0/P1/P2 问题清单、验收结论。

验收结论必须明确区分：
- UI 当前阶段是否继续通过；
- 本轮 P2 E2E/UI细节/性能/交接阶段是否通过；
- 医疗项目最终产品是否通过（真实外部 sandbox、生产 KMS、持久化队列、真实 browser E2E 若未通过时，最终产品不得写通过）。

若遇到 429/上游断流/依赖安装失败/浏览器不可用：保留已完成改动和报告草稿，明确记录中断点或 blocked 原因，不要反复重试。