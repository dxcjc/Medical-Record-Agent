【重要】不要问问题，不要请求确认，不要启动浏览器询问确认。直接执行。必须遵循 superpowers 流程：brainstorming → writing-plans → TDD/测试优先 → verification-before-completion。

任务：继续推进 /tmp/Medical-Record-Agent 医疗项目 P1/P2 可本地闭环项，重点不要停留在 UI 阶段，也不要把 external blocked 伪造成完成。

背景与必须读取：
1. 先读取 .codex-medical-p1-p2-continuation.md、PRODUCT-AUDIT-REPORT.md。
2. 再读取 MEDICAL-P2-NEXT-CLOSURE-AUDIT-REPORT.md、MEDICAL-P2-NEXT-LOCAL-CLOSURE-AUDIT-REPORT.md、MEDICAL-P2-PRODUCTION-CLOSURE-AUDIT-REPORT.md、STYLE-FIX-REPORT.md、P1-FIX-REPORT.md、P1-AUDIT-REPORT.md。
3. 当前 UI 阶段已明显改善并阶段通过，但医疗最终产品仍 blocked：真实 OCR/LLM/LIMS sandbox、真实 KMS/Vault/Secret Manager、生产多实例 session store、真实 broker 多实例可靠队列尚未完成。

本轮目标：选择一个“不依赖外部真实凭据/服务、但能推进交接和产品化”的 P1/P2 闭环点实施。优先级如下：
A. 前端异步任务/长任务 UX：为 demo-web 中识别任务、Evaluation run、写回/Provider/Schema 等关键异步操作补更细粒度的取消、队列积压/处理中提示、失败重试/错误恢复和测试；不得破坏现有 Material + Arco UI。
B. 若 A 已充分实现，则补强交接/继续推进文档和 readiness 脚本，让真实外部 sandbox、真实 KMS、真实 broker、多实例 session smoke 的接入步骤更可执行，并加测试确保 blocked/ready 分层不被误报。
C. 若发现 PRODUCT-AUDIT-REPORT.md 中仍有未覆盖的本地可修 P1/P2（例如 Evaluation schemaKey、API contract unknown、静态 fallback、写回可信边界），优先修真实代码和测试。

硬性约束：
- 不要粗暴重写 CSS；必须保持企业级 Material + Arco Design UI：Primary #3370FF、背景 #F7F8FA、白色侧栏、active nav pill、DM Sans + Noto Sans SC、移动端抽屉/单列/44px 触摸区。
- 不要修改 .env、node_modules、无关缓存。
- 不要提交 git commit。
- 不要只写计划，必须实际修复、测试、生成报告。
- external blocked 项要保持 blocked，不准为了通过报告而改成 passed。

必须验证：
1. corepack pnpm --filter @medical-record-agent/demo-web test:styles
2. corepack pnpm --filter @medical-record-agent/demo-web test:mobile
3. corepack pnpm --filter @medical-record-agent/demo-web build
4. corepack pnpm test（如失败，报告必须写清具体失败和是否本轮引入）
5. 检查 http://localhost:9901/ 和 /api/health；确认 dist/index.html 与 9901 首页引用同一个真实 dist bundle。

必须产出：
1. MEDICAL-P2-ASYNC-HANDOFF-FIX-REPORT.md：说明本轮修复点、文件、测试、9901 检查、剩余 blocked。
2. MEDICAL-P2-ASYNC-HANDOFF-AUDIT-REPORT.md：必须包含 7 个维度：产品概述、功能完整性、业务流程完整性、用户体验、技术实现、P0/P1/P2 问题清单、验收结论。
3. 如修改了交接文档，请在报告列出具体文档路径。

最终结论必须分层：UI 当前阶段 / P1-P2 本轮阶段 / 真实外部集成 / 医疗最终产品。不要把阶段通过写成最终完成。