【重要】不要问问题，不要启动浏览器，不要询问确认。直接开始工作。
【必须遵循 superpowers 流程】如果仓库内有 CLAUDE.md 或 superpowers 说明，读取后执行：brainstorming → writing-plans → TDD → verification-before-completion。不要只写计划，必须完成代码修复、验证和报告。

任务：修复 /tmp/Medical-Record-Agent 最新 P1 审计报告中的不通过项，并重新生成产品级审计报告。

【背景】
最新报告：/tmp/Medical-Record-Agent/P1-AUDIT-REPORT.md
结论：不通过。
已通过：build/typecheck/test 当前报告显示通过。
必须修复以下 P1 阻断项：

[P1-3] Schema 发布危险操作缺少二次确认
- 修复要求：发布动作必须与停用/回滚同级二次确认；点击“发布”只能打开确认弹窗，确认后才调用 api.publishSchemaDraft()。
- 测试要求：补测试证明未确认时不会调用发布 API，确认后才调用。

[P1-6] 前后端契约集中化仍不完整 + 生产写回装配漂移
- 修复要求 A：把页面内 API response shape 解析迁移到集中 API mapper/normalizer 层。页面不应直接维护 response: unknown、job: unknown、result: unknown 的后端 shape 兼容逻辑；unknown 只能保留在 JSON 入口、错误响应、外部 payload 边界。
- 涉及报告点名位置：EvaluationPage parseEvaluationSchemaOptions/parseEvaluationProviderOptions、NewRecognitionPage parseSchemaOptions、JobDetailPage parseApiDetail、WritebackPage normalizeApiJobToWritebackJob。
- 修复要求 B：修复生产写回装配漂移。/writeback 生产路径必须使用服务端持久化 RecognitionResult / readyFields 构造 payload，不能依赖 executor 入参 fields/payload。
- 测试要求：补生产装配测试，断言手动 /writeback 会把服务端 readyFields 写入 LIMS adapter，而不是 payload: {}。

[P1-8] 识别创建本地文件读取/哈希不可取消
- 修复要求：buildRecognitionFileUploadInput、blobToBase64、blobSha256Hex 或等价调用链接收 AbortSignal，在每个异步阶段前后检查并抛出 AbortError。
- 测试要求：补识别创建取消和重跑测试，覆盖本地读取/哈希取消。

【修复后必须运行】
1. pnpm typecheck
2. pnpm test
3. pnpm --filter @medical-record-agent/demo-web build
4. pnpm build

【必须生成报告文件】
1. /tmp/Medical-Record-Agent/P1-FIX-REPORT.md：说明修复文件、测试、验证命令结果。
2. /tmp/Medical-Record-Agent/P1-AUDIT-REPORT.md：重新生成 7 维度产品级审计报告，必须包含：
   1) 产品概述
   2) 功能完整性
   3) 业务流程完整性
   4) 用户体验
   5) 技术实现
   6) P0/P1/P2 问题清单
   7) 验收结论（明确通过/不通过）

不要提交 git，不要改 .env，不要提交 dist/node_modules。