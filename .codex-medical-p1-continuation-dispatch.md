【重要】不要问问题，不要请求确认，不要启动浏览器询问确认。直接执行。必须遵循 superpowers 流程：brainstorming → writing-plans → TDD/测试优先 → verification-before-completion。

任务：继续推进医疗项目 P1 业务/安全/集成优化闭环，修复最新审计报告 P1 未完成项中可在代码内闭环的部分，并生成报告。

工作目录：/tmp/Medical-Record-Agent

背景文件必须先读：
1. PRODUCT-AUDIT-REPORT.md
2. P1-P2-CONTINUATION-AUDIT-REPORT.md
3. .codex-medical-p1-p2-continuation.md

当前状态（调度员已复验）：
- demo-web test:styles 通过，11 tests passed。
- demo-web test:mobile 通过，1 passed、10 skipped。
- demo-web build 通过，无 500k JS chunk 警告。
- 根级 corepack pnpm test 通过，62 passed、1 skipped；291 passed、1 skipped。
- corepack pnpm smoke:production 因 PRODUCTION_SMOKE_BASE_URL 未配置失败，这属于环境未配置，不能通过代码伪造通过。
- 最新 P1-P2-CONTINUATION-AUDIT-REPORT.md 结论：P1 打回项阶段通过，但医疗项目整体不通过最终产品验收。剩余 P1 包括：Evaluation production runner schema 解析未闭环、Production smoke 未配置、生产异步任务队列未完成。

本轮必须修复（优先修可代码闭环项）：
[P1-1] Evaluation production runner schema resolution 未闭环
- 文件重点：apps/api/src/bootstrap/production-services.ts createProductionEvaluationRunner()，apps/api/src/services/api-services.ts createRun()，相关测试。
- 修复要求：生产 evaluation runner 必须按 runInput.schemaConfig.schemaKey/schemaVersionId 或等价字段解析目标 schema；不能继续固定 limsClinicalInfoSchema。应尽量复用生产识别 schema resolution 或抽出公共 helper。metrics/result 中应能记录/反映实际使用 schemaVersionId 或 schemaKey（按现有数据模型可行范围实现）。
- 必须新增/更新测试证明：创建 evaluation run 时指定不同 schemaKey/schemaVersion 后，production runner 使用目标 schema，而不是固定内置 schema。

[P1-2] Production smoke 未配置的产品化处理
- 不允许伪造外部环境通过。
- 要求：改进 scripts/production-smoke.ts 或 README/报告，使未配置时有清晰的 skipped/blocked 语义；如项目测试约定允许，可增加测试确保缺少 PRODUCTION_SMOKE_BASE_URL 时输出明确阻塞原因。报告里必须说明真实外部 OCR/LLM/LIMS 仍需环境变量和 sandbox，不能判定通过。

[P1-3] 生产异步任务队列未完成的最小闭环方案
- 不要求一次性实现完整队列系统，避免大范围破坏。
- 要求：先做产品级最小可验收改进：明确 job create 的同步/异步 mode 配置、超时/状态语义，或补齐可测试的队列接口骨架/文档/测试，避免前端以为 queued/running 已完整异步。若本轮无法安全实现 worker，必须在报告中列为 P1 remaining，并说明原因与下一轮明确任务。

修复后必须执行并记录结果：
1. corepack pnpm test
2. corepack pnpm --filter @medical-record-agent/demo-web build
3. corepack pnpm smoke:production（允许因缺少 PRODUCTION_SMOKE_BASE_URL 以 blocked 记录，但不能写成通过）
4. 若新增定向测试，请记录具体命令。

必须生成/更新报告：
1. MEDICAL-P1-CONTINUATION-FIX-REPORT.md：说明本轮修复内容、文件、验证命令、未完成项。
2. MEDICAL-P1-CONTINUATION-AUDIT-REPORT.md：必须包含 7 个维度：产品概述、功能完整性、业务流程完整性、用户体验、技术实现、P0/P1/P2 问题清单、验收结论。结论必须诚实区分“本轮 P1 可代码闭环项是否通过”和“医疗项目最终是否通过”。

约束：
- 不要提交 git commit。
- 不要修改 .env、node_modules、dist 缓存、无关构建产物。
- 不要破坏现有 Material + Arco UI 和 9901 部署。
- 不要只写计划，必须实际修复、测试、生成报告。
- 若遇到 429/上游断流，保留已完成改动和报告草稿，明确记录中断点，不要反复重试。
