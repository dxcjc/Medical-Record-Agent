# 医疗 OCR 识别系统 — UI/UX 全面重构头脑风暴

**日期**：2026-06-12
**状态**：头脑风暴完成

---

## 1. 当前问题清单（按严重程度排序）

### P0 — 阻塞性问题

| # | 问题 | 现状 | 影响 |
|---|------|------|------|
| 1 | **报错直接写在页面上** | 多处使用 Alert 组件显示 API 错误 | 用户看到技术性错误信息，不友好 |
| 2 | **页面过多，导航混乱** | 13 个页面，3 组导航（Core/Operations/Settings） | 用户找不到功能，操作路径不清晰 |
| 3 | **Schema/Provider 概念暴露** | 新建识别时需要选择 Schema 和 Provider | 非技术用户不理解这些概念 |
| 4 | **缺少操作引导** | 新用户不知道如何开始 | 首次使用体验差 |
| 5 | **识别流程不闭环** | 上传→识别→查看→编辑→确认→导出，分散在多个页面 | 用户需要跳来跳去 |

### P1 — 体验问题

| # | 问题 | 现状 | 影响 |
|---|------|------|------|
| 6 | **任务状态无实时反馈** | 创建任务后需要手动刷新查看状态 | 用户不知道任务进度 |
| 7 | **结果查看复杂** | 识别结果展示技术性字段过多 | 非技术用户看不懂 |
| 8 | **Schema 管理过于复杂** | SchemaStudioPage 功能强大但操作复杂 | 普通用户用不上 |
| 9 | **表单校验不统一** | 部分表单有校验，部分没有 | 用户提交后才知道错误 |

### P2 — 可优化项

| # | 问题 | 现状 | 影响 |
|---|------|------|------|
| 10 | **评测页面暴露技术概念** | EvaluationPage 显示数据集、指标等 | 非技术用户用不上 |
| 11 | **审计日志过于技术化** | AuditLogPage 显示原始操作记录 | 普通用户看不懂 |
| 12 | **Agent 追踪页面** | AgentTracePage 显示 LLM 调用细节 | 普通用户用不上 |

---

## 2. 新的信息架构设计

### 当前结构（13 页，3 组）
```
Core: Dashboard, New Recognition, Job Detail
Operations: Schema Studio, Evaluation, Feedback Samples, Writeback
Settings: Provider Settings, Audit Log, Agent Trace, Dataset Spec
```

### 新结构（4 个一级页面）
```
1. 工作台 (Dashboard) — 快捷操作 + 今日统计 + 最近任务 + 首次引导
2. 识别任务 (Jobs) — 任务列表 + 新建识别（引导式向导）+ 任务详情
3. 数据管理 (Data) — Schema 管理 + 已确认数据 + 导出
4. 系统设置 (Settings) — Provider 配置 + 评测 + 审计日志 + Agent 追踪
```

### 页面合并方案

| 原页面 | 新归属 | 说明 |
|--------|--------|------|
| DashboardPage | 工作台 | 保留，增加快捷操作和引导 |
| NewRecognitionPage | 识别任务 | 改为引导式向导，隐藏 Schema/Provider 选择 |
| JobDetailPage | 识别任务 | 保留，简化结果展示 |
| SchemaStudioPage | 数据管理 | 保留，但放到二级导航，普通用户可忽略 |
| EvaluationPage | 系统设置 | 合并到设置页的子 tab |
| ProviderSettingsPage | 系统设置 | 合并到设置页的子 tab |
| AuditLogPage | 系统设置 | 合并到设置页的子 tab |
| WritebackPage | 数据管理 | 合并到数据管理页 |
| FeedbackSamplesPage | 系统设置 | 合并到设置页的子 tab |
| AgentTracePage | 系统设置 | 合并到设置页的子 tab |
| DatasetSpecPage | 系统设置 | 合并到设置页的子 tab |
| LoginPage | 保留 | 独立页面 |

---

## 3. 每个页面详细设计

### 3.1 工作台 (Dashboard)

**目标**：用户打开系统第一眼看到的页面，快速了解今天要做什么。

**布局**：
```
┌─────────────────────────────────────────────┐
│ 欢迎回来，张医生 👋                          │
├─────────────┬─────────────┬─────────────────┤
│ 今日识别    │ 待确认      │ 待导出          │
│    12       │     5       │     3           │
├─────────────┴─────────────┴─────────────────┤
│ 快捷操作                                     │
│ [📄 新建识别] [📊 查看历史] [⚙️ 系统设置]    │
├─────────────────────────────────────────────┤
│ 最近任务                                     │
│ • 血常规报告 - 2分钟前 - ✅ 已完成           │
│ • 生化报告 - 1小时前 - 🔄 识别中             │
│ • 尿常规报告 - 昨天 - ⚠️ 待确认              │
└─────────────────────────────────────────────┘
```

**首次使用引导**（仅当无历史任务时显示）：
```
┌─────────────────────────────────────────────┐
│ 👋 欢迎使用医疗 OCR 识别系统                  │
│                                              │
│ 只需 3 步即可完成文档识别：                    │
│ 1️⃣ 上传医疗文档（支持 PDF/图片）              │
│ 2️⃣ 系统自动 OCR + 结构化提取                 │
│ 3️⃣ 确认结果并导出                            │
│                                              │
│            [🚀 开始第一次识别]                 │
└─────────────────────────────────────────────┘
```

### 3.2 识别任务 (Jobs)

**目标**：管理所有识别任务，包含新建和查看详情。

**子页面**：
1. **任务列表** — 表格展示所有任务，支持筛选（状态/时间/类型）
2. **新建识别** — 引导式向导（3 步）
3. **任务详情** — 查看识别结果、编辑、确认、导出

**新建识别向导**：
```
步骤 1: 上传文档
┌─────────────────────────────────────────────┐
│ 📄 上传医疗文档                               │
│                                              │
│ ┌─────────────────────────────────────────┐ │
│ │                                         │ │
│ │     拖拽文件到这里，或 点击上传            │ │
│ │                                         │ │
│ │     支持 PDF、JPG、PNG，最大 10MB        │ │
│ │                                         │ │
│ └─────────────────────────────────────────┘ │
│                                              │
│ 识别类型（可选，系统会自动判断）：              │
│ [血常规] [生化] [尿常规] [其他]               │
└─────────────────────────────────────────────┘

步骤 2: 识别中（自动跳转）
┌─────────────────────────────────────────────┐
│ 🔄 正在识别...                               │
│                                              │
│ ████████████░░░░░░░░ 60%                     │
│                                              │
│ ✅ OCR 文字提取完成                           │
│ ✅ 结构化数据提取完成                         │
│ 🔄 正在生成报告...                           │
│                                              │
│ 预计剩余时间：约 10 秒                        │
└─────────────────────────────────────────────┘

步骤 3: 确认结果
┌─────────────────────────────────────────────┐
│ ✅ 识别完成                                   │
│                                              │
│ 📋 提取结果                                  │
│ ┌─────────────────────────────────────────┐ │
│ │ 患者姓名: 张三                           │ │
│ │ 检查日期: 2024-01-15                     │ │
│ │ 白细胞:   6.5 ×10⁹/L  ✅ 正常           │ │
│ │ 红细胞:   4.2 ×10¹²/L ✅ 正常           │ │
│ │ 血红蛋白: 135 g/L      ✅ 正常           │ │
│ └─────────────────────────────────────────┘ │
│                                              │
│ [✏️ 编辑] [✅ 确认并导出] [🔄 重新识别]      │
└─────────────────────────────────────────────┘
```

### 3.3 数据管理 (Data)

**目标**：管理 Schema 和已确认的识别数据。

**子页面**：
1. **已确认数据** — 表格展示所有已确认的识别结果，支持筛选和导出
2. **Schema 管理** — 高级功能，默认折叠，展开后可管理 Schema

**布局**：
```
┌─────────────────────────────────────────────┐
│ 数据管理                          [导出 Excel] │
├─────────────────────────────────────────────┤
│ [已确认数据] [Schema 管理（高级）]            │
├─────────────────────────────────────────────┤
│ 🔍 搜索...  |  类型: 全部  |  时间: 全部     │
├─────────────────────────────────────────────┤
│ 患者姓名  | 检查类型 | 检查日期  | 状态 | 操作 │
│ 张三      | 血常规   | 2024-01-15| ✅   | 查看 │
│ 李四      | 生化     | 2024-01-14| ✅   | 查看 │
└─────────────────────────────────────────────┘
```

### 3.4 系统设置 (Settings)

**目标**：集中管理所有系统配置，普通用户可忽略。

**子页面**（Tab 切换）：
1. **Provider 配置** — OCR 和 LLM Provider 管理
2. **评测管理** — 数据集和评测任务
3. **审计日志** — 操作记录
4. **Agent 追踪** — LLM 调用细节
5. **反馈样本** — 用户反馈数据

---

## 4. 引导流程设计

### 4.1 首次使用引导

**触发条件**：用户首次登录且无历史任务

**流程**：
1. Dashboard 显示欢迎卡片（见 3.1）
2. 点击「开始第一次识别」→ 跳转到新建识别向导
3. 向导每一步都有说明文字和示例

### 4.2 空状态引导

**每个列表页空状态**：
```
┌─────────────────────────────────────────────┐
│ 📭 暂无识别任务                               │
│                                              │
│ 上传您的第一份医疗文档，系统将自动识别并       │
│ 提取结构化数据。                               │
│                                              │
│            [🚀 新建识别]                      │
└─────────────────────────────────────────────┘
```

### 4.3 操作提示

**关键操作旁增加 ? 图标**：
- 鼠标悬停显示说明
- 例如：识别类型旁的 ? 图标解释「血常规」「生化」等含义

---

## 5. 报错规范设计

### 5.1 统一错误处理

**API 错误**：使用 Arco `Message` 组件弹出 toast
```typescript
// 统一错误处理
catch (error) {
  const message = getErrorMessage(error);
  Message.error(message);
}
```

**错误码映射**：
| HTTP 状态码 | 中文消息 |
|-------------|----------|
| 400 | 请求参数错误，请检查输入 |
| 401 | 登录已过期，请重新登录 |
| 403 | 没有权限执行此操作 |
| 404 | 请求的资源不存在 |
| 500 | 服务器错误，请稍后重试 |
| 网络错误 | 网络连接失败，请检查网络 |

### 5.2 表单校验

**实时校验**：
- 必填字段标红色星号
- 输入时实时校验格式
- 提交时校验所有字段
- 错误信息显示在字段下方

---

## 6. 实时进度反馈设计

### 6.1 任务状态轮询

**实现方案**：
```typescript
// 创建任务后轮询状态
useEffect(() => {
  if (job?.status === 'processing') {
    const timer = setInterval(async () => {
      const updated = await api.getJob(job.id);
      if (updated.status !== 'processing') {
        clearInterval(timer);
        // 更新状态
      }
    }, 2000); // 每 2 秒轮询一次
    return () => clearInterval(timer);
  }
}, [job?.status]);
```

### 6.2 进度条

**多阶段进度**：
```
阶段 1: OCR 文字提取 (0-30%)
阶段 2: LLM 结构化提取 (30-80%)
阶段 3: 结果生成 (80-100%)
```

**实现**：后端返回当前阶段和进度百分比，前端展示进度条。

---

## 7. 技术风险总结

| 风险 | 等级 | 缓解措施 |
|------|------|----------|
| 页面合并后路由重构 | 中 | 保留原路由做重定向，逐步迁移 |
| Schema 隐藏后功能可用性 | 低 | 高级用户仍可通过数据管理页访问 |
| 轮询性能 | 低 | 任务数量少，2 秒轮询无压力 |
| 新建识别向导复杂度 | 中 | 分步骤实现，每步可独立测试 |

---

## 8. 推荐执行顺序

### 阶段一：核心体验（P0，预计 3 天）
1. 报错改 toast/message
2. 空状态引导
3. 任务状态实时轮询

### 阶段二：流程闭环（P0，预计 3 天）
4. 新建识别向导（3 步引导式）
5. 结果查看简化（分层展示）
6. 确认 + 导出一体化

### 阶段三：导航重构（P1，预计 2 天）
7. 页面合并（13 页 → 4 个一级页面）
8. 侧边栏导航重构
9. Schema 管理隐藏到高级设置

### 阶段四：体验优化（P1，预计 2 天）
10. Dashboard 重新设计（快捷操作 + 统计 + 引导）
11. 表单校验统一
12. 错误码中文映射

---

## 9. API 映射关系

### 现有 API（保留）
- `POST /auth/login` — 登录
- `POST /auth/logout` — 登出
- `GET/POST /files` — 文件上传
- `POST /jobs` — 创建识别任务
- `GET /jobs/:id` — 获取任务详情
- `GET /results/:jobId` — 获取识别结果
- `GET /providers` — Provider 列表
- `GET /schemas` — Schema 列表

### 需要新增的 API
- `GET /jobs/:id/progress` — 获取任务进度（阶段 + 百分比）
- `GET /data/confirmed` — 获取已确认数据列表
- `POST /data/export` — 导出数据为 Excel

### 需要修改的 API
- `POST /jobs` — 增加可选参数 `recognitionType`（血常规/生化等），自动选择 Schema

---

`---头脑风暴完成---`
