【必须遵循 superpowers 流程】
继续上一轮 hard remove mock provider 任务。不要重来，针对调度者二次验收发现的残留做收尾修复、验证和补报告。

背景：上一轮已生成 `MEDICAL-HARD-REMOVE-MOCK-PROVIDER-FIX-REPORT.md` 和 `MEDICAL-HARD-REMOVE-MOCK-PROVIDER-AUDIT-REPORT.md`，并完成大部分代码/页面修复。但调度者二次扫描发现仍有残留，当前不能最终通过。

用户标准：不是“占位”，是删除用户/业务/文档主线里的 mock 相关内容；后续由用户提供真实模型提供商。

二次验收残留：
1. `apps/demo-web/src/api/normalizers.ts` 仍有 `text.includes("mock provider")`。
2. `apps/api/src/services/api-services.ts` 仍有 `text.includes("mock provider")`。
3. `docs/system-architecture.md` 仍有大量 “Mock Provider” 架构说明，如：
   - “选择 Vitest 和 Mock Provider 的原因”
   - “Mock Provider 是架构必要部分”
   - “本地开发模式使用 mock provider”
   - “Mock Provider 保留为测试后备”
4. `docs/superpowers/specs/2026-06-04-medical-record-recognition-agent-design.md` 仍有 mock provider 路线说明。
5. `docs/superpowers/plans/2026-06-04-medical-record-agent-product.md` 仍有 mock provider 任务/闭环说明。
6. `docs/superpowers/plans/2026-06-09-remove-mock-line.md` 是本次任务留痕，可以保留“历史纠偏”语义，但不应作为产品/架构主线继续写 mock provider 是可用路线。

必须修复：
- 把上述 docs 中的产品/架构主线改为“测试替身 / 合成样本 / fixture / contract test double”等测试语义，避免写 mock provider 是架构必要部分或本地业务模式。
- 旧计划/规格里如果是历史记录，不要伪造历史；可改为“历史草案，已被 hard remove 方案取代”，并在显著位置标注不再作为当前执行方案。
- normalizers/api-services 中如果只是为了识别旧错误文本，改成不含 mock 字面文案的兼容判断，或删除该分支；不能让业务代码继续围绕 mock provider 文案做判断。
- 测试文件里的 `vi.mock`、fetchMock、测试技术 mock 可以保留。
- 历史报告文件里出现 mock 可以保留，但本轮扫描报告要明确哪些属于历史报告/测试文件，哪些已清理。

必须验证：
- `corepack pnpm --filter @medical-record-agent/demo-web test:styles`
- `corepack pnpm --filter @medical-record-agent/demo-web test:mobile`
- `corepack pnpm --filter @medical-record-agent/demo-web build`
- 相关测试；可行则全量 `corepack pnpm test`
- `corepack pnpm smoke:demo-web` 可行则执行
- `http://127.0.0.1:9901/` 与 `/api/health`
- 再跑用户/业务/当前文档主线禁词扫描，排除 `.test.*`、历史报告 `*REPORT.md`、本次任务 prompt/log 后，不应命中：`Mock Provider`、`mock provider`、`mock-ocr`、`mock-model`、`development_placeholder`、`开发占位 provider`。

必须产出：
1. 更新 `MEDICAL-HARD-REMOVE-MOCK-PROVIDER-FIX-REPORT.md`
2. 更新 `MEDICAL-HARD-REMOVE-MOCK-PROVIDER-AUDIT-REPORT.md`
3. 如有必要新增 `MEDICAL-HARD-REMOVE-MOCK-PROVIDER-FINAL-SCAN.md`

审计报告仍需 7 维度，并明确：这次补修是因为二次验收发现文档/业务代码残留，已清理后才可通过。

不要问问题，不要提交 git commit。