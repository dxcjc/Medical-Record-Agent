# 医疗记录识别系统 — E2E 人工模拟测试执行说明书

> 日期：2026-06-15
> 目标：逐功能点用浏览器工具模拟真人操作，排查所有 Bug
> 执行方式：助手用浏览器工具逐页测试 → Codex 审计（256K 上下文）

---

## 一、背景与教训

### 为什么要补 E2E 测试

**血的教训：** 2026-06-15 发现文件上传 400 错误，根因是前端发送 `checksumSha256: ""`（空字符串），后端 Zod 拒绝。这个 bug 只在 HTTP 环境下触发（`crypto.subtle` 不可用），之前每次"测试"都是用 curl 手动调 API，从来没在真实浏览器里走完整流程。

**如果当时用浏览器真实操作：** 点上传 → 点创建任务 → 看到 400 → 第一次就会发现。

### 核心原则

1. **用浏览器工具模拟真人操作** — 不是写测试代码，是逐个页面、逐个按钮点过去
2. **每个功能点记录通过/不通过** — 不通过的立即截图 + 记录错误详情
3. **通过 nginx 代理（9901 端口）** — 模拟真实用户访问路径
4. **Codex 审计** — 测试完成后让 Codex 审查完整性

---

## 二、执行方式

### 工具

- **浏览器操作：** `browser_navigate` → `browser_snapshot` → `browser_click` / `browser_type` → `browser_snapshot`（验证结果）
- **截图取证：** `browser_vision` 截图保存
- **API 验证：** `browser_console` 检查网络请求和 JS 错误
- **后台数据验证：** `terminal` 执行 psql/curl 确认后端状态

### 测试流程（每个功能点）

```
1. browser_navigate → 进入目标页面
2. browser_snapshot → 确认页面加载正常
3. browser_click/type → 执行操作（点击按钮、填写表单、上传文件）
4. browser_snapshot → 验证操作结果（成功提示？错误提示？页面跳转？）
5. browser_console → 检查是否有 JS 错误或失败的 API 请求
6. 记录：✅ 通过 / ❌ 不通过（附截图和错误详情）
```

---

## 三、系统功能点清单（11 模块，60+ 测试点）

### 3.1 认证模块

| # | 功能点 | 操作步骤 | 验证标准 |
|---|--------|---------|---------|
| A1 | 登录（正确凭据） | 填写邮箱+密码 → 点击登录 | 跳转到工作台，不报错 |
| A2 | 登录（错误密码） | 填写错误密码 → 点击登录 | 显示错误提示，不跳转 |
| A3 | 登录（空字段） | 不填直接点登录 | 表单验证提示 |
| A4 | 登录后跳转 | 登录后检查 URL | URL 不含 /login |
| A5 | 退出登录 | 点击退出登录按钮 | 跳转回登录页 |
| A6 | Token 过期 | 清除 localStorage → 访问受保护页面 | 自动跳转登录页 |
| A7 | 未登录访问 | 直接访问 /jobs（无 Token） | 跳转到登录页 |

### 3.2 文件上传模块

| # | 功能点 | 操作步骤 | 验证标准 |
|---|--------|---------|---------|
| F1 | 上传 PNG 图片 | 选择 PNG 文件 → 上传 | 文件出现在已上传列表，API 返回 200 |
| F2 | 上传 PDF 文件 | 选择 PDF 文件 → 上传 | 同上 |
| F3 | 上传文本文件 | 选择 TXT 文件 → 上传 | 同上 |
| F4 | 上传超大文件（>20MB） | 选择大文件 → 上传 | 前端拦截提示文件过大 |
| F5 | HTTP 环境无 checksum | 上传文件，检查网络请求 | 请求体不含空 checksumSha256，API 返回 200 ⭐ |
| F6 | 上传后文件列表 | 上传多个文件 | 列表显示所有已上传文件 |
| F7 | 删除已上传文件 | 点击删除按钮 | 文件从列表移除 |

### 3.3 识别任务模块（核心 ⭐）

| # | 功能点 | 操作步骤 | 验证标准 |
|---|--------|---------|---------|
| J1 | **上传+创建任务完整链路** | 上传文件 → 选 Schema → 点开始识别 | **不出现 400，任务创建成功** ⭐⭐⭐ |
| J2 | 使用示例文件创建任务 | 点击示例文件 → 创建任务 | 任务创建成功 |
| J3 | 不选 Schema 创建 | 不选 Schema 直接点开始 | 表单验证提示请选择 |
| J4 | 不上传文件创建 | 不上传文件直接点开始 | 表单验证提示请上传 |
| J5 | 查看任务列表 | 导航到任务列表 | 列表正常显示，有数据 |
| J6 | 查看已完成任务详情 | 点击已完成任务的"查看" | 显示识别结果、OCR 文本、置信度 |
| J7 | 查看运行中任务详情 | 点击运行中任务的"查看" | **不显示"资源不存在"** ⭐ |
| J8 | 查看 queued 任务详情 | 点击 queued 任务的"查看" | 显示等待中状态 |
| J9 | 删除任务 | 点击删除 → 确认 | 任务从列表消失 |
| J10 | 重新运行任务 | 点击重跑按钮 | 任务状态变为 queued/running |
| J11 | 导出任务结果 | 点击导出按钮 | 下载文件 |
| J12 | 任务列表分页 | 翻页操作 | 正确加载下一页数据 |
| J13 | 按状态筛选 | 选择状态筛选 | 列表只显示对应状态 |
| J14 | 按 Schema 筛选 | 选择 Schema 筛选 | 列表只显示对应 Schema |

### 3.4 识别结果模块

| # | 功能点 | 操作步骤 | 验证标准 |
|---|--------|---------|---------|
| R1 | 查看识别结果 | 进入已完成任务详情 | 结果字段正常显示 |
| R2 | 查看 OCR 文本 | 切换到 OCR Tab | OCR 文本可读 |
| R3 | 查看置信度 | 查看置信度面板 | 数值在 0-100% 之间 |
| R4 | 查看追溯链路 | 查看追溯 Tab | 显示 Provider/模型/耗时 |
| R5 | 图片+字段高亮 | 点击结果字段 | 对应区域高亮 |

### 3.5 Provider 管理

| # | 功能点 | 操作步骤 | 验证标准 |
|---|--------|---------|---------|
| P1 | 查看 Provider 列表 | 导航到 Provider 页面 | 列表正常显示 |
| P2 | 创建 Provider | 填写表单 → 保存 | 新 Provider 出现在列表 |
| P3 | 编辑 Provider | 修改配置 → 保存 | 配置更新成功 |
| P4 | 删除 Provider | 点击删除 | Provider 从列表消失 |
| P5 | 设为默认 | 点击设为默认 | 默认标识更新 |
| P6 | 健康检查 | 点击健康检查 | 显示检查结果 |

### 3.6 Schema 管理

| # | 功能点 | 操作步骤 | 验证标准 |
|---|--------|---------|---------|
| S1 | 查看 Schema 列表 | 导航到 Schema 页面 | 列表正常显示 |
| S2 | 创建 Schema | 填写表单 → 保存 | 新 Schema 出现在列表 |
| S3 | 编辑 Schema | 修改字段 → 保存 | 更新成功 |
| S4 | 发布 Schema | 点击发布 | 状态变为已发布 |
| S5 | 停用 Schema | 点击停用 | 状态变为已停用 |

### 3.7 反馈模块

| # | 功能点 | 操作步骤 | 验证标准 |
|---|--------|---------|---------|
| FB1 | 提交字段反馈 | 在识别结果页提交反馈 | 反馈提交成功 |
| FB2 | 查看反馈列表 | 导航到反馈页面 | 列表正常显示 |
| FB3 | 审核反馈 | 点击审核通过/拒绝 | 状态更新 |

### 3.8 回写模块

| # | 功能点 | 操作步骤 | 验证标准 |
|---|--------|---------|---------|
| W1 | 查看回写列表 | 导航到回写页面 | 列表正常显示 |
| W2 | 执行回写 | 点击执行回写 | 回写状态更新 |

### 3.9 审计日志

| # | 功能点 | 操作步骤 | 验证标准 |
|---|--------|---------|---------|
| AL1 | 查看审计日志 | 导航到审计页面 | 日志列表正常显示 |
| AL2 | 按操作类型筛选 | 选择操作类型 | 列表过滤正确 |
| AL3 | 按时间范围筛选 | 选择时间范围 | 列表过滤正确 |

### 3.10 评测模块

| # | 功能点 | 操作步骤 | 验证标准 |
|---|--------|---------|---------|
| E1 | 查看评测列表 | 导航到评测页面 | 列表正常显示 |
| E2 | 创建评测 | 填写表单 → 创建 | 评测创建成功 |
| E3 | 查看评测详情 | 点击查看详情 | 详情正常显示 |

### 3.11 Dashboard 模块

| # | 功能点 | 操作步骤 | 验证标准 |
|---|--------|---------|---------|
| D1 | 统计数据 | 查看工作台 | 数字正常显示（今日任务/待复核/已完成/Provider） |
| D2 | 任务趋势图 | 查看趋势图 | 图表正常渲染 |
| D3 | 最近任务列表 | 查看最近任务 | 列表有数据 |
| D4 | 快速操作按钮 | 点击"新建识别" | 跳转到新建页面 |

---

## 四、执行计划

### Phase 1：认证模块测试（A1-A7）

- 用浏览器访问登录页，逐个测试登录/登出/权限场景
- 预计耗时：10 分钟

### Phase 2：核心链路测试 ⭐⭐⭐（F1-F7 + J1-J14）

- **这是最关键的 Phase** — 文件上传 + 任务创建完整链路
- 重点验证：上传不报 400、任务创建成功、任务详情不 404
- 预计耗时：30 分钟

### Phase 3：识别结果测试（R1-R5）

- 查看已完成任务的识别结果、OCR 文本、置信度
- 预计耗时：15 分钟

### Phase 4：配置管理测试（P1-P6 + S1-S5）

- Provider 和 Schema 的 CRUD 操作
- 预计耗时：20 分钟

### Phase 5：辅助功能测试（FB1-FB3 + W1-W2 + AL1-AL3 + E1-E3 + D1-D4）

- 反馈、回写、审计、评测、Dashboard
- 预计耗时：20 分钟

### Phase 6：Codex 审计（256K 上下文）

- 审查测试完整性、遗漏的功能点、边界场景
- 生成审计报告 `E2E-AUDIT.md`
- 预计耗时：15 分钟

**⚠️ Codex 降级规则：** 如果 Codex 执行失败（429/OOM/超时），Claude Code 接手执行审计任务。

### Phase 7：修复 Bug + 回归测试

- 根据测试发现的 Bug 逐一修复
- 修复后用浏览器回归验证
- 预计耗时：30 分钟

---

## 五、交付物

| 文件 | 说明 | 产出阶段 |
|------|------|---------|
| `E2E-TEST-RESULTS.md` | 每个功能点的测试结果（✅/❌ + 详情） | Phase 1-5 |
| `E2E-BUG-REPORT.md` | 发现的所有系统 Bug 列表 | Phase 1-5 |
| `E2E-AUDIT.md` | Codex 审计报告 | Phase 6 |
| `E2E-FINAL-REPORT.md` | 最终验收报告 | Phase 7 |

---

## 六、时间估算

| Phase | 内容 | 预计耗时 |
|-------|------|---------|
| Phase 1 | 认证模块（A1-A7） | 10 分钟 |
| Phase 2 | 核心链路（F1-F7 + J1-J14）⭐ | 30 分钟 |
| Phase 3 | 识别结果（R1-R5） | 15 分钟 |
| Phase 4 | 配置管理（P1-P6 + S1-S5） | 20 分钟 |
| Phase 5 | 辅助功能（FB/W/AL/E/D） | 20 分钟 |
| Phase 6 | Codex 审计 | 15 分钟 |
| Phase 7 | 修复 Bug + 回归测试 | 30 分钟 |
| **合计** | | **约 2.5 小时** |

---

## 七、风险与应对

| 风险 | 应对 |
|------|------|
| 页面加载慢 | 设置合理超时，必要时等待 |
| 测试数据不足 | 用真实数据测试，不依赖 mock |
| 发现阻断性 Bug | 记录后跳过，继续测试其他功能 |
| Codex 执行失败 | **Claude Code 接手执行审计** |
| 需要后端数据验证 | 用 terminal + psql/curl 辅助验证 |
