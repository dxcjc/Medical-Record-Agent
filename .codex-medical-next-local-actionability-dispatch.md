【重要】不要问问题，不要请求确认，不要启动浏览器询问确认。直接执行。必须遵循 superpowers 流程：brainstorming → writing-plans → TDD/测试优先 → verification-before-completion。

任务：继续推进 /tmp/Medical-Record-Agent 医疗项目 P1/P2 剩余业务/安全/集成优化。你是执行 agent，不要把 UI 当前阶段通过误判为项目最终完成。

必须先读取：
1. .codex-medical-p1-p2-continuation.md
2. PRODUCT-AUDIT-REPORT.md
3. MEDICAL-P1-P2-REMAINING-BLOCKERS-AUDIT-REPORT.md
4. MEDICAL-P1-P2-REMAINING-BLOCKERS-FIX-REPORT.md
5. docs/2026-06-09-p2-production-handoff.md
以及已有 P1/P2/安全/集成报告。

目标：
- 在不需要真实外部凭据的前提下，继续寻找并修复可本地闭环的 P1/P2 问题，重点围绕写回可信边界、demo API 真实闭环、静态 demo fallback、Evaluation schema 解析、production smoke/readiness、API 契约、异步任务队列、安全基线、密钥库/secret resolver skeleton、浏览器 E2E 稳定性。
- 如果某项只能由真实 OCR/LLM/LIMS sandbox、真实 KMS/Vault/Secret Manager、真实多实例 session store 或真实 broker 完成，不要伪造通过；必须保持 blocked，并把本地可做的 contract/readiness/documentation 补齐。
- 保持企业级 Material + Arco Design UI：Primary #3370FF、背景 #F7F8FA、白色侧栏、active nav pill、DM Sans + Noto Sans SC、移动端抽屉/单列/44px 触摸区。不允许粗暴重写 CSS。

执行要求：
1. 先做代码/报告审计，列出还可本地推进的缺口；不要只写计划，必须实际修复至少一个可本地闭环问题；如果确实没有可本地修复项，必须生成明确的 actionability 审计说明。
2. 测试优先：新增/更新针对修复点的测试或 readiness harness。
3. 必须运行并记录结果：
   - corepack pnpm --filter @medical-record-agent/demo-web test:styles
   - corepack pnpm --filter @medical-record-agent/demo-web test:mobile
   - corepack pnpm --filter @medical-record-agent/demo-web build
   - corepack pnpm test
   - corepack pnpm readiness:deployment（允许 exit 2，但必须解释 blocked 仅限真实外部集成）
4. 验证 9901：检查 http://localhost:9901/ 和 /api/health 可访问，并确认 dist/index.html 与 9901 HTML 引用同一个新 bundle；如 build 后 dist bundle 与 9901 不一致，按项目现有方式刷新静态部署/记录差异，不要误报。
5. 生成报告：
   - MEDICAL-P1-P2-NEXT-LOCAL-ACTIONABILITY-FIX-REPORT.md
   - MEDICAL-P1-P2-NEXT-LOCAL-ACTIONABILITY-AUDIT-REPORT.md

审计报告必须包含 7 个维度：产品概述、功能完整性、业务流程完整性、用户体验、技术实现、问题清单（P0/P1/P2）、验收结论。验收结论必须分层：本地可闭环阶段、真实外部集成、医疗最终产品。

约束：
- 不要提交 git commit。
- 不要修改 .env、node_modules、无关缓存。
- 不要启动并发 agent。
- 遇到 429/上游断流，保留已完成改动和报告草稿，明确记录中断点，不要反复重试。
