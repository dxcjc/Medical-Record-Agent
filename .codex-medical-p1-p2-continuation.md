【重要】不要问问题，不要请求确认，不要启动浏览器询问确认。直接执行。必须遵循 superpowers 流程：brainstorming → writing-plans → TDD/测试优先 → verification-before-completion。

任务：继续推进医疗项目 demo-web 的 P1/P2 优化闭环，保持企业级 Material + Arco Design UI，不要粗暴重写 CSS，不要破坏现有 9901 部署和 API 代理。

工作目录：/tmp/Medical-Record-Agent

背景：
- 已有 MEDICAL-ARCO-UI-AUDIT-REPORT.md，报告显示 P0 UI 全局改造基本完成。
- 当前仍有遗留项：
  1. P1：全量 pnpm test 中存在非 UI Windows 路径 smoke 用例失败（scripts/production-smoke.test.ts 相关，期望 true 实际 false）。需要修复或确认真实代码/测试契约，让全量测试稳定。
  2. P2：demo-web build 主 JS chunk 超过 500k，需要通过路由级 lazy/dynamic import 或合理 manualChunks 优化，不能靠忽略警告掩盖问题。
  3. STYLE-FIX-REPORT.md 当前缺失，需要补齐本轮修复报告。

必须完成：
1. 保持现有 Material + Arco 设计系统：Primary #3370FF、背景 #F7F8FA、白色侧栏、active nav pill、DM Sans + Noto Sans SC、移动端抽屉/单列/44px 触摸区。不允许整体覆盖成旧主题。
2. 修复 P1 测试稳定性问题：定位 Windows 路径 CLI 入口判断失败，优先修正实现代码或测试夹具，使全量相关测试通过。
3. 优化 P2 chunk：对 demo-web 采用安全的路由懒加载和/或 Vite manualChunks，确保 build 不再出现主业务 chunk 超 500k 警告；不得影响首屏登录、路由保护、9901 访问。
4. 补充/更新测试：至少运行并确保通过：
   - corepack pnpm --filter @medical-record-agent/demo-web test:styles
   - corepack pnpm --filter @medical-record-agent/demo-web test:mobile
   - corepack pnpm --filter @medical-record-agent/demo-web build
   - corepack pnpm test（如仍有失败，必须在报告中明确具体失败和是否本轮未能修复，不能笼统写通过）
5. 验证 9901：build 后检查 http://localhost:9901/ 和 /api/health 可访问；确认 dist/index.html 引用了新 bundle。
6. 生成报告：
   - STYLE-FIX-REPORT.md：说明本轮 P1/P2 修复点、文件、验证命令、剩余问题。
   - 更新 MEDICAL-ARCO-UI-AUDIT-REPORT.md：必须保留/补齐 7 个维度：产品概述、功能完整性、业务流程完整性、用户体验、技术实现、P0/P1/P2 问题清单、验收结论。

约束：
- 不要提交 git commit。
- 不要修改 .env、node_modules、无关缓存。
- 不要并发启动其他 agent。
- 不要只写计划，必须实际修复、验证、生成报告。
- 若遇到 429/上游断流，保留已完成改动和报告草稿，明确记录中断点，不要反复重试。