【重要】不要问问题，不要请求确认，不要启动浏览器询问确认。直接执行。必须使用 Open Design MCP 作为设计参考，并遵循 superpowers 流程：brainstorming → writing-plans → TDD/测试优先 → verification-before-completion。

任务：使用 Open Design 优化 Medical Record Agent demo-web 前端显示。

工作目录：/tmp/Medical-Record-Agent

背景：
- 用户要求使用 Open Design 优化两个系统前端显示。本任务只负责医疗项目。
- Open Design daemon 已运行：http://127.0.0.1:7456
- Codex 已接入 open-design MCP。你必须先通过只读 MCP 资源/工具检查 Open Design 是否可用，并在报告中记录。
- 医疗 UI 之前已做过全局修复，报告：MEDICAL-UI-GLOBAL-POLISH-AUDIT-REPORT.md，但用户仍要求进一步利用 Open Design 提升显示效果。

设计方向：
- 保持 Material + Arco Design 企业医疗工作台风格。
- 主色 #3370FF，灰底 #F7F8FA，白侧栏，active pill，高级阴影卡片，DM Sans + Noto Sans SC。
- 不要廉价模板感，不要粗暴重写 styles.css，不要破坏 Arco 组件。
- 面向真实医疗 AI 工作台：识别任务、证据链、隐私合规、Provider 健康、写回队列、复核状态，信息密度要合理。

必须做：
1. 使用 Open Design MCP 做设计参考：至少列出/读取可用资源，或说明 Open Design MCP 当前可用资源情况；不得跳过。
2. 读取并参考：
   - CLAUDE.md（如存在）
   - MEDICAL-UI-GLOBAL-POLISH-AUDIT-REPORT.md
   - MEDICAL-UI-GLOBAL-POLISH-FIX-REPORT.md
   - apps/demo-web/src/styles.css
   - apps/demo-web/src/layouts/AppShell.tsx
   - apps/demo-web/src/pages/recognition/RecognitionDashboardPage.tsx
   - apps/demo-web/src/pages/recognition/NewRecognitionPage.tsx
   - apps/demo-web/src/pages/operations/ProviderSettingsPage.tsx
   - apps/demo-web/src/pages/operations/WritebackPage.tsx
   - apps/demo-web/src/pages/schema/SchemaStudioPage.tsx
   - apps/demo-web/src/pages/evaluation/EvaluationPage.tsx
3. 重点优化前端显示，不做后端大改：
   - 顶部 Hero / 页面标题区更专业，不拥挤。
   - KPI 卡片层级、图标背景、趋势/说明更统一。
   - 最近任务/表格列可读性、横滚、状态标签更优。
   - 隐私选项、上传配置、Provider 设置、写回队列、Schema/Evaluation 页面视觉一致。
   - 移动端：390x844 下按钮、表格、卡片、抽屉不溢出，触摸区 ≥44px。
   - 宽屏：1440/1920 下不显得左右空白大，也不要贴边。
4. 先补/更新 UI 样式守卫或移动端测试，再实现。
5. 使用增量 patch；不要整体重写 CSS；不要 git commit；不要改 .env/node_modules/cache。

必须运行并记录：
- corepack pnpm --filter @medical-record-agent/demo-web test:styles
- corepack pnpm --filter @medical-record-agent/demo-web test:mobile
- corepack pnpm --filter @medical-record-agent/demo-web build
- corepack pnpm smoke:demo-web
- 如可行：corepack pnpm e2e:demo-web:browser
- 检查 http://127.0.0.1:9901/ 与 /api/health，可访问且加载新 bundle。

必须生成：
- MEDICAL-OPEN-DESIGN-UI-FIX-REPORT.md
- MEDICAL-OPEN-DESIGN-UI-AUDIT-REPORT.md

审计报告必须 7 维度：产品概述、功能完整性、业务流程完整性、用户体验、技术实现、问题清单 P0/P1/P2、验收结论。

截图要求：
- 保存桌面：首页/识别看板/新建识别/Provider/写回。
- 保存移动：首页/识别看板/新建识别/Provider/写回。
- 报告写明截图路径。

验收口径：
- 只说 UI 显示优化阶段是否通过。
- 不得把医疗项目整体写成最终完成；真实 OCR/LLM/LIMS、KMS/Vault、生产多实例等外部集成仍需独立验收。
