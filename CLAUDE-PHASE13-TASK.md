# Phase 13: 任务详情页 & 列表页 7 项修复

## 修复清单

### 1. 追溯链路为空
**问题**：trace tab 有可点击元素但没有文本内容。
**根因**：追溯链路数据可能没有从后端正确返回，或前端渲染逻辑有问题。
**修复**：检查 `/results/:jobId` API 返回的 trace 数据结构，确保前端正确渲染每个 trace node 的 status、message、timing 等信息。

### 2. 列表"需复核"与详情不一致
**问题**：列表显示"需复核 25"，详情显示"需复核 0"。
**根因**：Phase 11 修改了详情页的 needReview 计算（排除空字段），但列表页的 needReview 是后端 jobs API 直接返回的，可能用的是旧计算逻辑。
**修复**：统一后端 jobs 列表 API 和 results API 的 needReview 计算逻辑，都排除空值字段。

### 3. 置信度分布缺空字段列
**问题**：只有"高≥80 / 中50-80 / 低<50"三列，空字段不显示。
**修复**：在置信度分布区域增加"空字段"列，显示空字段数量。格式：
```
高 ≥80    19 个字段  100%
中 50-80   0         0%
低 <50     0         0%
空字段     6         ← 新增
```

### 4. 列表状态与详情不一致
**问题**：详情页显示"已完成"，列表显示"部分完成"。
**根因**：后端 jobs 列表 API 返回的 status 可能和 results API 返回的 status 不一致。
**修复**：统一 status 计算逻辑。如果所有字段都已填写（无空字段）则"已完成"，有空字段则"部分完成"。

### 5. 置信度 98% 显示问题
**问题**：用户质疑 98% 的来源。
**说明**：98% 是 19 个已填写字段的平均置信度（90+100+100+100+100+100+100+100+100+100+100+100+100+100+100+100+100+100+100)/19 ≈ 98.4%，四舍五入 98%。这是正确的行为，但可以在 UI 上增加说明。

### 6. Provider 信息不显示
**问题**：任务详情和评测中心都没有显示使用了哪个 Provider。
**修复**：
- 任务详情页：从 job 记录中提取 providerKey，显示 Provider 名称
- 评测中心运行记录：从 run 记录中提取 providerKey
- 如果 providerKey 为空，显示"未指定"而不是"-"

### 7. 证据片段样式优化
**问题**：证据片段显示 "patientName-"、"patientGender-" 这种原始 key，不友好。
**修复**：
- 将 field key 映射为中文标签（用 schema 的 fieldLabel）
- 使用小卡片样式，每个证据片段一个卡片
- 卡片内显示：字段标签、值、置信度、来源摘要

### 8. 复核反馈交互优化
**问题**：复核反馈在页面最底部，不好找。
**修复**：改为类似"查看原图"的交互逻辑——在页面顶部操作栏增加"提交反馈"按钮，点击后弹出侧边抽屉(Drawer)显示反馈表单。

## 关键文件
- `medical-ui/src/pages/JobDetailPage.tsx` — 任务详情页
- `medical-ui/src/pages/JobListPage.tsx` — 任务列表页
- `apps/api/src/routes/jobs.ts` — 任务 API
- `apps/api/src/routes/results.ts` — 结果 API
- `apps/api/src/repositories/jobs.repository.ts` — 任务仓库
- `apps/api/src/repositories/results.repository.ts` — 结果仓库

## 验证
1. 追溯链路正常显示每个节点的 status、message、timing
2. 列表"需复核"与详情一致
3. 置信度分布有 4 列（高/中/低/空字段）
4. 列表状态与详情状态一致
5. Provider 信息正确显示
6. 证据片段使用小卡片样式，显示中文标签
7. 复核反馈通过顶部按钮打开侧边抽屉
