任务：修复医疗项目 /tmp/Medical-Record-Agent 产品级审计报告中的 P0/P1 问题，并重新生成审计报告。

【角色】你是开发修复 Agent。不要问问题，不要等待确认，直接修复、验证、写报告。

【背景】PRODUCT-AUDIT-REPORT.md 结论：不通过。必须优先修复以下 P0/P1。

【必须修复的问题】
[P0-1] 前端 TypeScript 构建失败，项目无法交付生产前端产物。
- 文件/功能：apps/demo-web/src/pages/recognition/JobDetailPage.tsx:529，apps/demo-web/src/pages/operations/WritebackPage.tsx:484，apps/demo-web/src/api/types.ts。
- 修复要求：反馈 payload 只在值存在时添加 evidenceId/evidenceQuote；createWritebackRequest 返回 confirmed 字面量 true 或明确类型；pnpm typecheck 和 pnpm build 必须通过。

[P1-1] 写回执行信任客户端 fields/payload，服务端未绑定已验证 readyFields。
- 文件/功能：apps/api/src/routes/writeback.routes.ts，apps/api/src/bootstrap/production-services.ts，LIMS 写回。
- 修复要求：POST /writeback 执行数据只能来自服务端 RecognitionResult.payload.writeback.readyFields；客户端只提交 jobId/confirmed/可选幂等键；服务端重新读取 job/result、确认状态和无阻塞写回；补测试覆盖客户端篡改 fields/payload 不会被执行。

[P1-2] 默认 demo API 创建识别任务不执行 OCR/LLM/校验，核心流程在默认运行方式下不闭环。
- 文件/功能：apps/api/src/index.ts、apps/api/src/demo-services.ts。
- 修复要求：demo 模式也使用 mock OCR + mock LLM + core orchestrator 或等效完整 mock 编排，并将结果与 jobId/sourceFileId 关联；不要对任意 jobId 返回同一静态结果；补测试。

[P1-3] 详情页和写回页用静态 demo 数据兜底，掩盖真实 API 失败。
- 文件/功能：apps/demo-web/src/pages/recognition/JobDetailPage.tsx、apps/demo-web/src/pages/operations/WritebackPage.tsx。
- 修复要求：生产/默认真实 API 失败时显示错误空态和重试，不展示静态 demo 数据；仅显式 VITE_DEMO_MODE=true 时可展示，并标识“演示数据，不可写回”；补测试。

[P1-4] Evaluation 创建 run 接受 schemaKey，但生产 runner 固定使用 LIMS 内置 schema。
- 文件/功能：apps/api/src/services/api-services.ts、apps/api/src/bootstrap/production-services.ts。
- 修复要求：评估 runner 按 runInput.schemaConfig.schemaKey/schemaVersionId 解析 active schema，与生产识别 schema resolution 复用逻辑；metrics 或 run 记录实际 schemaVersionId；补测试。

[P1-5] 生产 smoke 未配置，外部集成未完成可执行验收。
- 文件/功能：scripts/production-smoke.ts、README/环境配置。
- 修复要求：提供可在本地 demo/production-like 环境运行的 smoke 配置说明或脚本 fallback；至少覆盖 /status、登录、Provider 列表/health；无法真实外部 LIMS 时明确 sandbox/跳过条件，命令不能因缺少 env 无说明地失败。

[P1-6] API 路由契约大量使用 unknown，前后端契约漂移风险。
- 文件/功能：apps/api/src/routes/*.ts、apps/demo-web/src/api/types.ts、apps/demo-web/src/api/client.ts。
- 修复要求：优先为本次涉及的 feedback/writeback/jobs/evaluation 请求补共享 DTO 或 Zod/schema validation/明确 TypeScript 类型；至少消除已导致 build 失败的契约漂移并补类型测试。

【修复后必须做的事】
1. 运行 pnpm test、pnpm typecheck、pnpm build；必要时补充 demo-web/api/core/shared 分包 build。
2. 生成修复报告 P0-P1-FIX-REPORT.md，说明改动文件、测试结果、遗留 P2。
3. 重新生成 PRODUCT-AUDIT-REPORT.md，必须包含 7 个一级维度：产品概述、功能完整性、业务流程完整性、用户体验、技术实现、P0/P1/P2 问题清单、验收结论。若仍有 P0/P1，结论必须不通过；若只剩 P2 且构建测试通过，可结论通过/有条件通过。

不要问问题，不要只写计划，直接修复、验证、写报告！
