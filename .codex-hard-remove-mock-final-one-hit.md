【必须遵循 superpowers 流程】
继续 hard remove mock provider 收尾。调度者二次复验发现上一轮报告说无命中，但实际仍有 1 个用户/当前文档主线残留，必须修复并更新最终扫描报告。

残留位置：
- `docs/superpowers/specs/2026-06-04-medical-record-recognition-agent-design.md:318`
- 当前文本含：`调用真实 OCR provider 或 mock OCR provider。`

用户标准：不是占位，是删除 mock 相关内容；后面用户提供真实模型提供商。当前文档主线不得再出现 mock provider / mock OCR provider / mock model provider / mock-ocr / mock-model / development_placeholder / 开发占位 provider。

任务：
1. 修复上述残留，不要伪造历史；该旧 spec 已被 hard remove 方案取代，可把对应流程改为真实 OCR provider / 测试替身语义，或在历史草案说明中明确不作为当前方案。
2. 重新运行同一禁词扫描，排除 `.test.*`、历史报告 `*REPORT.md`、任务 prompt/log、构建产物后，必须 0 命中。
3. 更新 `MEDICAL-HARD-REMOVE-MOCK-PROVIDER-FINAL-SCAN.md`，如实记录这次发现 1 个残留并已修复。
4. 更新 `MEDICAL-HARD-REMOVE-MOCK-PROVIDER-AUDIT-REPORT.md` 的验收结论/问题清单，说明最终扫描现在 0 命中。
5. 如改动很小，可只跑文档扫描；但必须至少检查 `http://127.0.0.1:9901/` 和 `/api/health` 没受影响。若触及代码则跑相关测试。

不要问问题，不要提交 git commit。