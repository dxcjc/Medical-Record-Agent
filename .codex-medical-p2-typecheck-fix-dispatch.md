【重要】不要问问题，不要请求确认，不要启动浏览器询问确认。直接执行。必须遵循 superpowers 流程：brainstorming → writing-plans → TDD/测试优先 → verification-before-completion。

任务：修复 /tmp/Medical-Record-Agent 当前 P0 typecheck 阻断，并重新生成 7 维度产品级审计报告。

背景：统一产品审计调度本轮重新验证医疗项目，执行：
`corepack pnpm typecheck && corepack pnpm test && corepack pnpm --filter @medical-record-agent/demo-web build && corepack pnpm smoke:demo-web && corepack pnpm readiness:deployment`
当前在 `corepack pnpm typecheck` 阶段失败，错误如下：
- `apps/api/src/auth/auth.service.test.ts` 从 `./auth.service` 导入的 `createInMemorySessionInvalidationStore`、`createRepositorySessionInvalidationStore`、`hashSessionToken` 不存在。
- `apps/api/src/bootstrap/production-services.test.ts` 从 `./production-services` 导入的 `buildProductionSessionInvalidationStoreContract`、`createProductionSessionInvalidationStore` 不存在。
- `apps/api/src/server.test.ts` 某个 session invalidation store contract 参数类型被推断为 `undefined`。

必须修复的问题：
[P0-1] 医疗项目 typecheck 失败，当前最新审计报告声称 typecheck/test/build 通过已不可信。
- 修复要求：不要删除测试来掩盖问题；应恢复/补齐 session invalidation store、token hash、production contract 等真实实现，或修正导出/类型契约，使测试和实现一致。
- 必须保持已有 P2 安全会话目标：HttpOnly `mra_session` cookie、logout 失效 session、旧 session 轮换、Bearer/API token 兼容、生产前端默认不持久化 JWT。

修复后必须做：
1. 至少运行并记录结果：
   - `corepack pnpm typecheck`
   - `corepack pnpm test`
   - `corepack pnpm --filter @medical-record-agent/demo-web build`
   - `corepack pnpm smoke:demo-web`
   - `corepack pnpm readiness:deployment`（若 externalIntegration blocked，必须如实记录 localReadiness 与 blocked 项，不能写最终通过）
2. 更新或生成修复报告：`MEDICAL-P2-TYPECHECK-FIX-REPORT.md`。
3. 更新或生成审计报告：`MEDICAL-P2-TYPECHECK-AUDIT-REPORT.md`，必须包含 7 个维度：
   1. 产品概述
   2. 功能完整性
   3. 业务流程完整性
   4. 用户体验
   5. 技术实现
   6. P0/P1/P2 问题清单
   7. 验收结论（通过/不通过 + 改进建议）
4. 报告口径必须明确：医疗项目不是最终完成；UI 当前阶段值得肯定但仍有细节可优化；真实 OCR/LLM/LIMS sandbox、KMS/Vault、broker 多实例、production smoke 若未具备则列为 blocked。

约束：
- 不要提交 git commit。
- 不要修改 .env、node_modules、无关缓存。
- 不要只写计划，必须实际修复、验证、生成报告。
- 若遇到 429/上游断流，保留已完成改动和报告草稿，明确记录中断点，不要反复重试。
