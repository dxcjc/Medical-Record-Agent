【重要】不要问问题，不要请求确认，不要启动浏览器询问确认。直接执行。必须遵循 superpowers 流程：brainstorming → writing-plans → TDD/测试优先 → verification-before-completion。

任务：继续推进医疗项目 P1/P2 业务/安全/集成优化闭环，不要停留在 UI 阶段；保持企业级 Material + Arco Design UI，不要粗暴重写 CSS，不要破坏现有 9901 部署和 /api/health。

工作目录：/tmp/Medical-Record-Agent

必须先读取：
1. .codex-medical-p1-p2-continuation.md
2. PRODUCT-AUDIT-REPORT.md
3. MEDICAL-P1-P2-CONTINUATION-ROLLUP-AUDIT-REPORT.md
4. MEDICAL-P2-PRODUCTION-CLOSURE-AUDIT-REPORT.md
5. docs/2026-06-09-p2-production-handoff.md（如存在）

当前已验证状态（不要重复否定）：
- UI 当前阶段通过，但医疗项目最终产品没有完成。
- corepack pnpm --filter @medical-record-agent/demo-web test:styles 通过。
- corepack pnpm --filter @medical-record-agent/demo-web test:mobile 通过。
- corepack pnpm --filter @medical-record-agent/demo-web build 通过。
- corepack pnpm test 通过，只有既有 DEP0040 punycode warning。
- http://localhost:9901/ 和 http://localhost:9901/api/health 可访问，dist/index.html 已引用新 bundle。

本轮目标：继续推进可在当前环境本地落地的 P1/P2 残余，不伪造真实外部条件通过。

优先修复/收敛：
1. API route/response contract 继续收敛：重点检查 schemas、providers、audit 以及底层 repository/service 中仍为 unknown 或宽响应的关键路径；用 Zod DTO、共享类型或明确 response guard 收敛，补测试证明非法输入/未知字段/响应形状不会漂移。
2. 生产交接与 blocked 可执行性：更新/补强 production handoff，让部署方清楚真实 OCR/LLM/LIMS sandbox、真实 KMS/Vault/Secret Manager、多实例 session store、真实 broker 队列的 env、smoke、失败/blocked 判定。
3. 安全基线继续工程化：不得接入假的 KMS；但要确保 secretRef 不泄漏、provider health/log/response 不回显明文密钥，必要时补回归测试。
4. 不要做大规模 UI 重写；如触及 demo-web 样式，必须保持 Material + Arco Design：Primary #3370FF、背景 #F7F8FA、白色侧栏、active nav pill、DM Sans + Noto Sans SC、移动端抽屉/单列/44px 触摸区。

必须运行并在报告中记录：
- corepack pnpm --filter @medical-record-agent/demo-web test:styles
- corepack pnpm --filter @medical-record-agent/demo-web test:mobile
- corepack pnpm --filter @medical-record-agent/demo-web build
- corepack pnpm test
- 如涉及 API contract，运行相关 vitest 定向测试
- 检查 http://localhost:9901/ 和 /api/health；确认 dist/index.html 引用新 bundle

必须生成：
1. /tmp/Medical-Record-Agent/MEDICAL-P1-P2-CONTRACT-HARDENING-FIX-REPORT.md：说明修复点、文件、测试、仍 blocked 的外部条件。
2. /tmp/Medical-Record-Agent/MEDICAL-P1-P2-CONTRACT-HARDENING-AUDIT-REPORT.md：必须包含 7 个维度：产品概述、功能完整性、业务流程完整性、用户体验、技术实现、P0/P1/P2 问题清单、验收结论。

验收结论必须分层：
- UI 当前阶段
- P1/P2 本轮 contract/security/handoff 阶段
- 真实外部集成
- 医疗项目最终产品

约束：
- 不要 git commit。
- 不要修改 .env、node_modules、缓存目录。
- 不要把真实外部 sandbox/KMS/broker/session store 写成已通过。
- 不要只写计划，必须实际修复、验证、生成报告。
- 若遇到 429/上游断流，保留已完成改动和报告草稿，明确记录中断点，不要反复重试。
