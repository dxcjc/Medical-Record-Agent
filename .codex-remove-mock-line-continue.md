【必须遵循 superpowers 流程】
继续执行上一轮 `remove mock line` 半成品任务。不要重做已完成内容，只完成剩余项、修复测试/构建，并生成报告。

背景：上一轮 Codex 因上游 503 中断，已经产生部分改动：
- 新增计划 `docs/superpowers/plans/2026-06-09-remove-mock-line.md`
- `NewRecognitionPage.tsx` 已开始过滤 mock provider，默认不再选 mock，未配置真实 provider 时阻断创建
- `AppShell.tsx`、`ProviderSettingsPage.tsx`、`production-services`、provider routes/tests 等已有部分改动
但还没有生成：
- `MEDICAL-REMOVE-MOCK-LINE-FIX-REPORT.md`
- `MEDICAL-REMOVE-MOCK-LINE-AUDIT-REPORT.md`
也未完成最终测试验证。

你必须先检查当前 git diff 和上一轮日志 `.codex-remove-mock-line-task.log`，识别已完成/未完成，不要覆盖好改动。

剩余验收目标：
1. 用户可见主线不再出现 “Mock Provider Ready”。
2. 新建识别不默认 mock-ocr/mock-model；只有 mock provider 时创建按钮禁用，并提示配置真实 OCR/LLM Provider。
3. Provider 设置页普通用户主路径不再可选 Mock；不显示 mock endpoint/mock secret/mock health 文案。
4. API 可以保留内部 mock，但必须标识 `isMock: true` / 开发占位，前端过滤它，不当作真实 provider。
5. README/用户可见页面文案保留测试说明可以，但操作主线必须改成真实 Provider 配置路径。
6. 单元测试里的 vi mock/fetch mock 不要删除。

当前已知残留需要重点核对：
- `apps/demo-web/src/pages/evaluation/EvaluationPage.test.ts` 里的 mock-model 是测试 fixture，是否需要改取决于是否用户可见；不要为了 grep 干掉测试。
- `apps/api/src/bootstrap/production-services.ts` 中 fallback key 仍有 mock-ocr/mock-model，必须确认它不会被前端当真实默认；如保留，必须 isMock 标识清楚。
- `README.md` 第 163 行测试说明可保留，但应明确“仅测试/CI”，并把真实操作路径写在用户操作章节。

必须执行验证：
- `corepack pnpm --filter @medical-record-agent/demo-web test:styles`
- `corepack pnpm --filter @medical-record-agent/demo-web test:mobile`
- `corepack pnpm --filter @medical-record-agent/demo-web build`
- 相关测试：至少跑 demo-web NewRecognition/AppShell/ProviderSettings 测试和 API provider 相关测试；能跑全量 `corepack pnpm test` 就跑全量。
- `corepack pnpm smoke:demo-web` 可行则执行。
- 检查 `http://127.0.0.1:9901/` 和 `/api/health`；如运行环境未吃到新 build，说明并列为剩余项，不要谎称已部署。

必须产出：
1. `MEDICAL-REMOVE-MOCK-LINE-FIX-REPORT.md`
2. `MEDICAL-REMOVE-MOCK-LINE-AUDIT-REPORT.md`

审计报告必须包含 7 维度：产品概述、功能完整性、业务流程完整性、用户体验、技术实现、问题清单 P0/P1/P2、验收结论。

结论口径：
- 如果用户可见主线已去掉 mock，写“去 mock 用户主线阶段通过”。
- 如果内部 mock 保留，仅说明“内部测试/CI 可用，不作为真实用户操作路径”。
- 如果真实 OCR/LLM/LIMS 尚未配置，必须说明“真实外部集成待配置”，但 UI 不再误导 mock ready。

不要问问题，不要提交 git commit。